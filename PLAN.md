# Timetable PWA — implementation record

> **Archive note (Pass 67, 11 September 2026):** the visual UI audit package referenced in Passes 56–63 as `visual-ui-audit-2026-09-10/` now lives at [archive/enhancements/2026-09-10-visual-ui/](archive/enhancements/2026-09-10-visual-ui/README.md) (audit, batch plan, gap audit, designs, evidence and the closing capture matrix).

## Current architecture and Phase 1 implementation

The React/Vite PWA is hosted on GitHub Pages and Vercel. Two separately deployed
Cloudflare Workers provide push/cron/sync/analytics/study groups and the calendar feed;
KV holds worker state. The deployed topology is documented in DEPLOYMENT.md. Public
Google Sheets are read using GViz and recognised columns. CSV fallback, tab discovery
and a mapping/preview wizard described in the historical design below are not implemented.

Phase 1 corrective work (implementation branch `codex/phase-1`):

- Permanently disable legacy `/test`; add `/test-device` bound to the browser subscription's
  endpoint and auth/public key capability. Preserve worker-first compatibility: newer clients
  never fall back to the broadcasting endpoint on an old worker.
- Bound subscription/test JSON, validate supported HTTPS push services and encryption keys,
  reject credential replacement, disable redirects and use per-subscription test cooldowns.
  No administrator broadcast endpoint is added. Existing VAPID state remains untouched.
- Keep incomplete past deadlines in the list and summary, expose personal-only key dates,
  and suppress completed deadlines in the foreground notification loop.
- Add isolated worker, Web Push encryption/VAPID, client, deadline and deployment regression
  checks. Share a fail-closed build/test gate across Pages and Vercel, test PRs, and identify
  manual releases using a unique workflow-dispatch ID plus expected commit. Timeout/failure
  stops release. Direct worker driver targets now run the gate first.
- Correct source, attachment-sync, location-provider and pseudonymous-analytics copy.
- Broader identities, record sync, calendar coverage and background task-completion parity
  remain later roadmap phases; this release does not claim to fix them.

See tests/README.md for checks and DEPLOYMENT.md for compatible release and recovery.
Validation used synthetic subscriptions and in-memory KV; it did not send production test
pushes, write analytics pings or modify user KV records. Both Pages-path and Vercel-root
checks passed: six unit cases and three browser/client cases each. The reminder fix also
waits for saved completion metadata before the first check at startup/profile switch.

Worker-first release: push version `3f924dfb-0c72-49a4-9426-423ed0f6587e`; unchanged
calendar worker version `25b774b3-55ac-4e4e-9500-a9fc9ee40fe7`. The frontend is released
through the gated main-branch workflow. Hosted release outcomes are recorded in the
Phase 1 handoff; local tests alone do not demonstrate a successful hosted build.

## Historical design (superseded where it differs from the current architecture)

The following is retained as design history, not a statement that every planned feature shipped.

## Goal

A mobile-friendly PWA that reads a timetable from a Google Sheet (e.g. the UCL PGCE "Group 2 Timetable"), lets you filter it — including automatically hiding the specialism sessions you don't attend — and remembers everything (sheet URL, chosen tab, all filter choices) so opening the app just shows *your* timetable.

## How the sheet is read (decided)

The sheet is public, so the app fetches it directly from the browser with no API key and no server, using Google's GViz endpoint (`https://docs.google.com/spreadsheets/d/{ID}/gviz/tq?tqx=out:json&gid=...`), with the CSV export endpoint as a fallback. Both return typed/tabular data and support listing values per tab. The whole product is a static site — free hosting, no accounts, no secrets. (If the sheet were ever made private, the fallback would be Google Sheets API + OAuth — deliberately out of scope.)

## Architecture (decided)

- **Frontend-only static SPA** — no backend, no database, £0 total cost. Stack: **Vite + React + TypeScript**.
- **Hosting: GitHub Pages**, free forever for public repos. Deploys automatically on every push to `main` via a GitHub Actions workflow (also free for public repos). The app lives at `https://<username>.github.io/timetable-pwa/`, so Vite is configured with `base: '/timetable-pwa/'` and routing uses hash or no routing at all (single screen + settings panel — no router needed).
- **Persistence: `localStorage`** for settings (sheet URL, tab, filters, specialism choices) and **cached last-fetched sheet data** so the timetable still displays offline or when Google is slow.
- **PWA layer**: web manifest (installable to home screen) + service worker via `vite-plugin-pwa` (caches app shell + last data; "last updated X ago" indicator with manual refresh). Service workers and installability work fine on GitHub Pages (HTTPS, scoped to the base path).

## Data model (what gets saved)

```
settings {
  sheetUrl            // full pasted URL; sheet ID parsed out of it
  selectedTabGid      // which tab, e.g. "Group 2 Timetable"
  columnMap           // auto-detected: Title/Day/Date/Start/End/Room/Groups/Tutor/Subject/Link
  mySpecialisms[]     // e.g. ["Music"] — specialism sessions not in this list are hidden
  filters { dateRange, subject[], tutor[], room[], showSelfStudy }
  hideSpecialismsAutomatically: true/false
  activeView          // "day" | "week" | "month" — last-used view restored on open
}
cache { fetchedAt, rows[] }
```

## Core behaviours

1. **Setup flow (first run only)**: paste sheet URL → app fetches tab list → pick your tab → app parses the header row and previews the data → confirm. Never asked again unless you open Settings.
2. **Parsing**: header-row detection with a sensible mapping for the known columns (Title, Day, Date, Start, End, Room, Groups, Tutor, Subject, hyperlink). Dates like `2-Sep-2026` and times like `9:30` parsed into real datetimes so "today/this week" views work.
3. **Specialism filter (the headline feature)**: the app detects rows whose Title matches `Specialism N - <name>` and extracts the distinct specialism names (Art & Design, Music, Computing, PE, …). A one-time picker asks "which specialism(s) are yours?" — after that, all other specialism rows are hidden automatically, forever, across all future weeks in the sheet. The pattern is configurable in case the naming changes.
4. **General filters**: by date range (Today / This week / All), subject, tutor, room, and a toggle for Self Study rows. All selections persist.
5. **Display — three views, switchable via a segmented control, last-used view remembered**:
   - **Day/agenda view (default)**: opens on **today**, with a continuous vertical scroll into the following days (infinite agenda list, day headers sticky as you scroll). A "Today" button jumps back. Each session card shows time, title, room, tutor, and a tappable Moodle link. This is the primary mobile experience.
   - **Week view**: a time-grid for the week (Mon–Fri by default, weekend shown only if sessions exist), current week first, swipe/arrows to move between weeks. Denser layout suited to desktop and tablet; on phones it renders as a compact vertical list per day.
   - **Month view**: a calendar grid with dots/short labels per day for a quick "which days am I in" overview; tapping a day jumps to that day in the agenda view.
   - All views respect the same active filters (specialisms etc.), highlight "now/next" on today, and are verified at mobile width.
6. **Refresh**: data re-fetched on app open (when online) and via pull-to-refresh/refresh button; otherwise served from cache.
7. **ICS export (core, not a nice-to-have)** — two levels:
   - **`.ics` file download (P4a)**: one tap exports the *filtered* timetable (your specialisms only) as a standard .ics file, importable into Google/Apple/Outlook calendars. Fully client-side, works on GitHub Pages.
   - **Subscribable ICS feed (P4b)**: a URL you add once to your calendar app, which then stays in sync as the sheet changes. A static site can't serve this, so it's a tiny **Cloudflare Worker (free plan, no card)**: the app builds you a feed URL encoding the sheet ID, tab and your filter choices as query parameters; the Worker fetches the public sheet on each request, applies the filters, and returns ICS. No database, no secrets — still £0.

## Nice-to-haves (after MVP, in rough priority order)

- ✅ "Next session" card / countdown at the top (P5).
- ✅ Room-name shortening ("IOE - Bedford Way (20) - 631" → "Bedford Way 631") (P5).
- ✅ Colour-coding by subject (P5).
- Multiple saved sheets/profiles — moved to Future enhancements below.

## Edge cases planned for

- **CORS/fetch failures** → clear error + serve cached data.
- **Sheet structure changes** (columns reordered/renamed) → column mapping re-runs and warns rather than breaking.
- **Merged/blank cells and grouped rows** (common in timetable sheets) → forward-fill logic during parse.
- **The "Groups" column** (`1,2,3,...` vs `2`) → treated as data, optionally filterable, but not used to hide rows by default since the tab is already group-specific.
- **Hyperlinks**: the GViz endpoint doesn't return cell hyperlinks directly — but the example sheet has the URL as a visible column, so it is read as text. (If a future sheet embeds links in the title cell instead, fall back to the HTML export to recover them — noted as a known limitation.)

## Build phases

1. ✅ **P1 — Fetch & display**: scaffold Vite + React + TS project, URL input, parse GViz JSON, render the day/agenda view opening on today with scroll-ahead. (Also added later: tap a session for a detail sheet with date/time/duration/location/tutor/Moodle link.)
2. ✅ **P2 — Filters & persistence**: specialism detection/picker, general filters, localStorage save/restore, settings screen.
3. ✅ **P3 — Views + PWA + deploy**: week and month views with the segmented switcher (date-range filter moved into the Filters sheet); manifest, service worker (`vite-plugin-pwa`), offline cache; GitHub repo + Actions workflow deploying to GitHub Pages.
4. ✅ **P4 — ICS export**: (a) client-side .ics file download of the filtered timetable (Settings → Calendar export); (b) subscribable feed worker written in `workers/ics-feed` — deploy with `npx wrangler deploy`, then paste the workers.dev URL into Settings → Calendar feed.
5. ✅ **P5 — Polish/nice-to-haves**: Now/Next card at the top of the day view (current session with time remaining, or the next upcoming one with a countdown; tap for details), room-name shortening on cards ("IOE - Bedford Way (20) - 631" → "Bedford Way 631", full name kept in the detail sheet), deterministic per-subject colour coding on day cards and week-grid events.

Each phase is independently usable — after P1 the timetable is already viewable.

## Future enhancements (beyond P5)

Ideas that would meaningfully improve the product, roughly ordered by value-for-effort. Effort: S (hours), M (a day or two), L (multi-day / needs new infrastructure).

**Status of the original list:** 1–8, 10, 12 built; 13 covered by the travel/directions work. Still open: **9** (private-sheet OAuth — only if a sheet can't be public) and **11** (ICS feed hardening — waits on the worker being deployed to Cloudflare). A second round of ideas is logged below in **Future enhancements — round 2**.

**Built (2 Sep 2026, fourth pass):** key dates render as solid highlighted blocks in the day agenda (with month-view pins, a Filters toggle, a tappable view-all list, and search inclusion); Ko-fi support link (ko-fi.com/awsaqib) in Settings and README; public Vercel hosting at pgce-timetable.vercel.app alongside GitHub Pages.

**Built (2 Sep 2026, third pass):** leave alerts (notify with a chosen head start before start − live travel time), an OpenStreetMap embed in session details, **optional-session handling** (sessions titled "(optional)" get a badge and a Filters toggle to hide them), and **key dates** — Settings → Key dates takes the submissions-tab URL, upcoming deadlines show as a countdown strip on the day view (urgent styling within 7 days) and a full list with Today/Tomorrow/in-Nd chips.

**Built (2 Sep 2026, second pass):** item 12 (background freshness) is implemented — a "new version available" refresh toast, a NetworkFirst service-worker cache for the sheet data (fetches work offline), and Periodic Background Sync (Chrome, installed PWAs) that re-fetches cached sheet data in the background. Reminders were upgraded to **multiple configurable offsets** (Settings → Session reminders: any combination of 5 min–2 hours, e.g. 1 hour AND 15 min before each session, with the legacy single setting migrated). New beyond the original list: **walking times & directions** — Settings → Travel times uses device location (never leaves the device) to estimate the walk to each session, with rooms cross-checked against a UCL Bloomsbury campus gazetteer (20 Bedford Way, Darwin, Cruciform, Wilkins, and ~17 more buildings) and a Google Maps walking-directions link per session (this also covers item 13).

**Built (2 Sep 2026):** items 1–8 and 10 below are implemented — share links (Settings → Share this setup), change detection (bell icon + changes sheet), search (magnifier in the header), per-session Google Calendar buttons, multiple timetable profiles (Settings → Timetables, with migration from the single-profile storage), group filtering (Filters → My group), term-start week numbers (Settings), reminders (Settings → Session reminders; fires while the app is open/installed — the note in item 8 about calendar-app alerts via the ICS feed still applies for guaranteed delivery), and attendance/notes (in each session's detail sheet, with badges on cards). Items 9 and 11–13 remain open.

1. **Share your setup via link (S)** — encode the sheet ID, tab and specialism choices into a URL (`?setup=…`) so a coursemate opens the app fully configured with one tap. Highest-value social feature; pure client-side.
2. **Change detection & highlights (M)** — on each refresh, diff the new sheet data against the cached copy and badge what changed: "Room changed for Maths 2", added/cancelled sessions, with a small changelog sheet. Turns silent timetable edits into visible alerts.
3. **Search (S)** — a quick search box across titles, tutors and rooms ("when do I next have KaW?"). Client-side filter over loaded sessions.
4. **Per-session "Add to calendar" (S)** — a Google Calendar template-URL button in the session detail sheet for one-off adding, complementing the bulk .ics/feed export. No API needed.
5. **Multiple timetables/profiles (M)** — save more than one sheet (next year's timetable, partner's group) and switch between them; each profile keeps its own filters. Mostly a storage-schema change (array of profiles instead of one settings object).
6. **Groups-aware filtering (M)** — parse the Groups column (`2` vs `1,2,3,…`) properly so a single mixed-groups tab can be filtered to "my group", making the app work for cohorts whose sheets aren't split per group.
7. **Week/term awareness (S)** — configurable term start date to show "Week 3" labels in headers and the week view, matching how courses talk about time.
8. **Session reminders (L)** — true push notifications before sessions need a push backend (another small worker + Web Push subscriptions). Note the pragmatic version already works today: subscribe via the ICS feed and let the calendar app's native alerts do the reminding.
9. **Private-sheet support (L)** — Google Sheets API + OAuth (the original Option B) for sheets that can't be link-shared. Significant auth complexity; only worth it if a future sheet can't be public.
10. **Attendance & notes (M)** — tick off attended sessions and attach personal notes per session (localStorage), e.g. "bring PE kit"; optional export.
11. **ICS feed hardening (S)** — once the worker is deployed: an unguessable token in the feed path (the URL currently encodes only the already-public sheet ID), custom calendar name, and per-subject colour hints where clients support them.
12. **Background freshness (M)** — Periodic Background Sync (where supported) so the service worker refreshes sheet data before the app is opened; plus an in-app "new version available" toast when the PWA updates.
13. **Room directions (S)** — link the location in the session detail to Google Maps (building name + "London"), handy in the first weeks on an unfamiliar campus.

## Future enhancements — round 2 (logged 2 Sep 2026)

A fresh pass over the app as it now stands. Effort: S (hours), M (a day or two), L (multi-day / new infrastructure).

**Built (2 Sep 2026, seventh pass): visual route timeline.** The session detail's "best route" is now a step-by-step timeline: one row per leg with a mode icon, an official-colour line badge (Victoria blue, bus red, Overground orange…), from → to stops, per-leg minutes, and the live total — replacing the plain-text route line.

**Built (2 Sep 2026, sixth pass): live TfL disruption awareness.** With public transport selected: an amber collapsible banner on the day view lists lines not running good service (strikes, closures, delays — refreshed every 5 min); session details show the live-recommended route ("Best route now: 73: King's Cross St. Pancras → Euston · then walk") which TfL's journey planner already steers around disruptions, plus ⚠ warnings when the recommended route's own lines are disrupted; and leave alerts append a TfL disruption note when firing.

**Built (2 Sep 2026, fifth pass): items 1–9 below.** Key-date reminders (7/3/1-day chips under Settings → Key dates); key dates in the .ics download and worker feed as 📌 all-day events; backup export/import (Settings → Backup, JSON of all profiles + notes/attendance) and the share link now carries term start, travel mode, filters and the key-dates tab; live TfL journey times in session details when the mode is public transport (falls back to the heuristic); ⚠ Clash badges on overlapping sessions in the day view; attendance insights (overall % + per-subject counts + CSV export in Settings); PWA shortcuts (Today / Key dates on long-press) and app-icon badging with the unseen-changes count; multi-tab merge (Settings → Timetables → "Merge tab into this timetable", with deduplication); manual System/Light/Dark theme toggle. Remaining from this list: 10–13 (analytics, error monitoring, print stylesheet, accessibility pass).

1. **Key-date reminders (S)** — the deadline data and the notification plumbing both exist; add "notify me 7/3/1 days before each key date" chips next to the session reminders. Highest-value quick win.
2. **Key dates in the calendar export/feed (S)** — include deadlines as all-day events in the .ics download and the worker feed, so submission dates land in Google/Apple Calendar alongside sessions.
3. **Settings backup & richer share link (S/M)** — export/import everything (profiles, filters, notes, attendance) as a JSON file, and extend the share link to carry term start and filter choices. Protects against cleared browser data — currently the only copy of notes/attendance is localStorage.
4. **Real journey times via the TfL API (M)** — TfL's Journey Planner API is free and needs no key for light use; replace the straight-line heuristics with real door-to-door public-transport times and live disruption awareness. Walking/driving could stay heuristic.
5. **Conflict detection (S)** — warn when two visible sessions overlap (e.g. a specialism choice clashing with a group session), as a badge on the day and a note in both details.
6. **Attendance insights (M)** — per-subject attended/total counts and a simple term overview, with CSV export; builds directly on the existing attendance data.
7. **PWA app shortcuts & badging (S)** — manifest shortcuts (jump straight to Today / Key dates from a long-press on the home-screen icon) and the App Badging API to show unseen timetable changes on the icon where supported.
8. **Multi-tab merge (M)** — some cohorts split content across tabs (group tab + all-groups events tab); allow a profile to combine several tabs of the same spreadsheet into one timetable, deduplicating identical rows.
9. **Theme toggle (S)** — manual light/dark override in Settings (currently follows the system).
10. **Privacy-friendly usage analytics (S)** — a free GoatCounter/Plausible-style counter (no cookies, no personal data) to learn whether other cohorts adopt the app; directly informs the template-sales decision in the monetisation notes.
11. **Error monitoring (S)** — Sentry's free tier wired into the app so parsing failures against unfamiliar sheets are visible without user reports.
12. **Print/PDF week view (S)** — a print stylesheet so the week grid prints cleanly for a pinboard.
13. **Accessibility pass (M)** — keyboard navigation through cards and sheets, focus trapping in modals, reduced-motion support, and a screen-reader audit; worth doing before promoting the app beyond the cohort.

**Suggested order:** 1 and 2 first (small and high-value, both about deadlines users already asked for), then 3 (data safety), 5 and 7 as polish, 4 when journey accuracy starts to matter, 10/11 before any push to other cohorts.

## Future enhancements — round 3 (logged 2 Sep 2026)

Ideas building on the app after rounds 1–2 and the TfL work (which now includes a visual per-leg route timeline with line colours and timings). **Built (2 Sep 2026, eighth pass): items 1–5, 7, 8, 9 and 12.** Background push is fully coded (workers/push: VAPID + RFC 8291 web-push crypto, KV subscriptions, 10-min cron computing session/key-date reminders server-side; Settings → Background push; SW push handlers) and awaits `wrangler login` + deploy; live departures under the route timeline; Open-Meteo weather on the Now/Next card and rain notes in leave alerts; deadline workload line + notes surfaced in the key-dates list; ☕ free-gap rows in the day view; 📸 share-week-as-image from the week view; day-view windowing with show-earlier/show-later; offline campus map (OSM tiles through the service worker replace the iframe embed); plus item 7 — a Term stats sheet (sessions, taught hours, attendance %, busiest week, most-visited building) with a shareable image. Still open from earlier rounds: original 9 (private-sheet OAuth) and 11 (feed hardening, after the worker deploy); round-2 10–13 (analytics, error monitoring, print stylesheet, accessibility pass). Effort: S (hours), M (a day or two), L (multi-day / new infrastructure).

1. **True background push (L)** — the one structural gap: strike alerts, leave alerts and key-date reminders currently need the app open. A small Cloudflare Worker with cron triggers + Web Push subscriptions would deliver them with the app closed. The most-requested-in-spirit item; also unlocks "timetable changed overnight" pushes.
2. **Live departures (M)** — TfL's Arrivals API: once a route is shown, add "next 205 from King's Cross: 2, 9, 14 min" so you know whether to run for it.
3. **Weather-aware mornings (S/M)** — Open-Meteo (free, keyless): rain/temperature chip on the Now/Next card and a "rain at 9:00 — consider leaving earlier" note in leave alerts.
4. **Deadline workload view (M)** — a "next 14 days" pressure summary over key dates (count + list), plus per-key-date notes/checklists ("draft done, references left") reusing the session-notes storage.
5. **Free-slot finder (M)** — surface gaps between sessions ("2h free after English 1, next session 14:30") in the day view — the natural place to plan library time; pairs with the deadline workload view.
6. **Personal-calendar clash check (M/L)** — subscribe to the user's own Google Calendar (public ICS address pasted into Settings) and badge timetable sessions that clash with personal events (work shifts, appointments).
7. **Term stats / "wrapped" (S)** — end-of-term shareable summary from existing data: attendance %, busiest week, most-visited building, total taught hours.
8. **Share week as image (S)** — render the week grid to a PNG (canvas) for sharing in group chats, where links get ignored but screenshots get read.
9. **List virtualisation (S/M)** — the day view renders all 443 sessions; virtualise (or window by month) to keep low-end phones smooth as the sheet grows.
10. **Playwright E2E tests in CI (M)** — the app now has enough surface (parsing, filters, profiles, share links) that regressions are plausible; a smoke suite against the demo data on every push would catch them before deploy.
11. **Android TWA / Play Store wrapper (L)** — package the PWA as a Trusted Web Activity for the Play Store; gives real install distribution to other cohorts and enables richer notifications on Android. Only worth it alongside the template-sales route.
12. **Offline map tiles (S)** — cache the handful of OSM tiles around campus in the service worker so the detail-sheet map renders offline too.

**Suggested order:** 3 and 8 as quick delights, 5 + 4 as the next real feature pair, 10 before the codebase grows further, 1 when reminders prove popular, 11 only with multi-cohort traction.

## Future enhancements — round 4 (logged 2 Sep 2026)

**Built (3 Sep 2026, thirteenth pass): items 7, 9 and 10 — plus background leave alerts.** Background leave alerts (opt-in) cache the last app-open location to the push worker (throttled reports; live TfL journey in transit mode; staleness noted, >18h skipped). Feed hardening: 5-min edge cache, 60/hr per-IP rate limit (KV), and a `name=` parameter for custom calendar names (feed URLs now carry the profile name). Photo notes: attach downscaled photos to any session (camera/file picker), thumbnails + delete in the detail sheet, 📷 badge on cards, stored in IndexedDB and included in the v2 JSON backup. Backup nudges: a dismissible monthly banner ("your notes live only on this device") with one-tap export; exports record lastBackupAt, "Later" snoozes a week. Also fixed this session: Apple push delivery (VAPID sub claim used the reserved .invalid TLD → 403 BadJwtToken on every send; confirmed by A/B probe and repaired). Round 4 is now complete except item 6 (departures auto-refresh) and item 8 (notices tab).

**Built (2 Sep 2026, twelfth pass): item 1 — round 4's worker trio complete.** The cron keeps a per-sheet snapshot in KV and diffs every 10-minute run: room/time/tutor changes and added/cancelled future sessions push as "📋 Timetable change — Maths 2 (17/09): room 728 → 731", filtered to each subscriber's specialisms/groups, batched (3 lines + "+N more"), with a first-run seed, a >50-removal glitch guard, and a Settings opt-out. Items 1–5 of round 4 are now all built; remaining from this round: 6–10.

**Built (2 Sep 2026, eleventh pass): item 2.** The 07:00 cron run now also composes a morning briefing per subscriber on days with sessions — "Good morning — 3 sessions today · First: 09:00 PS1 · Bedford Way A5.03 · ☁️ 18° · 📌 Maths PLT 1 in 3d" — from their filtered sheet, campus Open-Meteo weather at the first session's hour, and the nearest key date within 7 days; opt-out toggle under Settings → Background push (re-syncs the stored config live). Item 1 (background change-detection push) is the last of round 4's worker trio.

**Built (2 Sep 2026, tenth pass): item 3.** The push worker's 07:00 London cron run checks TfL line status; severe disruption (severity ≤ 6: Severe Delays, closures, suspensions) or strike wording in the reason pushes "🚨 Strike action on TfL today" / "⚠ TfL disruption this morning" with the affected lines — only to transit-mode subscribers, only on days they have sessions, once per day. Verified against live data (would currently flag Jubilee Suspended, Weaver Part Closure, Windrush Part Suspended and ignore minor delays).

**Built (2 Sep 2026, ninth pass): items 4 and 5.** Reminder notifications (in-app and push) now carry "✓ Attended" and "⏰ Snooze 10m" action buttons — attended taps reach an open window via postMessage or queue in IndexedDB for the next launch; snooze re-delivers through the push worker's cron (new /snooze endpoint) with the app closed, or an open window's timer otherwise. Leave alerts in transit mode now use the cached live TfL journey time (labelled "live TfL") instead of the heuristic, so disruptions make alerts fire earlier automatically.

The app now has live worker infrastructure (background push with a 10-min cron + KV, the ICS feed, both deployed), full TfL integration (live routing, per-leg departures, disruptions), weather everywhere, key dates, stats, profiles, share links and backups. Round 4 is mostly about **exploiting the push worker** — it already fetches sheets on a schedule, so several high-value features are now small additions to it. Effort: S (hours), M (a day or two), L (multi-day).

1. **Background timetable-change push (M)** — the cron already fetches each subscriber's sheet; keep a per-subscriber snapshot in KV, diff it, and push "Room changed for Maths 2: 728 → 731" with the app closed. The in-app diff engine already defines the semantics; this moves it server-side. The single most valuable unlock of the deployed worker.
2. **Morning briefing push (M)** — one 07:00 push composing what the worker already knows: "First session 09:00 English 1 (Bedford Way) · leave ~08:40 · 🌧 68% rain · 📌 Maths Audit closes in 3 days". Open-Meteo works server-side too.
3. **Strike-day alerts (S)** — the 07:00 cron run checks TfL line status; severe disruption or strike wording → push before people set off. Complements the in-app banner, which only helps once the app is open.
4. **Notification action buttons (S/M)** — reminder notifications gain "✓ Mark attended" and "Snooze 10 min" actions (service-worker `notificationclick` with actions); attendance data then builds itself without opening the app.
5. **Live-TfL leave alerts (M)** — leave alerts currently use the heuristic travel estimate; when the mode is public transport, use the cached live TfL journey time instead, so a disrupted Victoria line automatically makes the alert fire earlier.
6. **Departures auto-refresh (S)** — the per-leg departure boards refresh every 30s and count down while the detail sheet is open.
7. **Feed hardening (S)** — now the ICS worker is live: an unguessable token segment in feed URLs, per-IP rate limiting, and a custom calendar name parameter. (Carried from round 1 item 11 — now actionable.)
8. **Notices tab / cohort broadcasts (S/M)** — a "Notices" tab in the same spreadsheet (date, message, link) rendered as dismissible banners in the app: cohort reps get a broadcast channel with no backend at all, in the same sheet they already maintain.
9. **Photo notes (M)** — attach a photo to a session note (whiteboard snap, handout) via the camera/file picker, stored locally (IndexedDB) and included in the backup export.
10. **Auto-backup nudges / Drive export (S/M)** — a monthly "your notes live only on this device — export a backup" reminder, and a one-tap save to the user's own Google Drive via the file picker. Cheap insurance for the attendance/notes data.

**Carried over, still open:** private-sheet OAuth (r1-9); analytics, Sentry, print stylesheet, accessibility pass (r2-10..13); personal-calendar clash check, Playwright E2E in CI, Play Store TWA (r3-6/10/11).

**Suggested order:** 1 → 3 → 2 (each a small addition to the deployed worker, together turning it into a genuine assistant), then 4 and 6 as quick wins, 8 when another cohort member wants to broadcast, 7 before sharing feed URLs widely.

## Future enhancements — round 5 (logged 3 Sep 2026)

**Built (3 Sep 2026, sixteenth pass): placement travel targeting.** Entering a placement address geocodes it automatically (postcodes.io for UK postcodes, Nominatim fallback, with 📍 status hints) and saves coordinates on the placement; the session detail's map, live TfL route timeline, departures, disruption warnings, leave-time weather and Directions link all then target the school instead of campus, and in-app leave alerts fall back to the school coords on roomless placement days. Known gap: the push worker holds no placement data, so *background* leave alerts and session reminders still don't cover synthesized placement days (→ round 6 item 1).

**Built (3 Sep 2026, fifteenth pass): placement & UX polish from user feedback.** Placements highlighted green with a 🏫 placements-only quick toggle beside Day/Week/Month; per-placement details (school, address, mentor, notes) editable from any placement session; marker rows like "SE1a begins (28th Sept – 2nd Oct 2026)" now expand into synthesized weekday placement entries (excluded from change diffs); personal deadlines moved to a floating "+" button above Today; Study Groups sheet reordered join-first; card travel times now read the same cached live-TfL minutes as the detail sheet (App warms routes for upcoming sessions every 5 min).

**Built (3 Sep 2026, fourteenth pass): items 1–7, 9 and 10 (all of round 5 except the email fallback).** Setup checklist card (X/4 done, per-item shortcuts, dismissible); "What's new" toast shown once per release; personal deadlines (👤) added in the key-dates sheet, merged into strip/reminders/export/backups; assignment status per deadline (○/◐ in progress/✓ submitted — done items grey out and leave the workload count and strip); digest batching (multiple due pushes arrive as one "N timetable updates"); placement mode (all-SE days collapse to a calm 🏫 block); sheet-health push after ~1h of failed fetches; TfL responses cached NetworkFirst for offline last-known journey info; and study groups — share a 6-char code, members publish only their free slots to the push worker's KV, the app shows when everyone's free (verified end-to-end: create/join/intersect/leave, groups auto-delete when empty, capped at 12 members, 21-day expiry).

Rounds 1–4 are essentially built: the app is now a full assistant (live TfL routing/departures/disruptions, weather, key dates, background push with briefings/strike/change/leave alerts, photos, backups, stats, profiles, share links, two workers deployed). Round 5 shifts from adding capabilities to **discoverability, personal workflow, and resilience** — the things that decide whether other cohorts adopt it. Effort: S (hours), M (a day or two), L (multi-day).

1. **First-run tour & feature discovery (S/M)** — the app has ~30 features and a new user sees a URL box. A three-step guided setup (specialism → key dates → enable push) plus a "set-up checklist" card in Settings showing what's not yet switched on. The highest-leverage item for adoption beyond your cohort.
2. **"What's new" toast (S)** — a one-time note after each update listing what changed (static changelog shipped with the build). At this pace of change, features are landing that users never discover.
3. **Personal deadlines (S/M)** — user-added key dates ("dissertation draft to supervisor") stored locally, merged into the key-dates list, countdown strip, reminders and ICS export alongside the sheet's official ones.
4. **Assignment status on key dates (M)** — a status per deadline (not started / in progress / submitted) on top of the existing notes; submitted ones grey out everywhere, and the workload line counts only what's still live. Turns the key-dates list into a real assignment tracker.
5. **Notification digest batching (S)** — when one cron run owes several pushes (briefing + strike + change alert on a bad morning), send one combined notification instead of three. Small worker change, big politeness win.
6. **Placement mode (M)** — school-experience blocks (SE rows) dominate whole weeks; detect them and give placement days a calmer rendering (banner + key dates + placement span) instead of empty-looking agenda days. Allow user to populate what school they are attending for that specific placement, and ensure maps and directions cater for this.
7. **Sheet health alerts (S/M)** — if the worker's fetches for a sheet fail for several consecutive runs (permissions changed, tab deleted), push "your timetable source is broken" to its subscribers instead of failing silently forever.
8. **Email fallback for the briefing (M)** — browsers without web push (older iOS, some desktop setups) could get the 07:00 briefing by email via MailChannels (free from Cloudflare Workers); an email field in Settings as an alternative to Enable.
9. **Offline journey resilience (S)** — cache the last TfL journey/disruption and weather responses in the service worker so the detail sheet shows last-known values (age-labelled) with no signal, e.g. on the tube.
10. **Study-group availability codes (L)** — share a short code with coursemates; the push worker's KV intersects free slots across the group ("all four of you are free Thu 13:30–14:30"). The only round-5 item needing new shared state; only worth it with real multi-user demand.

**Carried over, still open:** round-4 6 (departures auto-refresh) and 8 (notices tab); private-sheet OAuth (r1-9); analytics, Sentry, print stylesheet, accessibility pass (r2-10..13); personal-calendar clash check, Playwright E2E in CI, Play Store TWA (r3-6/10/11).

**Suggested order:** 2 and 5 as immediate small wins, 1 before promoting to other cohorts, 3 + 4 as the next feature pair (they complete the deadline story), 7 for operational peace of mind.

## Future enhancements — round 6 (logged 3 Sep 2026)

**Built (3 Sep 2026, seventeenth pass): all ten items.** (1) Worker parity: the push worker now expands placement spans exactly like the app (raw sheet still feeds the change diff, so no bell flood) and the subscription config carries placement school names + geocoded coords — background reminders/briefings name the school and background leave alerts route to it. (2) Placement day counter: unique school days per block, logged via the Attended tick; progress line on the placement block ("1/120 school days logged"), per-block breakdown + required-days setting + CSV day log in Settings → Placements, and a Stats tile. (3) Evidence journal: TS1–TS8 chips on every session's notes, a journal sheet (filter by standard, tap-through to the session, counts per standard) and a markdown export grouped by standard. (4) Cross-device sync: opt-in shared code; the whole store + notes/attendance is AES-GCM-encrypted client-side (key derived from the code, server sees only ciphertext keyed by a hash), parked in the worker's KV (90-day TTL), pulled on app start (newest wins) and re-parked ~8s after edits; verified end-to-end including decrypting the parked blob with only the code. (5) Push self-check: six-row diagnostic in Settings (support, permission, SW, subscription, worker reachability + latency, last-push-received recorded by the SW into IndexedDB) plus a Send-test-push button. (6) Recap: stats/share-image gain school-days and deadlines-submitted tiles; taught-time now excludes placement days. (7) Break awareness: ≥7-day session gaps render as a "🏖 19-day break — no sessions until Monday 4 Jan" band (verified against the real winter break), and the 07:00 briefing sends one "enjoy the break" note on the first morning instead of going silent. (8) Departure boards re-fetch every 30s while the detail sheet is open, marked "live". (9) Accessibility: all sheets are real dialogs (aria-modal, Escape closes — covered by the smoke test — Tab trapped, focus returned to the opener), :focus-visible outlines, prefers-reduced-motion honoured, dark-mode contrast bumped for placement tint and status colours. (10) Engineering health: App.tsx split into useTimetableData/useTravel/useNotifications hooks (~1,100 → ~700 lines, behaviour verbatim), plus a Playwright smoke test (build boots → demo renders → detail opens → Escape closes) that now gates the GitHub Pages deploy in CI.

Rounds 1–5 built the whole assistant. Round 6 is shaped by what the last passes exposed: the push worker lags the app on placements, everything the user records lives on one device, and the app now serves a PGCE course specifically — whose real admin burden (placement evidence, standards tracking) the app is one step away from carrying. Plus the engineering debt a 30-feature app accrues. Effort: S (hours), M (a day or two), L (multi-day).

1. **Worker parity for placements (M)** — send placement spans and geocoded school coordinates with the push subscription config so the worker synthesizes the same placement days the app does. Unlocks the three background features that currently skip placements: morning briefings naming the school, session reminders on placement days, and background leave alerts that route to the school instead of campus. Direct fix for the gap left by the sixteenth pass — highest-value item this round.
2. **Placement day counter & attendance log (M)** — PGCE courses require a minimum number of assessed school days (typically 120). Reuse the existing attended tick on synthesized placement days to drive a "34 / 120 days" progress line on the placement block and in Stats, with per-block subtotals (SE1a: 5/5) and an exportable day log for mentor/tutor sign-off. Turns a compliance chore into a glance.
3. **Evidence journal against Teachers' Standards (L)** — notes + photos already attach to sessions; add optional TS1–TS8 tags and a journal view filterable by standard, exportable as a dated PDF/markdown bundle. This is the PGCE's biggest ongoing admin task, and the capture mechanics (notes, photos, IndexedDB, backup) are all built — only the tagging, browsing and export layers are new.
4. **Cross-device sync via sync code (L)** — the profile store is device-local; a phone-and-laptop user maintains two copies. Reuse the study-group pattern: an opt-in 6-char sync code stores an encrypted settings+meta blob in the push worker's KV, last-write-wins with a merge prompt on conflict. Photos stay local (size); everything else roams. The single biggest quality-of-life gap for daily users.
5. **Push self-check panel (S)** — push has many silent failure modes (permission revoked, subscription expired, worker unreachable). A Settings diagnostic row: permission state, subscription liveness, worker /vapid reachability, last briefing received, and the existing throttled /test — one screen that answers "why didn't I get a notification?" without a debugging session.
6. **End-of-term recap (S/M)** — a shareable "your term in numbers" card (canvas, like the existing week image): contact hours by subject, attendance %, busiest week, days at placement, deadlines hit. Fun, zero-infrastructure, and the most likely thing to get screenshotted into the cohort group chat — i.e. free distribution.
7. **Half-term & holiday awareness (S)** — session-free weekday stretches ≥ a week render as a labelled "🏖 Half term / holiday" band instead of empty agenda days, and the briefing skips those mornings entirely (currently it just doesn't fire, indistinguishable from breakage).
8. **Departures auto-refresh countdown (S)** — carried from round 4 item 6: the per-leg departure boards refresh every 30 s and count down live while the detail sheet is open. Last small piece of TfL polish.
9. **Accessibility & lighthouse pass (M)** — carried from round 2 and now overdue with ~18 components: focus traps and Escape on all bottom sheets, aria-labels on icon buttons, contrast check on the 8 subject colours and the placement green in dark mode, reduced-motion support, keyboard navigation through the agenda. Required before promoting beyond the cohort.
10. **Engineering health: App.tsx split + CI smoke test (M)** — App.tsx is ~1,100 lines of interleaved effects; extract custom hooks (useReminders, useTravel, usePush, useSheetData) and add the carried Playwright smoke run (load demo sheet → agenda renders → detail opens) to the deploy workflow so a bad parse can't ship silently to both hosts.

**Carried over, still open:** round-4 8 (notices tab); round-5 8 (email briefing fallback — user-deferred); private-sheet OAuth (r1-9); analytics, Sentry, print stylesheet (r2-10..12); personal-calendar clash check, Play Store TWA (r3-6/11).

**Suggested order:** 1 first (it completes work just shipped), 5 + 7 as small trust-builders, 2 when placements start (late Sept 2026 — before SE1a on the 28th would be ideal), 10 before the next feature round, 6 at the end of term, 3 and 4 only with sustained daily use to justify the size.

## Future enhancements — round 7 (logged 3 Sep 2026)

**Built (3 Sep 2026, eighteenth pass): items 1–8.** (1) Calendar-feed placement parity: the ics-feed worker expands placement spans (verified live: 62 school-day events against the real sheet) and a `plc=` param — added automatically to the feed URL when placement details exist — puts "Winton Primary School, N1 9AG" as each event's LOCATION. (2) End-of-session attendance prompts (opt-in under Session reminders): "Did you attend X?" fires at each session's end from both the app (skips already-ticked sessions) and the push worker; a shared notification tag stops the two copies doubling up, and the ✓ action logs attendance through the existing machinery. (3) Evidence bundle: the journal's Print/PDF button builds a hidden print document — entries grouped by standard, notes and photos embedded, print-only CSS — verified: window.print fires with the bundle grouped under "TS2 — Promote good progress". (4) Sunday 18:00 week-ahead push: session/deadline counts plus "🏫 SE1B starts Mon — Winton Primary" when a placement block begins that week (uses the briefing toggle). (5) Sync hardening: blobs gzip before encryption (verified externally: gzip magic inside the ciphertext, 368 chars), notes/attendance merge per session with newest-edit-wins (verified E2E with a crafted second-device blob: local-only kept, remote-only gained, conflict resolved correctly, merged result re-parked), pulls re-run when the tab becomes visible, and Rotate code parks under a fresh code and deletes the old blob (verified: old id 404, new id 200) via a new /sync/delete endpoint. (6) iOS install guide: on iPhone/iPad Safari outside the Home Screen, the Enable button is replaced by a three-step add-to-Home-Screen guide (verified with a spoofed iOS user agent). (7) Notices: Settings takes a Date/Message/Link tab URL; rows render as dismissible 📣 banners, newest first, links included, dismissals persistent (verified end-to-end against a stubbed sheet). (8) Landing page: the first-run screen gains a tagline, six-feature grid and share-link/Ko-fi footer, plus a meta description — the compact form stays for "add another timetable". Not directly triggerable in test: the two new cron windows (Sunday briefing, worker-side attendance prompt) are logic-reviewed and share all their delivery machinery with paths already verified live.

Round 6 finished the placement story in the app and push worker — and exposed the last parity hole: the **calendar feed** knows nothing about placements (verified: the ics-feed worker has no span expansion, so subscribed calendars miss every school day). Round 7 closes the remaining loops (attendance capture, evidence output, sync robustness) and turns toward adoption beyond this cohort. Effort: S (hours), M (a day or two), L (multi-day).

1. **Calendar-feed placement parity (S/M)** — port the placement-span expansion into the ics-feed worker so subscribed calendars show school days too, with the school name/address as the event LOCATION (passed as a query param the app adds to the feed URL when placement details exist). The one place placement days still don't appear.
2. **End-of-session attendance prompt (S/M)** — at a session's end time, a "Did you attend?" push with the existing ✓ action (worker cron + in-app timer). Attendance and the placement day counter then build themselves without opening the app — today the tick still depends on remembering to.
3. **Evidence bundle with photos (M)** — the journal's markdown export references photos but can't include them; add a print view of the journal that embeds the photos (browser print → PDF), grouped by standard with dates and notes. Makes the bundle actually submittable, and absorbs the long-carried print stylesheet (r2-12).
4. **Sunday week-ahead briefing (S)** — one push on Sunday evening: session count, deadlines due, and "SE1B starts Monday · Winton Primary — check the entry notes". The worker already knows all of it; placement first-mornings are exactly when people want a day's warning.
5. **Sync hardening (M)** — sync is whole-blob last-write-wins and pulls only on app start: merge notes/attendance per key (newest field wins) instead of dropping one device's edits, pull again on tab-visibility, gzip the blob (CompressionStream) against the 400KB cap, and a "rotate code" button.
6. **iOS install guide (S)** — on iOS Safari outside standalone mode, enabling push can't work until the app is on the Home Screen; detect it and show a guided add-to-Home-Screen sheet instead of a failing Enable button. Removes the likeliest new-user dead end.
7. **Notices tab / cohort broadcasts (S/M)** — carried from round 4 item 8: a "Notices" tab in the same spreadsheet (date, message, link) rendered as dismissible banners — a broadcast channel for cohort reps with no backend at all.
8. **Landing page for new cohorts (S/M)** — the app's root currently assumes you were sent a share link; a short public "what is this / works with any Google Sheet timetable" page with screenshots and a paste-your-sheet start. The distribution half of the monetisation plan's template-sales route.
9. **Sentry + privacy-friendly analytics (S/M)** — promote the long-carried operational pair before promoting the app: error reporting (free tier) and a counter-only analytics ping (no cookies), so breakage in other cohorts' sheets is visible without them reporting it.
10. **Encrypted photo sync via R2 (L)** — the one thing sync can't carry; Cloudflare R2's free tier (10GB) could hold client-side-encrypted photo blobs keyed like the sync store. New (still-£0) resource — only with real two-device demand.

**Carried over, still open:** round-5 8 (email briefing fallback — user-deferred); private-sheet OAuth (r1-9); personal-calendar clash check, Play Store TWA (r3-6/11). (r2-12 print stylesheet folds into item 3; r4-8 notices is item 7; analytics/Sentry are item 9.)

**Suggested order:** 1 and 4 first (small worker wins that finish parity), 2 next (completes the attendance loop the day counter depends on), 6 before telling anyone with an iPhone about the app, 3 before the first evidence submission deadline, 5 once sync sees real two-device use, then 7–9 as the adoption push, 10 only on demand.

## Future enhancements — round 8 (logged 3 Sep 2026)

**Built (3 Sep 2026, nineteenth pass): items 1–8.** (1) Absence tracking: ✓ Attended / ✗ Absent chips with a reason (Sick/Travel/Personal/Other) in the detail sheet — verified saving {absent, reason} — the end-of-session prompt's actions become ✓/✗ (tag-detected in the SW), absences flow into the Settings summary, an absences Stats tile, and Absent/Reason columns in both CSVs. (2) Notices push: the worker fetches each notices tab once per cron, keeps a KV seen-set (first sight seeds silently), and pushes "📣 Cohort notice" for new rows, gated with change alerts. (3) View parity: week events go green with 🏫 + the school name (verified: 5 placement events in the SE1A week), month cells tint green on all-placement days and mark the first day of each ≥7-day gap 🏖 (verified: "19-day break starts" on 16 Dec). (4) Share-target: the manifest declares a POST share target, sw-push.js parks shared photos in IndexedDB and bounces to ?share=photo, and the app attaches them to today's latest started session and opens it (verified end-to-end from the IDB hand-off: photo landed on English 1 with a thumbnail; the SW POST leg itself can't run in the test pane, which blocks SW registration — production SWs demonstrably work). (5) Feed staleness: copying the feed URL records it; when filters/placements change the URL a ⚠ re-copy warning shows (verified by changing a placement address). (6) Android/desktop install: beforeinstallprompt is captured for an Install button in Settings and a checklist entry (standard pattern; the pane never fires the event, so logic-reviewed). (7) Notifications at a glance: one Settings section summarising every push type's state, plus quiet hours (Off/21–7/22–7/23–8) enforced in the in-app check loop and per-subscriber in the worker — skipped sends aren't marked, so still-relevant alerts fire when the window ends. (8) Code splitting: the eight bottom sheets are React.lazy chunks — main bundle 299KB → 245KB (SettingsSheet alone was 34KB) — and the smoke test still passes.

Round 7 closed the parity and adoption gaps it aimed at; this round comes from what's now visibly uneven. Verified before logging: the week and month views have **zero** placement/break awareness (all of rounds 6–7's placement work lives in the day view), the app ships as a single 299KB chunk, and there's no share-target or install-prompt handling. Themes: finish attendance honestly (absence is data too), push the notices channel, bring the other views up to par, and the capture/adoption polish. Effort: S (hours), M (a day or two), L (multi-day).

1. **Absence tracking (M)** — attendance is currently binary (ticked or silent). Add "✗ Absent" alongside ✓ on the end-of-session prompt and in the detail sheet, with an optional reason (sick / travel / other); Stats and the attendance CSV show absences, and the placement day log gains an Absent column — PGCE attendance requirements care about recorded absence, not just presence.
2. **Notices push (S)** — notices only appear when the app opens; the push worker already diffs sheets on a cron, so diff the notices tab too and push new rows ("📣 Room change: PS1 moves to 731") to subscribers. Makes the broadcast channel actually reach people.
3. **Week & month view parity (S/M)** — placement days green and break stretches labelled in the month grid; the week view gets the placement tint and school name. The day view learned all this in rounds 6–7; the other two views still pretend placements don't exist.
4. **Share-target photo capture (M)** — register the PWA as a share target so a whiteboard photo can be shared from the camera roll straight into today's session as evidence (picks the current/most recent session, tags it for the journal). Removes the clunkiest step in evidence capture, and works offline in schools with bad signal.
5. **Feed-URL staleness nudge (S)** — placement details and filters are baked into the copied feed URL; when they change after the URL was last copied, show "your calendar feed URL is out of date — re-copy it" in Settings. Fixes the silent drift noted when round 7 shipped.
6. **Android install button (S)** — capture `beforeinstallprompt` and offer an "Install app" button in Settings and the setup checklist. Pairs with round 7's iOS guide so both platforms get walked to the Home Screen.
7. **Notification preferences panel (S/M)** — push types are now scattered across four Settings sections (reminders, leave alerts, briefing/changes/leave toggles, attendance prompts, key dates). One "Notifications" section listing every type with its toggle, plus optional quiet hours. Consolidation, not new capability.
8. **Code splitting & bundle audit (S/M)** — 299KB in one chunk; React.lazy the sheets (Settings, Journal, Stats, Study group are all rarely-first-paint) and audit for dead weight, targeting a materially smaller initial load on school Wi-Fi.
9. **Sentry + privacy-friendly analytics (S/M)** — carried from round 7 item 9, unchanged: operational visibility before wider adoption.
10. **Encrypted photo sync via R2 (L)** — carried from round 7 item 10, unchanged: only with real two-device demand.

**Carried over, still open:** round-5 8 (email briefing fallback — user-deferred); private-sheet OAuth (r1-9); personal-calendar clash check, Play Store TWA (r3-6/11).

**Suggested order:** 2 and 5 as quick finishers of round-7 features, 1 + 3 as the substance (they complete attendance and view parity), 4 when placements begin (late Sept), 6–8 as the polish/adoption batch, 9–10 unchanged from last round.

## Future enhancements — round 9: the PGCE admin file (logged 3 Sep 2026)

**Built (3 Sep 2026, twentieth pass): all ten items — "My PGCE file" (🎓 in the top bar).** One lazy-loaded sheet with eight tabs. Verified live end-to-end: logged a reflection (TS7), a manual target (TS6), a mentor meeting whose two typed actions became tickable to-dos, an observation whose development points auto-became a target, a lesson evaluation (TS4), and a Maths baseline audit; ticked an action and cycled a target to met; added a PDF to the wallet. The Overview dashboard tracked it all exactly (2/8 standards with a gap warning naming the missing six, 1 open target, 1 open action, 1-week reflection streak, 0/1 audits secure, days 0/120), the placement-day block shows open targets, Stats gains lessons-taught/observations tiles, and the evidence journal picked up the 📔 reflection and 🍎 lesson entries. The full binder export printed with all seven sections (attendance/days, evidence per standard, targets, meetings, observations, lessons, audits). Data model: one admin file per profile (items carry `at` timestamps), merged per-item in cross-device sync — verified by decrypting the parked blob: all six collections present — included in backups (now v3, with the wallet as base64), and cleared with the profile. The worker gained a Friday 16:00 digest window fed by summary counts the app re-syncs after every admin change (logic-reviewed; cron windows can't be fired on demand); the reflection nag only fires in weeks that actually had sessions. Wallet files cap at 10MB each and live in their own IndexedDB store.

Theme requested by the owner: **make every piece of PGCE admin loggable in the app, so the student's "file" builds itself**. The course's real paperwork burden is: weekly mentor meetings with targets, formal observation records, weekly reflections, lesson plans + post-lesson evaluations, subject-knowledge audits, evidence against the Teachers' Standards, attendance/absence/day counts, and a binder to hand in at each placement's end. The app already holds the last three (evidence journal, absences, day counter) — this round adds the rest and ties it together. Effort: S (hours), M (a day or two), L (multi-day).

1. **Weekly reflection log (M)** — one guided entry per week (went well / challenges / next week's focus), taggable TS1–TS8, attached to the week (not a session). A Friday-afternoon push during placement weeks nudges it while the week is fresh. Reflections join the evidence journal and bundle. The habit every mentor asks for and every student backfills the night before a review.
2. **Targets tracker (M)** — mentor-set targets: text, date set, linked standards, status (set → in progress → met) and week met. Open targets surface on the placement block and in the weekly nudge; met targets are evidence. This is the spine of mentor meetings and reviews — currently it lives in a paper diary.
3. **Mentor meeting log (S/M)** — a quick per-meeting record: date, discussed, actions agreed. Actions become tickable to-dos that carry forward until done; meetings + actions export alongside the placement day log. Cheap to build (it's a structured note), high trust value at reviews.
4. **Observation records (M)** — formal observations received: observer, lesson/subject, focus, strengths, development points — with development points optionally becoming targets (item 2). A count of observations per block joins Stats and the placement log (courses mandate a minimum per placement).
5. **Lesson log with evaluations (M/L)** — lessons the student taught: class/subject/date, plan attached (photo or file), and a post-lesson evaluation note. "Lessons taught" per block joins Stats; evaluations are TS-taggable evidence. The biggest single chunk of placement admin.
6. **Subject-knowledge audit tracker (S/M)** — the sheet already carries audit deadlines as key dates; add per-subject audit status (baseline → revisited → secure) with a note per pass, so the start/mid/end audit story is in one place instead of three spreadsheets.
7. **Document wallet (M)** — the photo store grows up: attach PDFs and files (safeguarding certificate, DBS, lesson-plan template, school policies) to sessions or a general wallet, stored in IndexedDB, size-capped, included in backups. Placement schools ask for the same three documents every time; they should live in the app that's already open.
8. **"My PGCE file" dashboard (M)** — one screen answering "am I on track?": days 34/120 with absences, evidence counts per standard **with gap warnings** ("nothing against TS6 yet"), open targets, reflection streak, observations logged, audits pending, assignments due. Aggregates items 1–6 + existing data; the natural home is a fifth view or the Stats sheet grown up.
9. **Full binder export (M)** — one printable bundle (extends the round-7 print machinery): evidence journal + reflections + targets + meeting log + observations + lesson log + attendance and placement-day logs, grouped and dated — the end-of-placement hand-in, generated in one tap.
10. **Friday admin digest (S)** — a weekly push listing what's outstanding: unmarked attendance days, missing reflection, open targets, unticked mentor actions, audits due. Turns the whole round from "a place to log things" into "a thing that chases you kindly". Small worker addition once items 1–3 exist.

**Carried over, still open:** Sentry + analytics (r7/8-9); encrypted photo sync via R2 (r7/8-10); email briefing fallback (r5-8, user-deferred); private-sheet OAuth (r1-9); personal-calendar clash check, Play Store TWA (r3-6/11).

**Suggested order:** 3 → 2 → 1 first (they feed each other: meetings set targets, reflections review them, and all three are needed weekly from placement day one — before SE1a on 28 Sept 2026 ideally), then 4 + 5 as the observation/lesson pair, 8 once three data sources exist, 6 and 7 as fill-ins, 9 + 10 last since they aggregate everything.

## Bug fix — history retention (4 Sep 2026, twenty-first pass)

**Reported:** nothing before today is visible, and yesterday's attendance marks don't count in stats. **Root cause:** the source sheet is built on a rolling TODAY() filter — its GViz feed literally drops each day's rows overnight (verified: on 4 Sep the feed's earliest date was 4 Sep; on 3 Sep it had been 3 Sep). The app faithfully mirrored the feed, so history vanished daily and marks pointed at sessions that no longer existed. **Fix, in three layers, all keeping one rule — retention only ever applies to whole past days that fell off the sheet; the future always comes fresh, so a cancelled future session can never be resurrected (verified by planting fake cancelled-future rows in the cache: both purged on refresh, past-day row survived):**
1. **App:** `retainHistory` merges cache-held past days missing from each fetch (before placement-span expansion, so past marker rows keep regenerating their spans); key dates get the same treatment. Verified live: agenda shows Wed 2 / Thu 3 Sept before Today, ✓ badges render on yesterday's cards, attendance summary counts them (2 of 9), and history survives repeat refreshes.
2. **One-time recovery:** the push worker's per-sheet snapshot still held everything back to 2 Sep (it only rewrites on changes); a new `GET /history` endpoint exposes it and the app back-fills lost days once per profile — so the days lost before this fix existed came back.
3. **Workers:** the push worker's cron retains snapshot history the same way (snapshots accumulate it through rewrites; keeps placement spans alive after their marker date passes, and background features working against past-dependent state), and the ics-feed worker keeps a KV history per sheet (past-from-history + future-from-fresh-only, written only when new rows appear) so subscribed calendars keep past events from now on. Change detection is untouched — the diff was already future-only, so cancellations still push alerts.

Known residual: days that fell off before ~2 Sep (none existed) and feed-history before 4 Sep are unrecoverable; a placement span cancelled *after* its marker row has become history can't be detected from the sheet (the sheet carries no signal either way).

## Head-home card + self-hosted analytics (4 Sep 2026, twenty-second pass)

**🏠 Head home (owner request):** Settings → Travel times gains a Home field (geocoded like placement addresses; stays on-device and in encrypted sync/backups — never sent to the push worker). From 30 minutes before the day's last session ends until 23:00, the day view shows a "Head home" card with the live journey: minutes (live TfL in transit mode, refreshed every 5 min), arrival ETA, a leg summary and a Directions link. Verified live: "≈ 33m by public transport (live TfL) · arrive ~13:44 · 🚶 → 🚇 Piccadilly → 🚌 153 → 🚶" against a real N7 address.

**📈 Anonymous usage analytics (owner request; resolves the long-carried r7/8-9 analytics half):** self-hosted on the push worker — no third-party vendor, keeping the no-new-vendors rule. Once per day the app POSTs /ping: a random device token (generated locally, tied to nothing), installed-or-browser flag, platform class (ios/android/desktop) and app version. No location, no identity, no timetable data; on by default with an off switch and full disclosure under Settings → Support. Pings live 90 days in KV; `GET /stats?days=N` aggregates on demand: total devices ever, actives last 7 days, and per-day active/installed/platform/version counts — enough for install, use and retention curves at cohort scale. Verified live round-trip (ping → stats) and test entries deleted so counts start from zero. Sentry (the other half of r7/8-9) remains carried.

## Owner analytics PWA + UX follow-ups (4 Sep 2026, twenty-third pass)

**📈 Standalone analytics dashboard (owner-only):** `analytics.html` ships with the app's static deploys but is its own installable PWA (own manifest, name "Timetable Analytics", standalone display, add-to-Home-Screen on iOS/Android). It reads the worker's enriched `GET /stats` — now key-protected: a `statskey` stored in KV (set via wrangler, never in the repo) must match `?key=`, verified 401-without/200-with. The dashboard prompts for the key once and keeps it in that browser only. It shows: active today / 7-day / 30-day actives / devices ever / installed-today / new-in-window tiles, a 31-day stacked DAU chart (new vs returning, hover for detail), a stickiness breakdown (tried-once vs 2–4 days vs 5+ days active), today's platform and app-version split, and a 14-day table. `/stats` additionally computes first-seen-based new-device counts per day. Verified live end-to-end (lock → key → full render); the one stray test-device ping was deleted so real counts start from zero.

**UX fixes from owner feedback:** the day view now opens at today — history sits behind the existing "↑ Show earlier (N more days)" control instead of pushing today down (verified: first header "Friday 4 Sept · Today" with 2 days of recovered history one tap up). The Head-home card lost its end-of-day gate: it now shows **whenever you're away from home** (>400 m, any time — leave early whenever you like) and hides itself once you're home; verified showing mid-day with a live TfL route.

## PWA auto-update (4 Sep 2026, twenty-fourth pass)

Owner reported no devices on the analytics dashboard despite using the app: verified zero pings server-side — the phone was still running a pre-analytics bundle, because `registerType: 'prompt'` holds every new version behind a "Refresh" toast tap that nobody makes. Switched to `registerType: 'autoUpdate'` (the generated SW now skipWaiting/clientsClaims): a device downloads the new SW automatically on launch and runs the new bundle from the next open, no interaction. The toast remains only as a fallback. One-time crossing for devices still on the prompt-mode SW: open the app (tap Refresh if the toast shows), or force-close and reopen twice; from then on updates flow automatically.

## Header pill + history toggle (4 Sep 2026, twenty-fifth pass — owner feedback)

The full-width Head-home card and "Show earlier" button took too much room. Head-home is now a compact **🏠 pill in the top bar** showing the live minutes ("🏠 30m", cached live-TfL when available); tapping it drops down the full journey (route legs, arrival ETA, Directions) and it still only appears when away from home. History moved behind a **🕰 toggle beside Day/Week/Month**: the agenda always opens at today, and the toggle reveals every retained past day above it (today stays pinned via re-anchor on toggle); the big Show-earlier button is gone. Verified live: pill "🏠30m" → dropdown "≈ 30m by public transport (live TfL) · arrive ~14:11 · 🚶 → 🚇 Piccadilly → 🚌 19 → 🚶"; 🕰 on renders the recovered Sept 2–3 above today.

## Ping reliability + topbar overlap (4 Sep 2026, twenty-sixth pass)

Owner's phone was on the latest build yet the dashboard stayed at 0. Root cause: the analytics ping marked the day as "sent" **before** the request, so a single failed attempt (flaky network at launch) silenced it until the next day. Fixed: the day is only marked after a 2xx (failed attempts retry on every open), the throttle key moved to v2 so already-burned days don't carry over, and the ping also fires on visibilitychange — installed PWAs usually **resume** rather than relaunch, so a mount-only ping would miss whole days. Settings → Support now shows on-device status ("ping sent today ✓ / not sent yet") so this is self-diagnosable. Verified the fixed pipeline live end-to-end (app open → ping → dashboard count), then removed the test device from KV. Also fixed: the 🏠 pill overlapped the "updated Xm ago" text — the topbar title now shrinks with ellipsis, actions never wrap, and the updated label hides under 380px (verified no overlap at 375px and 430px).

## Head-home journey parity (4 Sep 2026, twenty-seventh pass — owner feedback)

The head-home dropdown only summarised the journey; sessions got the full treatment. Extracted the session detail's live-journey stack into shared pieces — `useLiveJourney` (route + per-leg departure boards refreshed every 30s + disruptions filtered to the route's lines) and `RouteSteps` (the visual timeline with line badges in official colours, from → to, per-leg minutes, live boards) — refactored SessionDetail onto them (verified unbroken), and gave the 🏠 dropdown the identical end-to-end view: route timeline, live departures per leg (the bus/train frequency ask), disruption warnings, weather for the journey, a map of home and a Directions button, in a scrollable panel. Verified live: "Walk to Russell Square 10m → 🚇 Piccadilly (🕐 due, 1m, 2m · live) → 🚌 91 (🕐 1m, 7m, 18m · Stop A · live) → Walk 2m", ☁️ 21° for the journey, map pinned on home.

## Usage analytics + pill waiting state (4 Sep 2026, twenty-eighth pass)

**Deeper analytics (owner request):** the daily ping now also carries — all still anonymous, counts only, never content — app opens since the last ping (with a 6-bucket time-of-day histogram), whitelisted feature-use counters (session details, week/month views, search, history toggle, head home, each sheet, photo adds, binder/evidence exports), and yes/no setup flags (push, location, home, key dates, sync, placements). The worker validates/clamps everything, and `/stats` aggregates the last 7 days: feature adoption (per-feature % of active devices + use counts), setup adoption across today's devices, opens per active device and the daypart histogram. The dashboard gained three sections: **Feature adoption** (bar list), **Setup adoption** (percentage tiles) and **Engagement** (opens + time-of-day chart). Settings' disclosure text updated to match. Verified end-to-end (interactions → ping → KV record `u:{detail:1,settings:1}` + setup flags → aggregated `/stats` → rendered dashboard), then all test devices purged from KV. Deliberately still never collected: sheet ids, session titles, notes, locations, identities.

**Home pill "disappeared" (owner report):** the pill was verified fine on the current build — the two ways it hides are (a) being within ~400m of home, by design, and (b) location silently dropping (iOS can lose the geolocation watch on resume), which previously looked identical. Now (b) shows a visible "🏠 …" waiting pill whose dropdown explains and suggests re-toggling Travel times, so the states are distinguishable.

## Cloudflare free-tier budget (4 Sep 2026, twenty-ninth pass — owner request)

Audited both workers against the free-tier caps. Requests (100k/day) were never at risk at cohort scale; the tight cap is **KV: 1,000 writes/day account-wide**, and the biggest spender was the ICS feed's per-IP rate counter (one KV write per uncached calendar poll). Changes, cheapest savings first:
1. **ICS feed:** the KV rate counter became an in-memory per-isolate counter (zero KV writes) and the edge cache went 5 → 15 minutes (~3× fewer origin hits; a timetable doesn't need 5-minute calendar freshness).
2. **Client dedupe:** `/subscribe` config re-syncs now hash the config and skip the request entirely when nothing changed (the Enable button forces); sync pushes hash the payload the same way (explicit Sync-now/connect/rotate force), and the auto-push debounce went 8s → 20s.
3. **Worker write-skips:** `/location` skips its KV write when the stored fix is <5 min old and <100 m moved.
4. **Per-user rate caps:** every endpoint now has a per-IP requests-per-minute cap (subscribe/sync/group 10, ping 6, stats 20…) via an in-memory map — an abuse guard that itself costs nothing. Honest caveat: it's per-isolate/per-colo, so bursts spread across isolates can slip through (verified: 8 rapid curls all passed); it bounds sustained single-source abuse, not determined attackers — acceptable since it protects a free-tier budget, not security, and KV-based counters would spend the very writes being protected.
5. **Dashboard:** caches `/stats` (the KV-list-heavy call) for 10 minutes client-side; the refresh link forces.
Post-change worst-case budget at ~30 active users: a few hundred KV writes/day (pings ≤2/device, real config/sync changes only, movement-gated locations, ~1 hist write/sheet) against the 1,000 cap, with cron overhead constant. Also noted: the "3 devices" briefly visible on the dashboard were this session's test devices, purged after verification — real counts start from zero.

## Post-mortem: the vanished device (4 Sep 2026, thirtieth pass)

Owner's device "not showing despite using the app today" — reconstruction shows the pipeline had actually worked: the phone auto-updated, pinged, and was one of the three devices briefly on the dashboard. Of the three ids purged as test data, two were provably this session's test devices; the third (`14471e…`) matched no test run and was almost certainly the owner's phone — **wrongly deleted in the purge**. Its successful ping means the device correctly won't retry until tomorrow, when it reappears by itself. Rules adopted: never write test pings against production analytics again, and never delete a KV record that wasn't explicitly created by the session. Shipped alongside, to end the "which build am I on?" ambiguity behind this whole saga: a version + build-time stamp in Settings → Support (baked in at build via `__BUILD_TIME__`), and service-worker update checks on every app resume plus hourly (installed PWAs resume rather than relaunch, so the browser's own check could lag builds for hours).

## Building/room split + sheet location audit (4 Sep 2026, thirty-first pass — owner request)

Audited every distinct Location string across the full year (live sheet + history snapshot: 41 strings). 31 follow "IOE - Bedford Way (20) - <room>" (some with hall names appended, e.g. "421 - Nunn Hall"); the rest: two Cruciform formats ("Cruciform Building B.3.04"), one Christopher Ingold ("… - XLG2 Auditorium"), one Darwin with the room FIRST ("B40 Darwin LT"), the non-rooms "In School"/"Self Study"/"TBC", and — flagged to the owner — **seven bare 7-digit numbers (7910321 etc.) that are room-booking references leaked into the Room column**, not rooms (affecting History 2, L&T 3/5/6, English 11, the Sustainability day and one Self study row). New `parseLocation` handles every observed format (unit-tested against all 41): cards now show the building short-name plus a distinct "Rm 642" chip (booking refs/TBC show a red "Room TBC" chip), and the detail sheet splits into Building ("IOE · 20 Bedford Way") and Room ("642", "421 · Nunn Hall") rows — booking refs render as "Not in the sheet yet (booking ref …)" so nobody hunts for room 7910321. Verified live across IOE, Cruciform, hall-name and booking-ref cases.

## Readable change notifications (4 Sep 2026, thirty-second pass — owner request)

Change detection now speaks the building/room split everywhere a change is described — the in-app bell/changes sheet (`diffSessions`) and the background change push (worker `diffSheets`, via a compact port of `parseLocation` kept sentence-for-sentence identical, unit-tested for parity): "Room changed: 642 → 731", "Building changed: Bedford Way → Cruciform (Rm B.3.04)", "Building & room changed: …", and the TBC/booking-ref transitions "Room confirmed: Bedford Way Rm 739 (was TBC)" / "Room now TBC (was …)". Tutor and end-time details tidied to match ("Tutor changed: …", "End time changed: …"). Verified end-to-end through the real diff pipeline by doctoring a cache against the live sheet: all three flavours rendered correctly in the bell ("Room changed: 999 → 305 (Clarke Hall)", "Building & room changed: Cruciform Rm B.9.99 → Bedford Way Rm 731", "Room confirmed: Bedford Way Rm 631 (was TBC)"). The worker path shares the exact same wording (cron-delivered; parity unit-verified).

## Sessions-by-subject counts (4 Sep 2026, thirty-third pass — owner request)

Term stats (Settings → Attendance → 📊) gains a **Sessions by subject** list where numbered sessions collapse into one subject — Maths 1…12 are all "Maths" (`baseSubject`: strip the trailing session index; specialism rows group under their specialism name, so a Computing specialist's core + specialism Computing merge; unnumbered "X - Y" titles group under X). Each row shows total sessions, hours, and attended-so-far where marks exist. Grouping audited against every title in the full-year sheet before shipping; sentence-length calendar rows (holidays, deferred-assessment notes) and one-off admin entries collapse into a "+N one-off entries" footer instead of polluting the list. Verified live with a Computing-specialism profile: "Computing 22 sessions · 44h, L&T 16 · 26h, English 11 · 22h, Maths 10 · 20h, PS 10 · 20h · 0/2 attended…", holiday rows excluded.

## Deployment documented (6 Sep 2026, thirty-fourth pass — owner request)

[DEPLOYMENT.md](DEPLOYMENT.md) now captures the whole deployment story in one place: the what-runs-where table (two app hosts, two workers, one shared KV namespace), the automatic paths (push-to-main → Pages with the Playwright gate + Vercel) and their manual equivalents (`gh workflow run`, `npx vercel --prod`, `wrangler deploy` — with the warning that **workers never auto-deploy** and the worker-first ordering rule for cross-cutting changes), first-time setup from clean accounts (KV creation, both wrangler.tomls, statskey, config.ts base URLs, Pages enablement, Vercel linking), the secrets/state inventory (statskey rotation; the never-delete-VAPID warning; every KV key prefix), a post-deploy verification checklist, rollback for app and workers, and the free-tier budget notes. Linked from the README.

## Agent-ready deployment (6 Sep 2026, thirty-fifth pass — owner request)

Deployment is now automatable by any AI agent (ChatGPT/Codex, Claude, CI): **`AGENTS.md`** at the repo root — the file agent tools read automatically — carries the runbook as non-interactive commands with decision rules (workers never deploy on push; worker-first ordering for cross-cutting changes; smoke test gates every deploy) and hard rules (never touch the `vapid` KV key; never delete KV records the task didn't create; test pings only under a reserved `ffffffff…` device id; KV-write frugality). Behind it, **`scripts/deploy.sh`** is a prompt-free driver with targets `preflight | build | workers | app | verify | all`, PASS/FAIL output and meaningful exit codes (2 = missing auth → agents stop and ask a human; auth itself stays a human-once browser OAuth, checked via `wrangler whoami`/`gh auth status`). Exercised live: preflight all-PASS, verify all-PASS (after it caught a real bug in its own first version — the feed check omitted the tab gid and hit a 422), build+smoke green, both workers idempotently redeployed. DEPLOYMENT.md links both.

## Deploying the push-test hardening (6 Sep 2026, thirty-sixth pass)

Deployed the externally-authored hardening commits (device-authenticated `/test-device` replacing the anyone-can-broadcast `/test`, hardened `/subscribe` validation, the shared validate gate) to production, worker-first per the runbook. Verified live: `/test` → 410, `/test-device` rejects invalid credentials, `/vapid` unchanged (all subscriptions intact). Caught and fixed a real release blocker while verifying the frontend: GitHub Pages was serving the new build but **Vercel was fail-closed on a stale deployment** — its new `vercel.json` gate ran Playwright, and Vercel's build image cannot launch Chromium (`libnspr4.so` missing, no root to install it; confirmed in the failed build's logs). Split the gate: Vercel now runs `validate.sh --skip-e2e` (build + unit tests, still fail-closed) while the full browser gate stays on GitHub Actions for the same commit; DEPLOYMENT.md and AGENTS.md corrected to describe the split honestly. After the fix: both hosts serve the new bundle (verified by the `test-device` marker in each host's SettingsSheet chunk) and `deploy.sh verify` is all-PASS.

## Completing the interrupted Phase 2 release (7 Sep 2026, thirty-seventh pass)

The previous agent ran out of tokens mid-release. Audit found the release mostly complete (commits pushed, CI green, push worker live with the Durable-Object-backed `/sync-v2` and `/sync` retired at 426) — but production was showing a wall of "Row N: invalid time or end before start. Row skipped." warnings. Root cause: the new shared parser (`shared/timetable.js`, used by the app AND both workers) rejected `end <= start`, but the sheet legitimately uses **zero-duration rows (end === start) as point-in-time markers** — PLT tasks, audit open/close dates, submission deadlines: 56 real rows were being silently dropped from the app, the push worker's snapshots/reminders and the calendar feed, on top of the scary banner. Fixed: only genuinely inverted times (end < start) are rejected; equality is kept (the 00:00/00:00 special case is subsumed). Added the missing regression unit test (the strict check had shipped untested — exactly how it reached production). Redeployed both workers (worker-first), pushed the fix through the full gate, and verified live: banner gone, marker rows back in the app and the feed.

## Identity-review fixes (7 Sep 2026, thirty-eighth pass)

Owner hit "4 event identities need your review… Showing your last saved timetable" with no way to find the sessions. Three fixes: (1) **the notice is its own tappable banner** — separated from the error state (no more misleading "last saved timetable" suffix), it names the first affected session, opens it on tap, and every affected card carries a 🔗 review badge. (2) **The owner's actual 4 flags were a reconciler deadlock, not real ambiguity**: the sheet repeats two deadline rows verbatim ("L&T Essay and Viva Deferred… finish" ×2 on 22 Jul, "Specialism Deferred Assessments/Resits finish" ×2 on 23 Jul) and the exact-match uniqueness guard refused duplicate claimants — flagging them forever and unresolvably. Key-identical rows now pair with duplicated history in order (a *moved* row can never enter that branch, so nothing can be stolen), and a leftover duplicate is treated as new rather than fuzzy-linked or asked about. (3) **Resolutions now stick**: resolveIdentity prunes the superseded auto-keyed history entry (previously the next refresh saw two records with one legacy key and re-flagged), and resolved entries win exact-match claims. Three regression unit tests added; full gate green; both workers redeployed (they share reconcileEvents) before the app. The banner remains for genuinely ambiguous cases, which now should be rare.

## Monetisation options (explored 2 Sep 2026)

Context: niche audience (one PGCE cohort today — likely low hundreds of users), £0 infrastructure, free-tier hosting. Ordered by fit:

1. **Donations — do this now.** Ko-fi / Buy Me a Coffee / PayPal.me / GitHub Sponsors. No backend, no cost, fits a student audience; add a "Support this app ☕" link in Settings and the README. GitHub Sponsors takes no fees. Expect pocket money, not income.
2. **Template / white-label sales — the realistic revenue path.** The app is sheet-agnostic: any course whose timetable lives in a Google Sheet could use it. Sell configured deployments or a one-time template (Gumroad / Lemon Squeezy, ~£10–30) to other cohorts, course reps or programme admins; or keep it open-source and charge for setup/customisation.
3. **Freemium** — free core, paid conveniences (hosted ICS feed, multiple profiles, priority features) via Stripe Payment Links + a small entitlement check (Cloudflare Workers KV would keep it £0). Only worth building once there's real multi-cohort traction.
4. **Ads — not recommended.** At this scale AdSense would earn pennies per month (display RPMs of ~£1–3 need thousands of daily views), damages the UX of a glance-at-it-daily utility, and drags in GDPR consent banners. Niche networks (Carbon/EthicalAds) require developer-scale traffic.
5. **Hosting constraint (important):** GitHub Pages and Vercel's Hobby plan are non-commercial tiers. A donation link on a personal open-source project is generally tolerated; running ads or selling access is not — that step needs Vercel Pro (~$20/mo) or a move to Cloudflare Pages (free tier permits commercial use).

**Recommendation:** add a donation link now; pursue template sales if other cohorts show interest; revisit freemium at hundreds of weekly users; skip ads.

## Resolved decisions (log)

1. **Sheet access**: the sheet is public — anyone with the link can view it → Option A (direct browser fetch, no backend, no OAuth).
2. **Hosting**: must be completely free (student project, no budget) → GitHub Pages + GitHub Actions on a public repo. No card, no trials, no usage caps that matter at this scale.

### Phase 2 — saved data, event ownership and sync (2026-09-07)

- Added visible persistence/export failures and retries; v4 backups include event/identity snapshots and change history. Validation/preview precedes import. Existing attachment DBs retain their schema and numeric IDs; optional stable UIDs and deterministic legacy identities prevent repeat-import duplication.
- Added a durable recovery journal before restore, profile removal and remote apply. IndexedDB commits are awaited. Failure rolls back metadata and files; startup replays interrupted journals before rendering. Profile deletion removes only its own local owners and retains a sync deletion marker.
- Extracted shared browser/worker date/time parsing, payload/config contracts, identity reconciliation and merge rules. Invalid dates/times produce row warnings without inheriting an invalid date. Added bounded share/backup/config inputs and endpoint-scheme checks.
- Existing event owners/calendar UIDs survive row movement and supported rescheduling. Optional source IDs support arbitrary title/date changes; ambiguous matches have an explicit review control and old unlinked notes remain in the journal. Main and deadline calendar feeds retain identity snapshots. Session photos distinguish files present here from remote metadata counts.
- Sync now merges profiles/metadata/PGCE collections, retains deletion markers, excludes device settings and includes identity history. All meaningful PGCE saves schedule sync. Remote changes apply without reloading open forms; status explains queued, deferred and failed states.
- Added revision-protected `/sync-v2` using a per-code SQLite Durable Object. Legacy encrypted KV data is imported without deleting it. Stale writes receive 409 and merge/retry; old client write paths return upgrade-required 426. Worker-first release and compatible rollback requirements are documented in DEPLOYMENT.md.
- Verification: synthetic unit/browser coverage includes parser edge cases, rescheduling, ambiguous identity, calendar UID transition, profile/file isolation, idempotent restore, quota rollback, startup crash recovery, PGCE-only tracking, two-device conflict/merge/deletion, and legacy v2/v3 restore. Final release results are recorded below after the gate.

Phase 2 release gate: Pages-layout build, 12 unit tests and 9 isolated browser tests passed; the full Vercel-layout gate also passed before the final persistence-notice refinements. The worker-first deployment gate reran the Pages build and all 21 tests successfully. Authentication preflight passed. Deployed push worker `500e90a0-2157-4441-9f85-acfd6d1b26d4` (including `SYNC` binding) and feed worker `ff3de1ee-7ea2-4217-a826-23dba37de119`. No production notification, subscription or analytics test writes were used.

Final worker revisions after calendar-ID escaping: push `917e1cd1-56f2-4f35-b192-345468cbfb18`, feed `43900bd2-e329-42b1-bdc1-08f9934c97c4`. The worker gate again passed all 12 unit and 9 browser tests. Equal-timestamp deletion markers take precedence over stale live records. Legacy single-profile migration now retains its original copy when persistence fails.

### Repository-local AI development handoff — 7 September 2026

Copied the detailed Phases 3–7 implementation handoff and its complete design-asset directory into the shared source repository. Added links from AGENTS.md and the enhancement report so Codex and Claude can discover the same specifications. Verified byte-for-byte copies and every relative design link. Documentation/assets only; application code is unchanged.

### Pass 39 — Phases 3–7 implementation plan — 7 September 2026

Read the full handoff (TIMETABLE_PWA_REMAINING_PHASES_IMPLEMENTATION.md, 921 lines) against the current baseline and produced the execution plan below. Baseline note: main is at `f783992`, two commits past the handoff's recorded Phase 2 release commit `439371d` — both are compatible bug fixes (shared parser zero-duration markers; identity-review duplicate-row deadlock) already deployed and verified live. No contract drift; the handoff's Phase 3 starting point holds.

**Sequence (binding, from the handoff):** Phase 3 (reliability/data correctness) → Phase 4 (design system + navigation redesign) → Phase 5 (editable workflows) → Phase 6 (real travel planning + collaboration) → Phase 7 (optional expansion, only on explicit instruction). No phase starts until the previous phase's release gate passes and its completion record (handoff §9.4) is written here.

**Branching:** per handoff §1.1, each phase runs on its own branch (`phase-3`, `phase-4`, …) with one commit per work item; merge to `main` (fast-forward) once that item's tests and the repository gate pass, so main stays releasable and worker-first ordering is preserved per item. Both gates for base-path-affecting work: `npm run validate` and `VERCEL=1 npm run validate`.

**Phase 3 execution order** (dependencies first, worker-touching items clustered to minimise worker deploy cycles):

1. **Fixtures first** — build `course-basic`, `membership-ranges`, `calendar-timezones` and `profile-race` synthetic fixtures (§9.1) before touching behaviour; extend `tests/fixtures.ts` keeping its external-request blocking.
2. **P3-02** race-safe per-source refresh (AbortController + generation counters in `useTimetableData`) — foundational; several later items depend on "late results are ignored".
3. **P3-03** single date-selection model shared by Day/Week/Month/Agenda.
4. **P3-01** membership vs display filters with group-range parsing (`1-10`, lists, all-groups) — pure logic in `src/lib/groups.ts`/`filters.ts` + settings UI.
5. **P3-06** attendance statistics with Attended/Absent/Unrecorded denominators (client-only).
6. **P3-08** travel freshness envelope (loading/live/cached/estimate/unavailable) in `src/lib/tfl.ts`/`campus.ts` consumers — presentation contract Phase 6 later builds on.
7. **P3-04** calendar/timezone/ICS parity (Europe/London course timezone, octet-based folding) — shared code + feed worker; worker-first deploy.
8. **P3-05** profile-scoped notification payloads with owner — push worker + client; worker-first deploy.
9. **P3-07** worker KV list pagination + stable study-group member IDs with Durable Object transactions — push worker; worker-first deploy. Items 7–9 land as one worker deployment window where practical.

Phase 3 exit: handoff §4 release gate + completion record here, then live verify via `./scripts/deploy.sh verify`.

**Phase 4:** P4-01 first (tokens + primitives + feature inventory — the inventory is the preservation checklist for every later screen), then Today → mobile Schedule → desktop Schedule → session detail (Overview + Travel & map with visible map) → Journey home full screen → Tasks/PGCE file → Settings → onboarding → accessibility + screenshot matrix (320/360/390/768/1024/1440, light/dark). Four-destination nav: Today · Schedule · Tasks · PGCE file. No future-date arrival-by claims (that is Phase 6); design assets in `handoff-assets/` are the reference.

**Phase 5:** storage contracts first (tombstone/revision pattern + DraftEnvelope), then P5-01 Tasks CRUD → P5-02 PGCE record editing/drafts → P5-03 placement hours & exceptions → P5-04 evidence search + binder preview → P5-05 assignment work plans → P5-06 personal commitments (no recurrence in v1). Every new collection joins backup/sync/delete before its UI ships.

**Phase 6:** P6-01 JourneyIntent/JourneyRequest model with arrival-by routing (verify TfL Journey Planner date/time/arrival parameters against current docs at implementation time) → P6-02 explicit origins + strict cache invalidation → P6-03 forecasts/disruptions/departure boards → P6-04 journey-home itinerary parity → P6-05 group availability/proposals.

**Phase 7:** explicitly deferred pending user instruction — P7-02 in particular may require paid object storage, which the handoff does not pre-authorise.

Standing constraints carried into every item: never delete/overwrite production KV outside records created by the task; reserved `ffffffff…` analytics test ids only; `/test` broadcast stays 410; deploy compatible workers before app; Vercel keeps `--skip-e2e`; browser gate stays in GitHub Actions; document migrations + rollback per stored-schema change.

### Pass 40 — Phase 3 complete: consistent schedule, integrations and statistics — 7 September 2026

Phase / work-item IDs completed: P3-01 … P3-08 (all eight), per TIMETABLE_PWA_REMAINING_PHASES_IMPLEMENTATION.md §4.

Commit and branch: implemented as eight reviewable commits on `phase-3` (`56622bb` P3-01, `d30ad24` P3-02, `04a84f0` P3-03, `2c3ea0b` P3-04, `e7acac6` P3-05, `00b7fe7` P3-06, `e5b71aa` P3-07, `5d59e55` P3-08), merged to `main`.

User-visible behaviour:
- Group ranges ("1-10", lists, all-groups) now match membership everywhere — app and push worker agree via one shared parser. Clear filters keeps group/specialism membership; reminder participation for optional/self-study sessions is an explicit choice, no longer a side effect of display filters; a room/subject filter can no longer remove reminders.
- A slow refresh from a previous profile/config can never overwrite a newer one; a failed source keeps its last good rows labelled stale; extra tabs are owned by source identity, not array position; the app revalidates on resume (10-minute throttle).
- One selected date across Day/Week/Month: month/week navigation moves it, view switches preserve it, Today resets it, a Today-only display range widens instead of hiding a picked date; today follows Europe/London and rolls at midnight without jumping the selection.
- Calendar downloads, Google Calendar links and the subscribed feed all emit UTC instants converted from London wall time (DST-correct on any device); deadlines appear in Week view; ICS lines fold by UTF-8 octets.
- Notification taps/actions route to the OWNING profile; deadlines get Open/Mark done/Snooze (never "Attended"); missing events show a safe notice + Changes.
- Stats/Settings/CSV/binder share one attendance definition with Attended/Absent/Unrecorded separated and inferred placement days labelled.
- Study groups: stable member ids + device capability tokens, same-name members distinct, transactional joins; travel durations are honestly labelled live / cached-with-age / estimate, and departure boards pause when hidden and expire.

Stored schema/contract changes:
- Settings: + remindOptional, remindSelfStudy (migrated conservatively from old display-filter behaviour), + groupMemberId, + groupToken (token excluded from cross-device sync via shared/merge.js deviceSettings).
- Notification payloads/queued actions: versioned v2 {profileId, key, kind}; legacy unscoped actions apply only in single-profile stores, are never re-attached.
- Push subscribe config: + profileId, + remindOptional (validated in shared/contracts.js).
- Worker: new GroupStore SQLite Durable Object (binding GROUPS, migration tag groups-v1); legacy KV `grp:` records imported on first access and left in place; old /group request/response shapes still accepted.
- No changes to sync-v2, backup v4, KV namespace ids, or calendar UIDs.

Migration and rollback: settings migrations run in normalizeStore on load (idempotent). Worker rollback: redeploy the previous worker revision but KEEP the GroupStore class export and GROUPS binding/migration in wrangler.toml (Cloudflare migrations are append-only; removing the class requires a deleted_classes migration and would drop group data). Reverting the app alone leaves the worker fully compatible with old clients. ICS consumers see identical instants (UTC vs floating-local for London users); no UID changed, so no duplicate events.

Automated checks + results: `npm run validate` and `VERCEL=1 npm run validate` both pass — 57 unit tests (membership, refresh/generation gate, calendar-time incl. 2027 spring gap + 2026 autumn ambiguity, ICS octet folding, eligibility, actions routing, GroupStore, KV pagination, travel freshness, plus all prior phase-1/2 suites) and 11 isolated browser tests (existing 9 + phase-three.spec.ts: shared date selection across views; Clear filters preserves membership).

Coverage contract (P3-04, binding): Today/Schedule = course membership; Notifications = explicit reminder eligibility independent of screen filters; Downloaded calendar = course membership all dates (chooser UI arrives with Phase 4/5 scope work); Subscribed feed = chosen public sources + enrolment rules, no personal content in query strings; Evidence/attendance = eligible completed sessions per shared/eligibility.js; Group availability = busy/free intervals only, no titles/notes/addresses.

Screenshots: not captured this phase (reliability release on the existing UI; the Phase 4 redesign carries the screenshot matrix requirement).

Real-device/usability checks actually performed: none this phase — automated suites only. Importing the new UTC-based ICS into Google/Apple Calendar on real accounts is pending and listed below.

Known limitations / intentionally deferred: in-app reminder scheduling still compares London dates with the device clock's minutes (exact only in the UK; full course-timezone scheduling arrives with the Phase 6 travel/time work); the feed emits no SEQUENCE/METHOD:CANCEL records (unchanged from Phase 2); durable (cross-isolate) abuse counters not added — in-memory per-IP caps retained; per-source status UI is minimal (statuses feed the existing error banner; richer surfacing is Phase 4).

Worker versions + hosted commit statuses: push worker `d34465a8-af85-4442-855e-87235680f684` (GROUPS Durable Object migration groups-v1 applied), feed worker `7b8cfc54-6629-49af-b7a4-e99bcdff79f0`, deployed worker-first before the app push. Live spot-check after worker deploy: /vapid 200, /stats 401 locked, DO group create→legacy join→stale-token 403→leave→404 all correct with a synthetic self-deleted test group. Hosted statuses and `deploy.sh verify` output recorded after the app rollout.

Phase 3 release verification (7 September 2026): GitHub Actions run 34138204032 for `8708f3f` completed **success** (full browser gate + Pages deploy); Vercel production deployment `my-timetable-1ivd1b33w` ● Ready from the same push, aliased to pgce-timetable.vercel.app. Live verification output, verbatim:

```text
== verify live endpoints ==
PASS: vercel app 200
PASS: pages app 200
PASS: push worker /vapid
PASS: /stats locked (401)
PASS: feed worker + cache header
PASS: analytics dashboard 200
done.
```

### Pass 41 — Phase 4 complete: daily-use redesign — 7 September 2026

Phase / work-item IDs completed: P4-01 … P4-10 (all ten), per TIMETABLE_PWA_REMAINING_PHASES_IMPLEMENTATION.md §5, including the §5.1 visual system and §5.2 responsive shell.

Commit and branch: ten commits on `phase-4` (`f8bbddb` P4-01/02, `82c49c7` P4-03, `077394f` P4-04, `cb1ba6d` P4-05, `d9abcdc` P4-06, `c15e497` P4-07, `35cbc92` P4-08, `7469e72` P4-09, `171491a` P4-10), merged to `main`.

User-visible behaviour:
- Four-destination shell — Today · Schedule · Tasks · PGCE file — as a labelled bottom nav on phones and a sidebar from 1024px, with hash routes, browser-Back walking detail → caller, and full-page details replacing the bottom nav on small screens.
- Today: date header with source status, at most one urgent item, a next/current hero with the room made prominent (Session details + Directions), rest-of-day rows with break gaps, deadline strip, journey-home entry (promoted, dismissible after the last session), tomorrow preview, and honest empty/failure states.
- Schedule: mobile Week mode is a seven-day Monday–Sunday strip driving ONE selected-day list (breaks derived from real intervals, clashes badged); Month drives the same list; desktop gets the full-width week grid plus a ≥1280px selected-session panel; search and temporary filters live here.
- Session detail: full-page route with Back on phones, sheet on desktop; Overview / Travel & map tabs (map visible immediately, steps collapsed, freshness-labelled summary, sticky-size external nav + outside-app caption); deadlines open task detail (status + notes, no attendance/travel).
- Journey home: labelled Today entry and a full #/home screen with explicit origin states (no-home, waiting, off, near-home) — the header pill and clipped popover are gone.
- Tasks: Overdue/Upcoming/Completed; PGCE file: Placement / Evidence & reflections / Development / Documents with counts linking into every existing record type.
- Settings: index + seven focused pages incl. a data-health list (save state, sync, per-source last-updated, storage estimate) and a new compact density; What's new lives in Help & privacy.
- Onboarding: Connect → Personalise → Preview; the profile is created only after a validated preview.
- §5.1 palette/typography/spacing tokens with dark mode and explicit theme overrides beating the OS in both directions; single SVG icon family; 3px accent focus outline; reduced-motion guard; month grid arrow-key navigation.

Stored schema/contract changes: Settings + `density?: 'compact'`; the old `filters.dateRange` display filter is retired (field kept, ignored — no migration needed); no worker, shared-module, sync, backup or notification contract changes (verified: zero diffs under workers/ and shared/).

Migration and rollback: pure frontend release — reverting the app commit fully restores the old UI; caches, metadata, sync and notification payloads are untouched. Setup share links (#setup=) and notification deep links continue to work (hash routes coexist).

Automated checks + results: `npm run validate` AND `VERCEL=1 npm run validate` both green — 57 unit tests and 23 isolated browser tests (12 existing/behavioural + 11 new phase-four visual/acceptance). tests/phase-four-visual.spec.ts runs the required matrix with deterministic 7 Sep 2026 fixtures and network isolation: six widths (320/360/390/768/1024/1440) × top-level screens with horizontal-overflow assertions, session Overview + Travel & map, home entry/route/no-home, all seven Settings categories, dark + explicit light/dark overrides, 200% zoom reflow proxy, keyboard nav (calendar arrows, focus ring), source-failure state, and a full onboarding walk against a synthetic sheet (Back preserves choices; invalid rows reported, not guessed). Screenshots land in test-results/phase4/ per run.

Feature preservation: docs/PHASE4_INVENTORY.md — every pre-redesign control mapped and ✅ in the new shell; the Day tab and the 🕰 history toggle are consciously retired (any past day is one tap away via the strip/Month; history data untouched).

Screenshots (viewport/theme/state): generated by the visual spec on every run (not committed); manual before/after comparison done against handoff-assets/comparison-*.png during development.

Real-device/usability checks actually performed: none this phase — automated + emulated only. Real iOS/Android install, safe-area, keyboard and the "find your next room" user test remain open (listed in the handoff §9.2 as separate from automated success).

Known limitations / intentionally deferred: no future-date arrival-by promises anywhere (Phase 6); desktop detail between 1024–1279px uses the dialog sheet rather than a drawer; the 200% check is a zoom proxy, not OS text scaling; unfinished-setup drafts are not persisted (Back within the wizard preserves state; a reload restarts it).

Worker versions + hosted commit statuses: no worker deployment required (frontend-only); hosted statuses for the released commit recorded below after push.

Phase 4 release verification (7 September 2026): GitHub Actions run for `df2cbf3` concluded **success** (full browser gate incl. the phase-four visual matrix + Pages deploy); Vercel production Ready and serving the new bundle (verified by fetching the deployed JS — new onboarding marker present — and confirming "Step 1 of 3 · Connect" renders live after the service worker's auto-update cycle; the one-open-stale window is the PWA's designed autoUpdate behaviour). Live verification output, verbatim:

```text
== verify live endpoints ==
PASS: vercel app 200
PASS: pages app 200
PASS: push worker /vapid
PASS: /stats locked (401)
PASS: feed worker + cache header
PASS: analytics dashboard 200
done.
```

No worker deployment was needed or performed (frontend-only phase; push `d34465a8` / feed `7b8cfc54` remain current).

### Pass 42 — Phase 5 complete: editable planning and PGCE workflows — 7 September 2026

Phase / work-item IDs completed: P5-01 … P5-06 (all six), per TIMETABLE_PWA_REMAINING_PHASES_IMPLEMENTATION.md §6.

Commit and branch: eight commits on `phase-5` (`ce02d77` foundations, `2c5f574` P5-01+draft core, `8f827c6` P5-02, `55926d5` P5-03, `3de253a` P5-04, `0f17716` P5-05+06, `f70d10d` backup-validation fix), merged to `main`.

User-visible behaviour:
- Tasks are fully editable records: create/edit/duplicate/complete-reopen/delete with Undo; identity survives edits; imported sheet deadlines stay source-owned with an explanation of what the sheet controls; open mentor actions appear in Tasks and complete on the meeting record exactly once.
- Every PGCE record type (reflections, targets, meetings incl. per-action identity, observations, lessons, audits) has a labelled Edit/Delete with local drafts: debounced honest "Draft saved", Continue/Discard on return or reload, Keep mine / Use latest when sync changed the record mid-edit.
- Placement: planned vs logged days/hours with per-date exceptions (holiday/inset/part-day/cancelled/hours corrections), explicit working-hours + inset policy, inferred days labelled until confirmed, provenance in the CSV; excluded days leave schedule/stats/calendar/reminders together.
- Evidence journal: search (query/date range/type/standards/Untagged) across notes, photo captions, reflections, evaluations, targets and observations; honest empty states; context survives Back. Photo captions ride backups and the printed binder; the binder now previews range/sections/counts and labels photos that live on another device.
- Work plans under a deadline: subtasks/milestones/study blocks with progress, effort totals and clash flags; a moved due date names stranded blocks for review; completing with open subtasks asks an explicit policy.
- Personal commitments/study blocks: individual events with busy/free participation and a reminder choice; they show as Personal on Schedule/Today, keep "day finished" honest, join clash detection and study-group busy time (intervals only — titles never leave the device), and appear in the .ics download only behind an explicit choice.

Stored schema/contract changes:
- shared/contracts.js: four new record collections — tasks, exceptions, plans, commitments — in the generic revision/tombstone/merge machinery; OPTIONAL on read so legacy sync payloads and v2/v3/v4 backups still validate; strict enum/date/time/bounds validation when present.
- Settings: + placementHours, placementInsetCounts, includePersonalInExport. Session view gains a non-persisted isFreeTime marker; photos gain a caption (excluded from identity hashing — no photo forks).
- Drafts: timetable.draft.v1.<kind>.<id>.<pid> local envelopes (never synced; swept by profile deletion).
- Migration: Settings.customKeyDates → task records keeping the old ids and `custom-<id>` calendar UIDs (no duplicate calendar events) and adopting saved status/notes from the legacy owner key; the field clears only after the records persist.

Migration and rollback: all migrations are forward-only but non-destructive — an old app version ignores the new collections (and drops them from ITS sync blob; a new device re-merges them from local state), reads the same backups, and sees personal deadlines only if it predates the migration (the records remain; the legacy field is empty). Reverting the app leaves records intact.

Automated checks + results: `npm run validate` AND `VERCEL=1 npm run validate` green — 67 unit tests (new: record merge/undo-tombstone semantics, back-compat validation, placement rules incl. inset policy/half-days/provenance) and 32 browser tests (9 new phase-five: migration fidelity, CRUD/undo, mentor-action single ownership, draft recovery across reload for tasks AND PGCE records, evidence search + honest empty states, binder preview counts/availability, work-plan review prompts, commitment lifecycle/day-finished honesty). One regression caught and fixed at the gate (photo-caption validation had broken legacy-backup import).

Release-gate check (§6): every supported record type editable with recovering drafts ✓; every new collection participates in backup/sync/delete via the shared record store ✓; placement/evidence totals consistent by construction (single shared helpers) ✓; work plans and personal commitments reference their canonical records (no duplicated status anywhere) ✓.

Known limitations / intentionally deferred: three-way field-level merge falls back to whole-record Keep mine/Use latest (per-field merge deferred); commitment reminders reuse session-reminder offsets; imported-deadline work plans are supported in data (parentId = event key) but the plan UI attaches to personal tasks only; no recurrence for commitments (deliberate, per the handoff).

Worker versions + hosted commit statuses: recorded after deployment below.

Phase 5 release verification (7 September 2026): workers deployed FIRST per the runbook (shared/contracts + shared/placement changed) — push worker `e3fb4354-cbb6-4476-9cb8-b7204743206a`, feed worker `05e5e74b-b774-411e-a00c-83a3e2c56e81`; worker behaviour is unchanged (validWorkerConfig untouched), the deploy keeps bundled shared code in step. GitHub Actions for `4b22653` concluded **success** (full browser gate incl. the 9 phase-five specs + Pages deploy); Vercel production serves the new bundle (deployed JS contains the Phase 5 surfaces). Live verification, verbatim:

```text
== verify live endpoints ==
PASS: vercel app 200
PASS: pages app 200
PASS: push worker /vapid
PASS: /stats locked (401)
PASS: feed worker + cache header
PASS: analytics dashboard 200
done.
```


## Coordinated timetable/admin icons — 7 September 2026

Generated native vector icon families matching the updated UI: an indigo session calendar for My Timetable and a navy calendar/analytics mark for the admin dashboard. Added SVG and multi-size ICO favicons, 16/32/48 PNG favicons, 180px Apple touch icons, 192/512 standard and maskable PWA icons, 1024px artwork, preview and a reproducible generator. Updated only HTML icon links and manifest icon entries; preserved launch URLs, scope, theme settings, registerType and concurrent Phase 6 work. Source and preview are in `branding/`; admin assets are isolated in `public/admin/`. Dimension and maskable safe-circle validation passed for both families. Build verification is recorded after the isolated asset check. Not deployed by this task.

Icon verification: isolated snapshot `npm run build` (TypeScript + GitHub Pages Vite/PWA build) passed; `VERCEL=1 vite build --outDir dist-vercel` passed. Both generated manifests and both HTML pages resolve all icon assets under both host base paths. Both ICO containers validate as 16/32/48 PNG entries. All maskable glyph pixels fit inside the safe circle. Existing unrelated dynamic/static import warnings remain. No production request or deployment was made.

Deployment note: committed as `1957c18` and released together with Phase 6 (Pass 43) — the full validate gate was re-run on the combined state (icons + Phase 6) before the push, and the live verification below covers both. Only icon links and manifest icon entries changed; registerType, launch URLs and scope verified untouched in the released diff.

### Pass 43 — Phase 6 complete: accurate travel planning and collaboration — 7 September 2026

Phase / work-item IDs completed: P6-01 … P6-05 (all five), per TIMETABLE_PWA_REMAINING_PHASES_IMPLEMENTATION.md §7.

Commit and branch: two commits on `phase-6` (`2245230` P6-01..P6-04 journeys, `807392f` P6-05 study groups), merged to `main`.

User-visible behaviour:
- A future session's Travel tab now asks TfL for a real arrive-by journey on the session's ACTUAL date (date/time/timeIs=Arriving), and answers "Leave by HH:MM" with the planned arrival, the journey + arrival-buffer breakdown, when the plan was fetched, and which origin it is from. The arrival buffer is user-settable (0/5/10/15/20 min) per profile.
- Origins are explicit: current location (with fix age; a missing/denied fix is stated, never silently substituted), home, campus and geocoded placement schools. The plan clears the instant origin, destination, mode, buffer or intent changes — a stale itinerary cannot survive any of them by construction (request-identity keying).
- Itineraries are honest end-to-end: per-leg planned times, line badges, live departure boards (stop + direction, near-term journeys only, 30s poll while visible), and disruption warnings adjacent to the affected leg. A passed planned departure says so and switches to a truthful leave-now route — never a negative countdown. Driving keeps a labelled estimate; no fabricated plans.
- Provider failure keeps the destination usable: address, map area, Copy address and Open in Google Maps all remain.
- Leave-alert notifications consume the SAME published plan the travel tab displays (one leave-plan store keyed by event), so the reminder and the screen always agree; a plan that errors clears its published plan rather than letting reminders fire on stale data.
- Journey home is leave-now with an explicit origin selector and live boards; the crude distance estimate stays clearly labelled when no plan exists.
- Study group: every member row shows Last updated; members whose availability is stale (>24h) or missing are EXCLUDED from the common slots and named as such — nobody is shown confidently free on old data. Working hours and minimum meeting length are explicit controls. Meeting proposals are a two-step explicit send with stable id and revision; accept/decline/withdraw are capability-gated and conflict-checked (simultaneous edits get the fresh copy, never a silent merge). An accepted slot exports to the user's OWN calendar with a stable UID — the UI states it does not appear in anyone else's calendar.

Stored schema/contract changes:
- Settings: + arrivalBufferMins, groupWorkStart, groupWorkEnd, groupMinMeeting (device preferences; group credentials remain never-synced).
- GroupStore DO record: members gain a validated IANA timezone; new `proposals` map {id, rev, by, slot, status, participants, responses, at} — intervals only, no free text accepted or stored; past-dated proposals pruned on read; a leaving member's votes are removed and their open proposals withdrawn. New worker routes /group/propose and /group/proposal/respond (rate-capped, capability-gated). Availability publishes carry tz + horizonDays; old clients that omit them keep working (fields optional).
- New shared modules: shared/journey.js (request identity, itinerary validation from provider wall times, feasibility pick, departure state) and shared/availability.js (freshness policy, free-interval computation, cross-timezone slot conversion, payload builder, proposal calendar identity). Client-side: 10-min journey plan cache per request identity, 20-min leave-plan store, publish guard (republish only when slots changed or 12h elapsed).

Migration and rollback: no stored-record migrations. Old clients ignore proposals and tz (additive DO fields); reverting the app loses only journey planning, not data. Worker is backwards-compatible with the released app (new endpoints are additive; existing /group contracts unchanged — the member listing only gains optional fields).

Automated checks + results: `npm run validate` AND `VERCEL=1 npm run validate` green — 86 unit tests (13 new: journey identity/buffer/DST parsing/feasibility/departure states; availability freshness/busy blocks/working hours/timezone conversion incl. cross-midnight splits/same-name distinctness/payload privacy/export identity; DO proposals: capability, 409-on-stale-rev, proposer-only withdraw, leave cleanup, past pruning) and 38 browser tests (5 new phase-six, network-blocked with synthetic TfL/group fixtures).

Release-gate check (§7): actual future-date itinerary intent proven by captured request fixtures (date=20260908 & time=0850 & timeIs=Arriving; buffer change → time=0840) ✓; buffer and reminders agree (single published leave plan consumed by both) ✓; stale results cannot survive origin/destination/mode/intent changes (request-identity keying + abort, browser-tested) ✓; external navigation works through provider failures ✓; group availability is current (freshness policy), private (intervals-only payloads inspected in unit AND browser tests) and concurrency-safe (DO transactions + revision checks) ✓.

Known limitations / intentionally deferred: no full-route map geometry (P7-03); driving has no planning provider (labelled estimate only, by design); proposal exports carry a fixed neutral title ("Study group meet-up") since free text is deliberately refused server-side; departure boards cover the first three transit legs.

Worker versions + hosted commit statuses: recorded after deployment below.

Phase 6 release verification (7 September 2026): workers deployed FIRST per the runbook (shared/ and workers/push changed) — push worker `659f0710-d51e-41f1-ac1c-668d262e0726`, feed worker `c101c5d3-3eb3-4897-a426-9c43b2676f2e`; the new /group/propose and /group/proposal/respond routes and member tz fields are additive, so the deployed worker stayed compatible with the still-running app release. Released commit `70adc54` (merge `050e369` phase-6 + `1957c18` icon families + PLAN records); GitHub Actions run 34153715801 for that exact commit concluded **success** (full browser gate incl. the 5 phase-six specs + Pages deploy). Both validates were re-run on the combined icons+Phase-6 state before the push (86 unit + 38 browser, both hosting layouts). Live checks: new favicon.svg serves 200 on Vercel AND Pages, admin/favicon.svg 200, and the deployed Vercel bundle contains the Phase 6 journey surfaces. `./scripts/deploy.sh verify`, verbatim:

```text
== verify live endpoints ==
PASS: vercel app 200
PASS: pages app 200
PASS: push worker /vapid
PASS: /stats locked (401)
PASS: feed worker + cache header
PASS: analytics dashboard 200
done.
```

### Pass 44 — Phase 7 (P7-01 + P7-03): configurable courses and full-route maps — 7 September 2026

Phase / work-item IDs completed: P7-01 and P7-03, per TIMETABLE_PWA_REMAINING_PHASES_IMPLEMENTATION.md §8. **P7-02 (encrypted attachment sync) was explicitly excluded by the owner's instruction** ("Run phase 7 but exclude P7-02") — it may need paid storage, which the handoff does not pre-authorise; nothing of it was built, and the release-gate clause about verified file bytes/encryption/deletion is therefore out of scope for this pass.

Commit and branch: two commits on `phase-7` (P7-01 course configuration, P7-03 route maps), merged to `main`.

User-visible behaviour:
- **Course configuration (P7-01):** Settings → My timetable gains a Course section and a Course setup sheet. The UCL Primary PGCE setup is a first-class built-in; another course can be set up in a form (name, template id/version, Intl-validated IANA timezone, campus label/coordinates/search suffix, terminology labels, feature toggles, keyword-matched buildings editor) — no raw JSON needed. Templates import by paste with validation errors or a preview (name, timezone + today-there, campus, building count, features) before an explicit Apply; export downloads a sanitized template. Onboarding names the active course. The configured course drives: campus journey origin, building gazetteer, unknown-room external search suffix, "today"/session-time conversions, Google Calendar links, .ics export zone (and a validated `tz` param on the subscribed feed), study-group availability timezone, specialism/group terminology in filters, and feature flags that hide the PGCE file destination and/or Placement section for courses without them. TfL request/response wall times deliberately stay Europe/London — provider truth, not course-configurable. Unknown rooms keep raw text + external search; they are never guessed onto a similarly named building.
- **Full-route maps (P7-03):** when TfL sends leg geometry (`path.lineString`), the travel tab and journey home draw the actual route over OSM tiles — line-coloured legs, dashed walking, origin/destination markers, "© OpenStreetMap · Powered by TfL Open Data". Selecting a step in the text directions (keyboard: Enter/Space, aria-pressed) highlights that leg on the map; text steps remain the primary, complete description. Legs without geometry are simply not drawn and the map says so — a straight line between stops is never fabricated. Bounds fit once per itinerary with padding, so live departure-board polls can't make the map jump; the map is static (no gestures, no scroll trapping). Absent geometry/provider failure/offline keep the Phase 4 destination-only map + address + steps. An explicit note marks pins as approximate building centres with entrances/step-free access NOT verified — no accessibility claims exist without a verified source.

Stored schema/contract changes:
- Settings: + `courseConfig` (validated whitelist rebuild of {configId, version, name, timezone, terminology, campus, buildings, features}); absent = UCL built-in, so migration is a no-op and old profiles behave identically (unit-fixtured).
- New shared modules: shared/course.js (validator, UCL built-in fixture, sanitizeTemplate — whitelist rebuild so sync codes/addresses/evidence structurally cannot enter a template — and applyTemplate which changes courseConfig and nothing else) and shared/route-geometry.js (lineString parse, bounds/fit/projection math). Itinerary legs gain `geometry` ([lat,lng][], [] when absent).
- Feed worker: optional `tz` query param (Intl-validated IANA, default Europe/London unchanged) so a non-London course's subscribed calendar stays on course wall time. Additive and backwards-compatible; push worker untouched.

Migration and rollback: no data migrations. Reverting the app ignores `courseConfig` (unknown settings field) and loses only the new surfaces; a stored config that later fails validation falls back to the UCL built-in rather than half-applying.

Automated checks + results: `npm run validate` AND `VERCEL=1 npm run validate` green — 97 unit tests (11 new: migration fixture, second course in another timezone on the SAME shared conversion path, same-name distinct configIds, invalid-template refusal, template update preserving personal data byte-for-byte, no-secrets share payload; lineString parse/partial garbage/downsample, padded fit with in-viewport proof, long-vs-short zoom + degenerate bounds, leg geometry present/absent/broken) and 40 browser tests (3 new phase-seven: geometry-only drawing + attribution + leg highlight + honest entrance note; absent-geometry fallback with complete text steps; full course-template flow incl. invalid refusal, preview, feature-flag nav change, terminology, unknown-room suffix and switch-back).

Release-gate check (§8, minus the excluded P7-02 clause): another course works without source-code edits — the NYU fixture applies entirely through the UI/template path and reshapes campus, timezone, terminology, features and search behaviour ✓; route lines are backed by actual provider data only (geometry drawn solely from TfL lineStrings, absent geometry draws nothing, browser-proven) and no entrance/accessibility claim exists anywhere, with an explicit not-verified statement instead ✓.

Known limitations / intentionally deferred: P7-02 excluded by instruction (above). Placement tag patterns and source parsing remain UCL-sheet-shaped (a new course reuses the same sheet format); course templates carry no placement policy overrides yet. Route maps have no pan/zoom gestures (static fitted view + OSM link, by design); tile requests use the standard OSM tile server under its usage policy with normal browser/SW caching only — no bulk tile prefetching.

Worker versions + hosted commit statuses: recorded after deployment below.

Phase 7 release verification (7 September 2026): workers deployed FIRST per the runbook (shared/ and workers/ics-feed changed) — push worker `e736c7a7-9ae5-4773-ae73-5535706af486`, feed worker `5df0f491-fdbb-43e3-b9dc-5b5af5ecf60d`; the feed worker's `tz` param is additive (default behaviour byte-identical), the push worker redeploy only refreshes bundled shared code. Released commit `8ae19f4` (merge of `phase-7`); GitHub Actions run 34155245893 for that exact commit concluded **success** (full browser gate incl. the 3 phase-seven specs + Pages deploy). Both validates ran green before the push (97 unit + 40 browser, both hosting layouts). The deployed Vercel bundle contains the Phase 7 route-map surfaces ("Powered by TfL Open Data"). `./scripts/deploy.sh verify`, verbatim:

```text
== verify live endpoints ==
PASS: vercel app 200
PASS: pages app 200
PASS: push worker /vapid
PASS: /stats locked (401)
PASS: feed worker + cache header
PASS: analytics dashboard 200
done.
```

### Pass 45 — Admin analytics A1: secure access and honest output — 7 September 2026

Work items: phase A1 of `admin-analytics-handoff/ADMIN_ANALYTICS_ENHANCEMENTS.md` (§9), per `admin-analytics-handoff/DEVELOPMENT_PLAN.md`. Findings addressed: ADM-01, ADM-02, ADM-03, ADM-04, ADM-09, ADM-10, ADM-11, ADM-12 (both halves), ADM-14, ADM-16 (bounds portion), ADM-20 (disclosure portion). Remaining findings are A2+ scope by design.

Commit and branch: one commit on `admin-a1`, merged to `main`.

Behaviour changes:
- `/stats` fails closed: absent/empty server key → 503 "configuration unavailable" and wrong/absent credential → 401, both BEFORE any analytics scan; `Authorization: Bearer` supported (constant-time compare) with the legacy `?key=` accepted only until 21 Sep 2026 (removed in the A2 worker deploy); `Cache-Control: no-store` on every stats response including errors; CORS preflight allows Authorization. Named 7/30-day figures use their own fixed windows regardless of `?days`; only valid-read rows count as activity; capped scans surface `scanComplete:false` (shared `listAllKeys` semantics untouched — analytics uses a new checked variant); positive-only adoption; per-flag known-value setup denominators. `/ping` body bounded to 8 KiB, stores positive counters and reported booleans only.
- Dashboard: in-memory credential + snapshot only, legacy `tt.statskey`/`tt.statscache` purged on load; visible Lock (clears + aborts + generation-guards; late responses cannot repopulate); honest 401/503/429/network states with retry; fixed chart geometry with axis/date labels/focusable value text; truthful metric copy (tokens not people, standalone reports not installs, observed-days not "tried it once", attempt-labelled legacy counters, aggregation-time disclaimer, both retention facts disclosed); loud incomplete-scan warnings; unreported setup flags show "—", never 0%.

Data/contract changes: `/stats` response ADDS `timezone`, `includesPartialToday`, `completeness{scanComplete,missingRows,invalidRows}`, `daily[].listed` and `setup.known` — all additive; existing fields keep their names so a stale installed dashboard still renders (its cached-key path now simply gets 401/503 states). No stored-data migrations; no analytics rows written or deleted beyond normal ping ingestion.

Automated checks + results: `npm run validate` AND `VERCEL=1 npm run validate` green — 104 unit (7 new: fail-closed matrix with no-scan-before-auth spies on a fake KV, both auth forms, days=1 window independence, null-row completeness, 60-page capped-scan flag, zero/negative adopter minting, ping bounds/boolean-only setup) and 44 browser tests (4 new admin-a1: legacy purge + no-fetch-before-unlock + header-only credential, full lock lifecycle including a late in-flight response being discarded, honest 401/503 copy, nonzero bar geometry + honest labels + completeness warnings). Screenshots from synthetic fixtures at 390px and 1440px: `admin-analytics-handoff/evidence/a1-390.png`, `a1-1440.png` (page is dark-only until A3 ships themes).

A1 acceptance (spec §9): unauthorized/misconfigured stats never scan or render ✓ (unit spies + 503/401 before list); no secret in URLs or new persistent storage ✓; forced 401 after a loaded snapshot removes it ✓; a response arriving after Lock cannot repopulate ✓ (browser-tested); nonzero bars visibly render ✓ (offsetHeight-asserted); short-range requests do not truncate named fixed windows ✓; missing rows/capped scans flagged ✓; zero denominator shows unavailable ✓. Worker deploys before the page per the runbook.

Rollback: auth fail-closed + no-store are retained under any rollback; rendering changes are independently revertible. The legacy query credential path is documented for removal in A2 and must not outlive it.

Worker versions + hosted commit statuses: recorded after deployment below.

A1 release verification (7 September 2026): workers deployed FIRST — push worker `1260099d-e4d1-4777-81a7-43b99f585d33`, feed worker `405ac9d0-59f6-46dd-bee4-9c31a9bb573a` (feed redeploy only refreshes bundled shared code; its behaviour is unchanged). Live fail-closed checks against production BEFORE the page shipped: `/stats` with no credential → **401**, wrong Bearer → **401**, `cache-control: no-store` present (the production statskey is configured, so the 503 configuration path is not the live branch; it is unit-covered). Released commit `17ceb61`; GitHub Actions run 34156463597 concluded **success**. Both hosts serve the new dashboard (Admin workspace + legacy-key purge present in served HTML). `./scripts/deploy.sh verify`, verbatim:

```text
== verify live endpoints ==
PASS: vercel app 200
PASS: pages app 200
PASS: push worker /vapid
PASS: /stats locked (401)
PASS: feed worker + cache header
PASS: analytics dashboard 200
done.
```


## Learner app follow-up audit — 7 September 2026 (documentation only)

Added `timetable-app-audit/TIMETABLE_APP_AUDIT_AND_ENHANCEMENTS.md` and synthetic evidence: current UI review, 20 prioritized findings, eight new-feature specifications, styling/data contracts and R1–R6 implementation sequence for Claude. Frozen baseline `f342695`; admin A2 work discovered during packaging is explicitly excluded and preserved. Isolated validation: 104 unit tests, all 44 existing browser scenarios and six final audit observation scenarios completed; TypeScript/Vite/PWA build passed. Audit-only port/selector corrections are documented. No learner/admin application code, worker configuration or live data was changed, and nothing was deployed.

### Pass 46 — Admin analytics A2: reliable consent-aware collection and the v2 aggregate contract — 7 September 2026

Work items: phase A2 of `admin-analytics-handoff/ADMIN_ANALYTICS_ENHANCEMENTS.md` (§9), per `admin-analytics-handoff/DEVELOPMENT_PLAN.md`. Findings addressed: ADM-05 (in-flight events survive sends), ADM-06 (idempotent ingestion + atomic multi-tab claims), ADM-07 (consent gate on every call, opt-out clears + generation-guards), ADM-08 (UTC event-day buckets; dayparts deliberately omitted from v2), ADM-13 (versioned shared action catalogue; A4 events reserved and visibly not-collected), ADM-16 (schema-validated bounded ingestion), ADM-17 (published snapshots; /stats/v2 never raw-scans per refresh). Also closes the A1 legacy `?key=` window: `/stats` and `/stats/v2` are Bearer-only.

Commit and branch: one commit on `admin-a2`, merged to `main`.

Behaviour and contract changes:
- Client: coarse counters queue in a dedicated transactional store (`timetable.telemetry.v1` IndexedDB, bounded in-memory fallback) under their UTC observed date, with a cross-tab lease and immutable random-ID batches acknowledged by exact ID. Consent is read from the ACTIVE profile on every call; demo/opt-out collect nothing, and switching off clears pending counters and starts a new generation (Settings copy states that delivered batches cannot be unsent). Flushes on open/resume/reconnect/15-min foreground interval with bounded jittered backoff; Retry-After honoured; schema rejections discard rather than loop. Legacy undated counters (`timetable.usage.v1`, `timetable.lastping.v2`) are discarded at migration — v2 measurement start is recorded server-side; nothing is backdated. Builds now carry an immutable id (git commit) for release analytics.
- Worker: new isolated `AnalyticsStore` Durable Object (`analytics-v1` migration; VAPID/subscription/sync/group resources untouched) deduplicates `(token, batchId)` transactionally for 8 days, adds counts exactly once per accepted batch, tracks v2 first-seen and measurement start, and acknowledges only after durable commit — replays and restarts return the same ack. `POST /v2/batch` validates against the shared contract (16 KiB bound, whole-batch refusal) before any write and maintains the legacy `adev:` first-seen union on the same token namespace. `GET /stats/v2` (Bearer-only, fail-closed, section allowlist, no-store) serves the snapshot published by the existing 10-minute cron; a failed aggregation keeps the last valid snapshot; unchanged aggregates skip the KV write (≈0 extra writes/day at steady state — measured against the 1000/day KV free-tier budget, worst case 144/day).
- Legacy compatibility: `/ping` and `/stats` continue serving old clients/readers unchanged (receipt-day data, separately labelled); v2 and legacy counts are never merged into one metric.

Automated checks + results: `npm run validate` AND `VERCEL=1 npm run validate` green — 121 unit tests (17 new: contract accept/refuse matrix incl. future/expired dates, >7 segments, unknown/capability-2 events, count bounds; queue invariants — event during send survives, lost ack resends the SAME batchId, duplicate ack no-op, two-tab lease + expiry takeover, offline multi-day dates, opt-out clear + no resurrection, 999 clamps; DO — replay/restart idempotence over the same storage, same-token/day double batch adds once, aggregate new/returning/watermark/standalone coverage + not-collected honesty, scoped pruning; routes — /v2/batch refuses before write, /stats/v2 auth matrix + typed 503, Bearer-only regression asserting the closed query window) and 47 browser tests (3 new admin-a2: envelope + observed-day + privacy sweep of every outgoing payload, same-batchId retry across restart, real Settings opt-out clearing pending and blocking resurrection under a fresh generation).

A2 acceptance (spec §9): event during send survives ✓; replay does not double count ✓ (unit + DO restart); two tabs cannot drop or multiply accepted usage ✓ (lease + server dedupe); offline buckets preserve dates ✓; opt-out clears pending collection and blocks future sends ✓ (browser-tested via the real toggle); invalid/oversized payloads fail before writes ✓; legacy and v2 fixtures yield explicitly separate semantics ✓; coordinator kill/restart keeps committed acknowledgements idempotent ✓; resource usage measured (DO writes per batch ≈ days+2; snapshot cadence reuses the existing cron; KV snapshot writes skipped when unchanged) ✓.

Rollback: v2 collection stops safely by reverting the app (the worker keeps accepting both contracts; durable DO data is retained); the queue migration is idempotent; fail-closed Bearer-only auth is retained under any rollback and the query credential must not return.

Worker versions + hosted commit statuses: recorded after deployment below.

A2 release verification (7 September 2026): workers deployed FIRST with the `analytics-v1` Durable Object migration — push worker `402526fc` then `e51c6009-5e8f-4467-a0e3-cf5d89a01d50` (adds the reserved-test-token aggregation exclusion), feed worker `e6225441-3147-4737-9539-879d561ca7d1` (bundled-shared refresh only). Live endpoint proofs against production, using ONLY the reserved `ffffffff…` test token (its `adev:` KV row deleted by exact name afterwards; the DO excludes the prefix from every metric): legacy `?key=` credential now **401** (window closed) while Bearer returns 200; invalid `/v2/batch` **400 with reasons before any write**; valid batch **acked**, replayed batch returned the **same ack with `duplicate:true`** (live idempotence); `/stats/v2` wrong key **401**, pre-publication **typed 503 "aggregate unavailable"**, and after the next cron tick a **200 complete envelope** (schemaVersion 2, UTC period, completeness block, honest `observedThrough: null` / zero metrics — the test token correctly counts for nothing). Released commit `45ca2d3`; GitHub Actions concluded **success**. `./scripts/deploy.sh verify` all PASS (verbatim in the run log; same six PASS lines as Pass 45).

### Pass 47 — Admin analytics A3: responsive admin shell and core views — 7 September 2026

Work items: phase A3 of `admin-analytics-handoff/ADMIN_ANALYTICS_ENHANCEMENTS.md` (§9), per `admin-analytics-handoff/DEVELOPMENT_PLAN.md`. Findings addressed: ADM-15 (state machine, typed validated responses, timeout, cancellation, generation guard, visible retry), ADM-18 (responsive hierarchy, themes, labelled key field, accessible chart interaction), ADM-19 (analytics URL preserved on both bases as a real Vite entry; the learner SW's navigateFallback now denylists /analytics/ so an installed service worker cannot swallow the dashboard — registration strategy untouched).

Commit and branch: one commit on `admin-a3`, merged to `main`. Frontend-only (no worker/shared changes; no worker deploy needed).

User-visible behaviour: the dashboard is a React admin workspace on the handoff's exact design tokens — six destinations (desktop rail, mobile labelled section picker), System/Light/Dark themes defaulting to System, hash-routed sections that Back/Forward restore (deep links show the locked state first — the key never persists), an in-memory session with reveal/hide key control and a 15-minute idle auto-lock, and a status strip (aggregation time with a >30-min stale badge that explicitly does NOT claim service health, UTC/partial-today, v2 observed-through watermark, Refresh). Overview and Feature adoption are fully implemented: labelled legacy receipt-day data is the primary source, v2 event-day figures take over per feature once real observations exist, every uncollected event shows "Not collected" — never 0%. Return visits ships return frequency with cohorts explicitly not collected yet; Reliability and Releases are explicit coming states (release-note markers labelled as NOT build identities). Data & access carries the session truth, metric glossary, retention statement (including the no-expiry first-seen ledger and the A5-pending bounded-retention proposal) and an aggregate CSV export with per-row definitions, numerators/denominators and formula-prefix neutralisation. The chart is a single keyboard navigator (arrows/Home/End with a visible status readout) with a data-table alternative, and wide plots scroll inside their card — the page never scrolls horizontally.

Automated checks + results: `npm run validate` AND `VERCEL=1 npm run validate` green — 122 unit and 54 browser tests. New/updated: admin-a1 invariants re-anchored to the new shell (legacy purge, header-only credential, lock lifecycle incl. late-response discard, chart geometry, honest copy) plus 7 admin-a3 tests (zero page-wide overflow at 320/390/768/1440 across all six sections, hash history + locked-first deep link + mobile picker, theme cycling/persistence, idle lock under clock control, single-tab-stop chart keyboard navigation, sortable/searchable adoption with aria-sort and honest not-collected rows, CSV definitions/denominators/formula-safety/no-secrets). MPA output verified in BOTH dist layouts. Screenshots at 390px and 1440px in light AND dark: `admin-analytics-handoff/evidence/a3-*.png`.

A3 acceptance (spec §9): hierarchy/dimensions/tokens per the supplied contract (token table verbatim; rail/selector breakpoints; KPI stacking) ✓; no horizontal page overflow at listed widths ✓ (asserted); targets/focus/contrast per contract (44px controls, 3px ring, token palette both themes) ✓; data definitions in exports ✓; all private state clears on lock ✓; metrics unchanged by theme/viewport (pure CSS) ✓; learner suite green untouched ✓. Direct-route/Back-Forward/both-bases tested.

Known limitations / deferred by design: a global period selector is deferred (the spec's blessed initial layout — fixed labelled windows + a 31-day chart — ships instead; ADM-09's fix makes a future selector safe); cohorts/reliability/release comparisons arrive with A4/A5 data; screen-reader testing beyond automated semantics remains a manual acceptance item.

Rollback: revert the merge and the previous static dashboard returns at the same URL with the same worker contract; the SW denylist is retained under any rollback (it only ever prevents wrong fallbacks).

Hosted commit statuses: recorded after deployment below.

A3 release verification (7 September 2026): frontend-only release — no worker deploy (workers/ and shared/ untouched; push worker stays `e51c6009`, feed `e6225441`). Released commit `c69503f`; GitHub Actions concluded **success** (full browser gate incl. the 11 admin specs + Pages deploy). Live: both hosts serve the new Vite admin entry (Vercel HTML references the hashed analytics module; Pages likewise), the deployed learner sw.js carries the analytics navigateFallback denylist, and `./scripts/deploy.sh verify` reports all six PASS lines (verbatim in the run log; unchanged set).

### Pass 48 — Admin analytics A4: real instrumentation and return analysis — 8 September 2026

Work items: phase A4 of `admin-analytics-handoff/ADMIN_ANALYTICS_ENHANCEMENTS.md` (§9), per `admin-analytics-handoff/DEVELOPMENT_PLAN.md`. Findings addressed: ADM-13 (route/task/travel/PGCE successes instrumented with distinct attempt-vs-success names; unavailable coverage shown, never 0%), plus §4.3 eligibility/§4.4 cohorts/§7.4 catalogue.

Commit and branch: one commit on `admin-a4`, merged to `main`.

Behaviour/contract changes:
- `CAPABILITY_VERSION` → 2. The catalogue gains route-view and successful-persistence events and sits exactly at the 32-events-per-capability cap (session-detail and search remain under their v1 names). A capability-1 client cannot report a v2 event — validator-enforced both in unit tests and by the worker.
- Client success instrumentation, all counts-only and content-free: `view_today/schedule/tasks/pgce` on real route changes (rerender-deduped); `task_created`/`task_completed` decided against the current store before the write (create vs status→done; retries and pure edits never double-fire); `pgce_record_saved` via a central revision diff at the single `saveAdminFile` point (create/edit/undo across every tab; deletes excluded); `evidence_photo_saved` after compression and persistence both settle; `journey_session_opened`/`journey_home_opened` on the travel UI; `journey_planned` once per request identity when a real plan renders; `navigation_link_opened` on external map links; `export_prepared` after binder/evidence/.ics artifacts generate (never "completed"); `sync_outcome` once after the sync settles (reason/counts only).
- Coordinator DO: per-capability measurement-start dates (a v2 event's history begins when capability-2 clients first reported, not the v2 epoch — never backdated); weekly cohorts computed from first v2-observed week on UTC Monday boundaries with EXACT follow-on-week returns (not rolling N-day, never "returned any later date"), incomplete observation weeks withheld, no backfill; adoption eligibility denominators count only tokens whose capability can report the event.
- Dashboard: Return visits renders the cohort table — headline percentages only for complete weeks AND cohorts ≥10 tokens, small cohorts show counts without a percentage, open/forming weeks show a dash + "Not complete" — with return frequency kept as a separate labelled measure; Feature adoption takes v2 numbers per feature once real observations exist while legacy rows keep received-day labelling.

Automated checks + results: `npm run validate` AND `VERCEL=1 npm run validate` green — 124 unit and 57 browser tests. New: capability-gating contract tests; cohort/eligibility DO tests (UTC-Monday membership incl. a Sunday first-seen edge, exact-week vs rolling distinction, small and partial cohorts, per-capability start dates); 3 admin-a4 browser tests (events fire exactly once at success points with the task title proven never to leave the device; cohort table honesty for small/open/complete cohorts; per-feature v2 takeover with legacy labelling intact). Two real defects caught by the browser tests during development — a required due-date and incremental-flush poll timing — fixed in the tests.

A4 acceptance (spec §9): missing instrumentation cannot appear as 0% use ✓ (not-collected + unavailable states); attempt/success names remain distinct ✓; no private content in any outgoing payload ✓ (serialized-body sweep asserts the task title never leaves); exact-week cohort fixtures pass UTC Monday boundaries, partial weeks, old/new tokens and insufficient sample cases ✓; no fabricated historical backfill ✓ (v2 first-seen only). Long labels/mobile widths covered by the A3 overflow suite.

Rollback: reverting the app stops capability-2 collection (the worker keeps accepting both; DO data retained); the CAPABILITY_VERSION bump is backwards-safe (old readers ignore v2 features/cohorts; the aggregate's `cohorts` field is additive).

Worker versions + hosted commit statuses: recorded after deployment below.

A4 release verification (8 September 2026): workers deployed FIRST (shared/analytics-contracts + the DO changed) — push worker `f967d702-b273-4444-81f5-c36c61efd97e`, feed worker `acaf3abf-a2e6-4e2d-aebf-c96d29f64c84`. Live capability-gating proofs against production using reserved `ffffffff…` test tokens (adev rows deleted by exact name afterwards; excluded from all metrics): a capability-2 batch carrying `task_created`/`view_today` is **acked**; a capability-1 client sending `task_created` is **refused** with `unknown event task_created` before any write. Released commit `44474a1`; GitHub Actions concluded **success**. After the next cron tick the published `/stats/v2` snapshot exposes the new shape: schemaVersion 2, 32 features (`task_created` measurement `available`), and the additive `cohorts` array. `./scripts/deploy.sh verify` all six PASS (verbatim in the run log; unchanged set).

### Pass 49 — Admin analytics A5: reliability, release coverage and operational readiness — 8 September 2026

Work items: phase A5 of `admin-analytics-handoff/ADMIN_ANALYTICS_ENHANCEMENTS.md` (§9), per `admin-analytics-handoff/DEVELOPMENT_PLAN.md`. Findings addressed: ADM-17 (measured budgets; snapshots already published — reliability adds one Durable Object request per refused attempt and nothing in KV), ADM-20 (accurate retention disclosure; bounded first-seen policy adopted PROSPECTIVELY and left dormant). Completes the admin workstream: A1–A5 all released.

Commit and branch: one commit on `admin-a5`, merged to `main`.

Behaviour/contract changes:
- **Reliability (§4.5)** — the coordinator counts every v2 attempt by reason and UTC day: accepted/duplicate inside the batch transaction, and schema-rejected / oversize / rate-limited via a fire-and-forget outcome report the worker sends for attempts refused before a batch exists (aggregate reason counts only, never request samples; rows expire after 30 days). The snapshot publishes the thresholds beside the numbers (stale >30 min; alert when >2% of ≥100 attempts over the last COMPLETE UTC day were refused) and a last-accepted watermark. The dashboard's Reliability page shows three sources with their own denominators — aggregate freshness, worker ingestion acceptance, late client-reported sync outcomes carrying the "offline failures are unobserved, not zero" caveat — a per-day reason table, and an open-issues list (stale aggregate, rejection alert, unknown build coverage, incomplete data). Below the denominator minimum the rate is suppressed as "insufficient", never 0%; no green "healthy" claim exists anywhere.
- **Releases (§4.6)** — immutable build ids (git commit) with each active token attributed once to its latest observed build in the 7-day window, first-observed dates, and Unknown as a visible category. Comparisons (opens per token over the last complete 7 UTC days) appear only for builds with ≥20 eligible tokens; otherwise "Comparison unavailable (n/20)". No deltas, arrows, significance badges or causal statements. Legacy release-note markers stay labelled as not build identities.
- **Retention (§7.3 / ADM-20) — prospective and DORMANT.** Last-seen dates are now recorded (`alast:<token>`, ≤1 KV write per token per week, self-expiring after 200 days) on both ping and v2 accept. The 180-day first-seen expiry is implemented as a pure, unit-tested rule (`expiredFirstSeenTokens`) that runs only when `analytics:retention-policy` in KV holds an ISO activation date, after a 30-day grace period so active-but-old tokens cannot be swept on day one, on `adev:` rows only, at most 200 per run, and never on a partial ledger scan. **It is not enabled**: the owner's instruction to run A5 was not the explicit yes the plan requires for a destructive semantics change, so the switch stays off (activation command documented in DEPLOYMENT.md §6). Data & access states the policy, the semantics change it would cause ("ever recorded" → "recorded within retained history"), the current no-expiry truth, and per-capability collection start dates.
- **Ops** — DEPLOYMENT.md (Bearer-only auth, new KV prefixes `alast:`/`astats:latest`/`analytics:retention-policy`, DO-owned row families, dashboard/cron/retention operating notes) and AGENTS.md hard rules (never set the retention flag unasked; DO isolation; no query credential) updated in the same change. Budget note: steady-state KV writes unchanged except ≤1 last-seen write/token/week; snapshot writes skipped when unchanged.

Automated checks + results: `npm run validate` AND `VERCEL=1 npm run validate` green — 128 unit and 61 browser tests. New: DO tests (transactional reason counters incl. invalid-reason refusal, last-accepted watermark, null/insufficient rate below the minimum, latest-build attribution without double counting, first-observed dates, comparison gate at 20 eligible tokens, no phantom Unknown row); worker tests (refused attempts reported as oversize/rejected/rate-limited, accepted batches record last-seen, retention rule dormant without activation / inside grace / expires only old first-seen tokens with no last-seen / ignores garbage); 4 admin-a5 browser tests (stale aggregate is an open issue and never "healthy"; suppressed rate below the minimum with the honesty caveat; release comparison gating, Unknown visibility and absence of deltas/badges; retention copy naming the dormant policy plus collection-start dates). Screenshots: `admin-analytics-handoff/evidence/a5-*.png`.

A5 acceptance (spec §9): stale/incomplete pipelines cannot show healthy totals ✓ (issues list + stale badge; suppressed rates); counts from different sources never share an unexplained denominator ✓ (each card names its source and denominator); release comparisons suppressed until compatible/large enough ✓; retention touches only designated analytics data ✓ (rule scoped to `adev:`, gated, bounded, unit-tested — and not activated); installed analytics works at both host bases ✓ (Vite entry on both; SW denylist from A3); learner notifications/sync/PGCE flows still pass ✓ (full suite green); no production test records or subscriptions created ✓ (reserved test tokens only, deleted by exact name).

Known limitations / deferred: load-fixture tuning of rate caps and concurrency was not exercised beyond unit fixtures (current caps unchanged; the cohort is small and measured writes are far under budget); Safari/installed-PWA offline behaviour and VoiceOver remain manual acceptance items; the retention switch awaits the owner's explicit decision.

Worker versions + hosted commit statuses: recorded after deployment below.


## Audit addition — 8 September 2026 (documentation only)

Added TT-21 to `timetable-app-audit/TIMETABLE_APP_AUDIT_AND_ENHANCEMENTS.md`: move editable key-date reminder timing from Settings → My timetable to Settings → Reminders, preserving source connection controls, preferences and scheduling behaviour. Includes navigation, accessibility and regression acceptance criteria; assigned to R3 with independent delivery allowed. No application code changed.

A5 release verification (8 September 2026): workers deployed FIRST — push worker `453007ed` (A5), then two same-day fixes found by LIVE verification and each redeployed after a unit regression test: `a8611197` (the unchanged-snapshot skip compared metrics only, so new sections never republished) and `8d4617e3-93fb-4f31-97ef-b0ffd3e3b25d` (refused-attempt reports were dangling promises cancelled at response time; now `ctx.waitUntil`). Feed worker `0c8e38c8-5714-40ad-81f0-d9613bafcaed` (bundled-shared refresh only). Released commits `e74fd47` (merge) → `3e45448` → `96dc53a`; GitHub Actions concluded **success** for each. Live proofs against production: `analytics:retention-policy` confirmed **absent** in KV (404 — the retention job is dormant); a deliberately invalid capability-2 batch with a reserved `ffffffff…` token returned **400** and the published `/stats/v2` snapshot then showed `reliability.days[today].rejected = 1` with `lastCompleteDay.status = insufficient` (no rate fabricated below 100 attempts), `builds` (2 real builds) and `cohorts` present. `./scripts/deploy.sh verify` all six PASS. **Admin analytics A1–A5 complete (Passes 45–49); the only open decision is the owner's yes/no on activating first-seen retention.**


## Audit addition — cloud backups, 8 September 2026 (documentation only)

Added NF-09 to the learner audit: Google Drive app-managed encrypted backup/history/restore and iCloud Drive Files/share export/import, with an optional separate CloudKit adapter. Includes consent, archive encryption, snapshot validation, scoped retention, restore recovery, honest foreground automation limits, official provider references and acceptance tests. Updated R5 and NF-05 scope. No app implementation, account connection or upload performed.

### Pass 50 — Learner audit R1: trustworthy schedule, routing and saved work — 8 September 2026

Work items: batch R1 of `timetable-app-audit/TIMETABLE_APP_AUDIT_AND_ENHANCEMENTS.md` (§9), per `timetable-app-audit/DEVELOPMENT_PLAN.md`. Findings addressed: **TT-01, TT-02, TT-03, TT-04, TT-05, TT-06, TT-07, TT-08, TT-09, TT-10** (all ten R1 findings). TT-18 was already closed by admin A2.

Commit and branch: one commit on `audit-r1` (six work groups), merged to `main`. `shared/contracts.js` changed (plan validation at the wire), so the workers deploy FIRST.

Behaviour before → after:
- **TT-08 safe navigation** — `#/session/%E0%A4%A` threw `URI malformed` and blanked the body → `src/lib/navigationState.ts` parses hashes without throwing (typed malformed/oversized/unknown results, 512-char key bound, settings-section allowlist); an "This link could not be opened" page keeps Today/Schedule one tap away; unknown settings sections land on the index with a notice and a clean hash; an app-level `ErrorBoundary` offers a non-destructive reload; Back walks history only when an in-app predecessor exists (internal return context replaces `history.length > 1`).
- **TT-05 course clock** — Today compared device `getHours()` against course wall times (a New York device showed "Up next · in 4h45m" for a London session that was "Now") → `useCourseClock` gives one course-zone now/today (30 s tick + visibility resume); Today, Journey home (forecast hour, arrival time), demo sessions, task completion stamps (App, editor, queued notification actions) all use the course day; a compact "course time HH:MM" badge appears when the device zone differs.
- **TT-02/03/04 canonical projection** — personal commitments were missing from desktop Week/Month and from search; a Saturday-only deadline vanished; header pins silently dropped past two → `src/lib/scheduleProjection.ts` decides display/search membership once, deduplicated by owner identity, with a dedicated "Show personal events" display toggle (course filters never touch personal records); search runs over course members + personal events + task pins across all dates with a visible "All dates; display filters not applied" note; Week columns come from the union of visible timed events AND deadline pins ("Show all seven days" available); header pins show two plus an accessible "+N more" expander.
- **TT-01 desktop width** — `.week-grid` measured 322 px in a 1440 px shell → the main grid item now has an explicit width contract (`width:100%; min-width:0; justify-self:stretch`, wide pages capped at 1440 px); the calendar measures ≈1192 px and keeps ≥800 px beside the 320 px panel.
- **TT-06 Today by identity** — Today picked one current item and dropped overlapping/simultaneous events → `shared/intervals.js` `classifyNow` puts every relevant record exactly once into current/upcoming/finished/untimed; "Also now" rows list every other active event with a clash count for busy overlaps (free entries labelled, not conflicts); "day finished" ignores untimed records; "Show finished sessions" keeps past records reachable. (`assignLanesByComponent` is also in `shared/intervals.js`, ready for TT-14 in R2.)
- **TT-07 draft recovery** — a new personal-event draft got a random id and was never offered again → `src/lib/draftIndex.ts` discovers new-record drafts by profile + kind; `useDraft` offers the newest (Continue/Discard), resumes under the draft's INTENDED record id so the eventual save commits the same record, flushes pending writes on pagehide/hidden/unmount instead of cancelling them, and validates envelope shape/version before populating a form. Applied to commitment and task creation (PGCE record editors already edit existing ids).
- **TT-10 plan validation** — an 18:00–17:00 block with no date persisted → `shared/planValidation.js` enforces real dates, valid times, end > start and effort ≤ 6000 min at the editor (errors name and focus the field), and `shared/contracts.js` applies the same rules on backup import and sync ingestion; existing invalid records stay stored, flagged "Needs scheduling", never timed or deleted.
- **TT-09 completed-task copy** — a done task read "2d overdue" → "Completed" chip with the completion date and original due date; the cyclic status button is now an explicit labelled status menu (`Status for <task>`).

Stored schema/contract changes: `Filters.showPersonal` (optional, default true); wire validation for `plans` tightened (effort cap 6000, block end > start) — additive rejections for NEW impossible intervals only; draft envelopes unchanged (index reads existing keys; no migration, no deletion).

Automated checks + results: `npm run validate` AND `VERCEL=1 npm run validate` green — 136 unit tests (7 new: connected-component lanes, identity classification incl. equal starts/nesting/free entries, real-date/interval/effort rules, wire-boundary enforcement) and 70 browser tests (9 new `audit-r1`: positive desktop width with and without the panel, weekend deadline column, "+1 more" via keyboard, search parity with scope note, London-vs-New-York course clock at one instant with the zone badge, "Also now" with clash count and no duplication, new-draft recovery under the same id, malformed/unknown/settings-section links, completed-task copy + explicit status control, impossible-block rejection). Two legacy tests adapted from the removed cycle button to the status menu. Screenshots (synthetic seed, both themes): `timetable-app-audit/evidence/r1-schedule-1440-*.png`, `r1-today-390-*.png`.

R1 gate (audit §9): every reproduced issue has a corrected assertion ✓; existing suites green ✓; screenshots show a readable desktop calendar and a complete mobile day ✓; a draft advertised as saved is recoverable with the same intended identity ✓; no new record can commit an invalid block ✓.

Known limitations / deferred: remote deletion of a record mid-edit still resolves through the existing conflict flow rather than a dedicated "deleted elsewhere" outcome; the desktop week keeps the audit's per-day lane count until TT-14 (R2); no telemetry event was added or moved out of its success point.

Rollback: view changes revert independently; the safe parser, course clock and plan validation stay. Draft index is read-only over existing envelopes.

Worker versions + hosted commit statuses: recorded after deployment below.

R1 release verification (8 September 2026): workers deployed FIRST because `shared/contracts.js` now enforces plan validation on sync ingestion — push worker `a9747db8-8ccd-4bcc-9055-a075b8c1d86c`, feed worker `110921d7-cd0c-462a-8818-434e87e96d9a` (the tightened rule only rejects NEW impossible intervals; existing stored records are untouched and shown as "Needs scheduling" in the app). Released commit `3ca665c`; GitHub Actions concluded **success**. `./scripts/deploy.sh verify` all six PASS. Deployed main chunks carry the R1 surfaces — Vercel `assets/main-DKGn5bUh.js`: invalid-link page string ×1; Pages `assets/main-D7aTWsw1.js`: search scope note ×1.

### Pass 51 — Learner audit R2: journey continuity and consistent actions — 8 September 2026

Work items: batch R2 of `timetable-app-audit/TIMETABLE_APP_AUDIT_AND_ENHANCEMENTS.md` (§9), per `timetable-app-audit/DEVELOPMENT_PLAN.md`. Findings addressed: **TT-11, TT-12, TT-13, TT-14, TT-15, TT-16, TT-17** (TT-18 was closed by admin A2), plus NF-06 groundwork and one owner-reported defect (duplicate disruption warnings on a leg).

Commit and branch: one commit on `audit-r2`, merged to `main`. `shared/` changed (journey.js dedupe, course.js `journeyProvider`), so the workers deploy FIRST.

Behaviour before → after:
- **TT-11 replan when departure passes** — an open travel tab could reach "planned departure has passed" with no alternative → `useJourney` has a transition effect keyed by request identity + departure state: the FIRST transition into passed fetches exactly one leave-now alternative (abort/generation-protected), later ticks do nothing, a stale plan (>10 min) refreshes on visibility resume, and the UI distinguishes refreshing / alternative found / none found.
- **TT-12 superseded plans + negative cache** — a published leave plan lived on for up to 20 min after origin/buffer changed, and a network failure was cached for 10 min → the leave plan is cleared the instant identity changes, publication/consumption is keyed by profile + event (+ request identity check), network failures are remembered for 15 s only, and a visible Retry bypasses the cache. The reminder consumer (`useNotifications`) reads plans for the active profile only.
- **TT-13 reminder preference** — editing a commitment silently dropped `remind` → the editor carries the flag through every edit, with an explicit "Remind me" control (new events default off; busy never implies reminders).
- **TT-14 overlap lanes** — a morning clash narrowed the whole day → lanes are assigned per connected overlap component via `shared/intervals.js`; an isolated afternoon block keeps full width (asserted: full column width vs the shared chain).
- **TT-15 panel identity** — the desktop panel stored a session object → it stores a stable event key, resolves the record from current data on every render, shows "This session changed or is no longer visible" when it vanishes, closes on week change unless explicitly pinned.
- **TT-16 map states** — a pin on blank space when tiles failed → `MapState` tracks tile load/error for both maps: "Map could not load"/"Map unavailable offline" with a single Retry and the readable address, "Some map tiles unavailable" for partial loads, one screen-reader status per state change, no automatic re-requests.
- **TT-17 journey-home honesty** — "Leaving now — live route" before any plan, "turn location on" despite a usable saved origin, silent clipboard failures → the subtitle follows one journey state (choose origin / planning / live with age / estimate / unreachable / no route / external directions), saved origins are offered before any permission prompt, `CopyButton` says "Address copied" only after success and exposes selectable text with "Copy unavailable here" on failure.
- **NF-06 groundwork** — `settings.defaultOriginHome` (a "Use this origin by default" action on the journey-home page) and a course-config `journeyProvider` (`'tfl' | 'none'`, default tfl, template-validated); a `'none'` course shows an explicit external-directions state instead of a failing provider.
- **Owner report (8 Sep)**: TfL repeats the same disruption entry on a leg, so the travel tab showed each warning twice → deduplicated at the itinerary boundary; a line-status warning that restates a leg's own text is dropped.

Stored schema/contract changes: `Settings.defaultOriginHome` (optional); `CourseConfig.journeyProvider` (optional in templates, default `tfl`); `CommitmentRec.remind` unchanged in shape, now preserved. Leave-plan store keys change shape in memory only.

Automated checks + results: `npm run validate` AND `VERCEL=1 npm run validate` green — 138 unit and 76 browser tests. New: 7 `audit-r2` browser regressions (departure passing while mounted → exactly one alternative request and no flooding; failed fetch → Retry → origin change supersedes immediately; reminder flag survives an edit and toggles off explicitly; component lanes; panel closes on week change unless pinned; all-tiles-failed fallback with Retry and usable address; journey-home copy/saved-origin/default-origin/clipboard-failure) and unit tests for the course provider field and disruption dedupe. Screenshots (synthetic seed): `timetable-app-audit/evidence/r2-home-390-{light,dark}.png`, `r2-home-notiles-390-light.png`, `r2-schedule-lanes-1440-light.png`.

R2 gate (audit §9): future departure crossing works without navigation ✓; a superseded route cannot drive the current display or a local reminder ✓; a reminder-enabled commitment survives edit ✓; all-map-failure remains useful ✓; no fake live/stale/arrival/accessibility claims ✓. TT-18 tracked as satisfied by A2.

Known limitations / deferred: alternate-route selection and region-specific providers remain R6; the journey state is still expressed through the hook's status fields rather than a single discriminated-union type; background push leave alerts are unaffected (the worker never carried leave plans).

Rollback: revert view changes independently; keep the 15 s negative cache, profile-scoped plan keys and disruption dedupe.

Worker versions + hosted commit statuses: recorded after deployment below.

R2 release verification (8 September 2026): workers deployed FIRST (`shared/journey.js` dedupe and `shared/course.js` `journeyProvider` changed) — push worker `5950336d-f548-4fd6-96b7-aae4e27c2671`, feed worker `7398397d-7a7c-4f7f-8b0a-8f9cd1a83c85`. The deploy script's own regression pass caught one older assertion (phase-seven "geometry absent" expected a blank static map where the new TT-16 state honestly shows "Map could not load"); the test now accepts either and the chain was re-run. Released commit `8ebc3f5` (merge `9c609d5`); GitHub Actions run 34216421659 concluded **success**. `./scripts/deploy.sh verify` all six PASS. Deployed main chunks carry the R2 surfaces — Vercel `assets/main-DjFmXJeN.js`: clipboard fallback copy ×1; Pages `assets/main-DBNjRKnd.js`: map-failure state ×1.

### Pass 52 — Learner audit R3: UI consistency and discoverability — 8 September 2026

Work items: batch R3 of `timetable-app-audit/TIMETABLE_APP_AUDIT_AND_ENHANCEMENTS.md` (§5 styling/interaction contract, TT-19, TT-20, TT-21, the accessible List view, local Settings search), per `timetable-app-audit/DEVELOPMENT_PLAN.md`. Plus one owner-reported defect carried from R2 (a disruption still shown twice on the travel tab).

Commits and branch: `audit-r3`, merged to `main`. TT-21 landed FIRST as its own commit (`dc9c075`, "key-date reminder timing moves to Settings → Reminders") so it could have shipped alone; the wider R3 work followed as one commit; the disruption dedupe as a third. `shared/journey.js` changed, so the workers deploy FIRST.

Behaviour before → after:
- **TT-21 key-date reminders** — the editable 7/3/1-day chips sat under Settings → My timetable beneath the sheet link → they live in ONE "Key-date reminders" group under Settings → Reminders (anchor `key-date-reminders`, group name "Days before each key date", labels "7 days before / 3 days before / 1 day before", chips keep `aria-pressed`). My timetable keeps the source URL, connection status and a "Configure key-date reminders →" link that opens Reminders through the existing section navigation and focuses the heading; with no source connected Reminders explains why and offers "Connect key dates →" back to the source. `settings.keyDateReminderDays` and `toggleOffset('keyDateReminderDays', …)` are untouched: no new storage key, no migration, no permission prompt from navigation, per-profile values as before. The explanatory copy no longer promises delivery — it states foreground (and background-push, when enabled) support and points at calendar-app reminders for guarantees.
- **§5 page width contracts** — desktop main column was 640px for every page except the 1440px schedule → `.shell-main` carries a `--page-max` that each page resolves: reading surfaces (Today, Settings, detail, journey home) 720px, Tasks and PGCE lists 960px, Schedule up to 1440px. Distinct inner containers, 24px desktop gutter, 16px mobile (12px ≤340px). Verified: no horizontal overflow at 320/768/1024/1440/1920 on every destination.
- **TT-19 reach and shell semantics** — only Today had a Settings action on mobile; the bottom nav hard-coded four columns → the same labelled Settings action (`SettingsAction`) sits in every primary header (Today, Schedule, Tasks, PGCE) and hides from 1024px where the sidebar footer carries it; the nav grid uses `--nav-count` from the real destination list (a three-destination course divides evenly); a "Skip to content" link is the first Tab stop and focuses the main landmark without touching the hash router; a route change focuses the new page `h1` (never on a routine refresh, and not when the page already placed focus, e.g. a settings anchor) and a polite live region carries the page name; the sidebar profile name exposes its full text via `title`; the Settings index shows the active profile with a "Switch timetable" link to the profiles group.
- **TT-20 Tasks** — Overdue/Upcoming/Completed with an explanatory sentence → Overdue (oldest first) / Today (by due time) / Upcoming (by due date) / Completed (newest completion first, falling back to the due date without inventing a timestamp), summary chips "N outstanding · M completed", a local task search that also searches completed records, the explicit status `<select>` per row, and mentor actions with a named completion control ("Mark “…” complete"), an "Open meeting" link back to the owning record and untick-to-reopen. The cycle-status prose is gone.
- **TT-20 PGCE** — Journal linked twice, five equal-weight record-type buttons → four stable cards (Placement, Evidence & reflections, Development, Documents), each with ONE dominant `btn-primary`, one summary line and a restrained "View all →"; record types (targets, meetings, observations, lessons, audits, with counts) live behind Development's accessible `QuickMenu` (menu/menuitem roles, first item focused, arrow keys, Escape closes and returns focus, outside click closes); "Weekly reflections (n)", "Print binder & exports" and "Term stats" stay one tap away as secondary text actions. Coverage copy still says it is not a competency score; no record type lost.
- **List view** — Week/Month only → Week/Month/List: a first-class agenda over the SAME `scheduleSessions` (display filters applied, key dates woven in) and the SAME shared date anchor (the strip moves it; the scope note names it; the agenda scrolls to it; `Today` resets it). Every record is a focusable button opening the same detail; the choice persists in `settings.activeView` like the others (`ViewMode` gains `'list'`; legacy `'day'` still reads as week).
- **Settings search** — none → a local search box on the Settings index over a static entry list with synonyms (home/address, backup/export, sync/device, notifications/reminders); a result opens the real section and focuses the setting's heading via stable anchors (`profiles`, `key-dates-source`, `notices`, `specialisms`, `session-reminders`, `key-date-reminders`, `quiet-hours`, `leave-alerts`, `background-push`, `travel-mode`, `home-address`, `calendar-feed`, `calendar-export`, `data-health`, `backup`, `sync`, `theme`, `density`, `whats-new`, `install`); nothing leaves the device; an honest "Nothing matches" state.
- **Owner report (8 Sep, second)** — a station notice (e.g. "Wembley Park: No Step Free Access") attached by TfL to BOTH the leg arriving at the interchange and the leg departing it still showed twice → `validateItinerary` now deduplicates disruption text across legs (first leg keeps it; whitespace/case-insensitive). Also fixed in passing: a departure board reading "towards null" when TfL sends the literal string `"null"`.

Stored schema/contract changes: `ViewMode` accepts `'list'` (settings validation has no view enum, so no wire change); no other stored shape changed. `shared/journey.js` output changes only by dropping repeated disruption strings.

Automated checks + results: `npm run validate` AND `VERCEL=1 npm run validate` green — **139 unit and 88 browser tests**. New: 11 `audit-r3` browser regressions (TT-21 ×4: source page vs reminders group, seeded values survive navigation/reload with zero permission prompts, toggling through the existing path leaves `reminderOffsets` alone, per-profile values, no-source explanation + Connect link, keyboard reach at 320px and 1440px with focus on the heading; TT-19 ×2: Settings in one action from all four destinations at 320px, three-destination nav divides evenly + skip link + heading focus + live region; §5 widths at 1440 and no overflow 320–1920; TT-20 ×2: task groups/summary/search/named mentor controls/Open meeting, PGCE four cards with one primary each and the record menu's keyboard contract; List view sharing anchor and filters, persisting, working at 1440; Settings search synonyms + heading focus + Switch timetable) and one unit test for the cross-leg disruption dedupe. One race in a new test (asserting `activeElement` before React committed) was caught by the Vercel-layout run and made a polling assertion. Screenshots (synthetic seed, same viewport before and after): `timetable-app-audit/evidence/r3-{before,after}-{tasks,pgce,settings-reminders,settings-index,schedule-list}-{390,1440}-{light,dark}.png` (40 files).

R3 gate (plan §2): all destinations and actions still reachable ✓ (legacy phase-one/phase-five locators unchanged and green); 320–1920 matrix without overflow ✓; keyboard contract for the skip link, heading focus, menu and List ✓; before/after screenshots compared at the same viewport ✓.

Known limitations / deferred: "Open meeting" opens the meetings list rather than scrolling to the specific record; Evidence/Development list search-and-type filters stay inside the existing journal/admin sheets (unchanged); the heading-focus ring is visible after programmatic focus when the browser judges the last interaction to be keyboard (intended for keyboard users); real 200%/400% browser-zoom reflow is covered by the existing viewport proxy only; Settings search indexes settings, not help text.

Rollback: revert view changes independently; keep the shared dedupe, the anchors and `SettingsAction`.

Worker versions + hosted commit statuses: recorded after deployment below.

R3 release verification (8 September 2026): workers deployed FIRST (`shared/journey.js` cross-leg disruption dedupe) — push worker `07b24de0-8f62-490a-ade9-2bfdac060221`, feed worker `64eab726-d038-4af4-a6d6-9ca66ba9a8b7`; the deploy script's own regression pass was green. Released commit `0ac9b50` (merge `368bc16`; TT-21 commit `dc9c075`, main R3 commit `76099d4`); GitHub Actions run 34220109075 concluded **success**. `./scripts/deploy.sh verify` all six PASS. Deployed main chunks carry the R3 surfaces — Vercel `assets/main-Bz5HWof0.js`: skip link ×1, record menu ×1; Pages `assets/main-izQ3Th-Q.js`: skip link ×1, record menu ×1.

### Pass 53 — Learner audit R4: planning and local retrieval — 8 September 2026

Work items: batch R4 of `timetable-app-audit/TIMETABLE_APP_AUDIT_AND_ENHANCEMENTS.md` (§6 NF-01, NF-02, NF-03), per `timetable-app-audit/DEVELOPMENT_PLAN.md`. Plus one bug found while testing Back from a Find result.

Commit and branch: `audit-r4`, merged to `main`. `shared/` gained two runtime-neutral modules (`find.js`, `planWeek.js`), so the workers redeploy first even though neither worker imports them (the deploy script keys on the directory).

Behaviour before → after:
- **NF-01 Find anything** — search existed only on the Schedule page over sessions → a `#/find` page (opened from the Schedule search bar's "Search everything →"; safe-parsed like every route) over sessions, personal events, tasks, work-plan items, PGCE records (reflections, targets, meetings, observations, lessons, audits) and wallet file names. Index in `shared/find.js`: entries keyed by `(profileId, ownerType, ownerId)` with the owner's revision; `useFindIndex` rebuilds lazily on data change, reusing unchanged entries and dropping owners that vanished (tombstones), so a rename or delete updates results and another profile can never see them. Default scope is titles/rooms/tutors/subjects/file names; "Include my notes and captions" (a per-profile setting, default off) adds session notes, photo captions, task/event notes and PGCE record text, with a local snippet. 200 ms debounce, 50-result pages with "Show more", type chips, ↓/↑ keyboard movement from the box into results, Enter opens the canonical editor (session detail, personal-event editor, task editor, the parent task for a work-plan item, the PGCE sheet tab, the wallet). The query is kept per profile in sessionStorage so Back returns to the same view; while the timetable is still loading the page says so instead of "no results". No request ever carries query text or results.
- **NF-02 work-plan projection** — study blocks lived only inside the task editor → valid, scheduled blocks of an existing task project as read-only sessions `plan-<id>` / event key `plan:<id>` (`src/lib/planProjection.ts`) into Today, Week, Month, List, the clash check and group availability (intervals only). The block is stored ONCE in `plans`; opening a projection opens the PARENT task's editor with that block in edit mode and focus on its start time; invalid blocks stay "Needs scheduling" and off every grid; orphan blocks (parent gone) are not drawn; a done block shows but no longer counts as busy. Work-plan children can now be edited in place (same id, new revision) with a done checkbox per block; reminders never include projections.
- **NF-03 Plan week** — no planning aid → a "Plan week" sheet on Schedule over the anchored week: workload per day (timetabled / planned / personal), deadlines due that week, and suggested gaps from `shared/planWeek.js` (Monday start, visible range 08:00–20:00 configurable per profile, 30-minute minimum, 120-minute maximum with "the gap continues until…", a named 15-minute travel buffer around timetabled sessions because journey time is unknown, quiet hours excluded, free-marked personal events shown but never blocking). Every suggestion states why it fits ("free between X and Y", "ahead of ‘deadline’") and the sheet lists what was excluded; choosing one prefills either a personal study block (CommitmentSheet with times/kind) or a work-plan block for a chosen open task (TaskEditSheet with "Prefilled from Plan week") — nothing is saved until Add/Save; a block added this way gets an Undo banner on Schedule. Source changes invalidate suggestions because they are derived on render; wall-minute arithmetic keeps DST weeks honest.
- **Bug (found by the new tests)** — Back from any detail always fell back to Today: assigning `location.hash` fires `popstate` as well as `hashchange`, so R1's internal-depth counter was cancelled by our own navigation → the router now ignores the pop its own hash write triggers; Back from a session/event/task detail returns to the page that opened it (Schedule, Find, Tasks…).

Stored schema/contract changes: `Settings.findIncludeNotes`, `Settings.planRangeStart/End` (optional, per profile); no wire or worker change; `plans` records unchanged in shape (editing writes a new `at`).

Automated checks + results: `npm run validate` AND `VERCEL=1 npm run validate` green — **148 unit and 95 browser tests**. New unit: `find.test.mjs` (normalization, scope, incremental rebuild/tombstones, ranking, 10,000-entry build and query timing with 50-result paging) and `plan-week.test.mjs` (no overlap + buffer, quiet hours/range/free events, deadline reasons + workload, DST week on wall minutes). New browser (`audit-r4`, 7): block projection into List/Week/Today with in-place edit from the calendar, invalid/orphan blocks off the grid, delete removes the projection, Plan week non-overlap/reasons/estimate labels/prefill-only/undo, Find across record types with opt-in notes + keyboard + preserved query, rename/delete/profile isolation, 10,000 tasks typed without stalls, Back-from-detail regression.

R4 gate (plan §2): repaired search grew into the local index without any network path ✓; one owner per block (no second commitment; sync/backup carry `plans` as before) ✓; suggestions never overlap canonical busy time (unit-asserted) and create nothing without Save ✓; availability still shares intervals only ✓.

Known limitations / deferred: photo captions are indexed only when the opt-in is on and read from IndexedDB on toggle; Find opens PGCE records at their tab, not scrolled to the record; Plan week suggests over one week at a time and does not yet weigh effort estimates; the group-availability payload gains plan blocks as busy intervals (same contract, no text).

Rollback: revert the Find page and Plan week sheet independently; keep the router pop fix and the block projection.

Worker versions + hosted commit statuses: recorded after deployment below.

R4 release verification (8 September 2026): workers deployed FIRST (`shared/find.js` and `shared/planWeek.js` added; no worker imports them) — push worker `13b3095d-a640-4e82-a5eb-776bb1d91ea3`, feed worker `7d2046c9-78ce-4331-9a09-8e379c9febba`; the deploy script's regression pass was green. Released commit `f5fc684` (merge `1530310`); GitHub Actions run 34222320530 concluded **success**. `./scripts/deploy.sh verify` all six PASS. Deployed main chunks carry the R4 surfaces — Vercel `assets/main-DJst2hHi.js`: Find page ×2, Plan-week prefill ×1; Pages `assets/main-B_eg9GuI.js`: Find page ×2, Plan-week prefill ×1.

### Pass 54a — Learner audit R5a: recovery and evidence workflows — 8 September 2026

Work items: the account-free half of batch R5 in `timetable-app-audit/TIMETABLE_APP_AUDIT_AND_ENHANCEMENTS.md` (§6 NF-05, NF-08) plus the reviewed authenticated-encryption envelope that NF-09 (54b) will reuse, per `timetable-app-audit/DEVELOPMENT_PLAN.md`. NF-07 stays unscheduled (optional, not requested). 54b (Google Drive / iCloud) is NOT started: it needs the owner's Google OAuth client id first.

Commit and branch: `audit-r5a`, merged to `main`. `shared/` gained `backupEnvelope.js` and `contracts.js` validates the new `reviewLater` meta flag, so the workers redeploy first (the push worker validates synced meta with the same contract).

Behaviour before → after:
- **Encrypted backup envelope (reviewed subtask, not a UI patch)** — none → `shared/backupEnvelope.js`, format `my-timetable-encrypted-backup` v1: PBKDF2-HMAC-SHA-256 (600,000 iterations, 16-byte random salt, NFKC-normalised passphrase ≥ 12 chars) → AES-256-GCM (12-byte random IV, 128-bit tag) over the gzip-compressed plain backup JSON, with the format, version, iteration count, salt and compression bound as additional authenticated data so a parameter edit fails exactly like a wrong passphrase. `openBackup` never throws: `not-an-envelope` / `unsupported-version` / `malformed` / `wrong-passphrase-or-corrupt` (wrong key and corruption are deliberately one state). Documented in the module header with a fixed-parameter test vector; the passphrase is never stored, is unrelated to the sync code or any provider credential, and cannot be recovered. Runtime-neutral (browser and Node WebCrypto).
- **NF-05 data-health centre** — one line "Saved on this device… persist locally" and a vague sync line → separate truthful states under Settings → Data & devices: saved state (pending saves / last failure named), "Synced records at …" (records only, never photos or documents), photos and documents counted for the active profile as device-only, "Last backup generated …" with "only you can confirm the file was kept" (or "never on this device"), "N recoverable drafts", sources, and "Storage estimate unavailable" when the browser reports nothing.
- **NF-05 scoped backup with preview** — a single "Export backup" click → a Back up sheet: scope "Everything on this device" (default, the recovery option) or "Only ‘this timetable’" (owner ids kept exactly — a scoped backup restores back onto the same identity and never touches other timetables), a preview listing each included timetable's session records, cached events, PGCE/task records, photos and documents, an optional passphrase-sealed export, and the result "Backup generated: <file> (<size>)… only safe once you can see the file where you saved it" — never "safely backed up".
- **NF-05 restore with preview** — `window.confirm` text → a Restore sheet: an encrypted file asks for its passphrase first (a wrong one reports "either the passphrase is wrong or the file is corrupt. Nothing on this device changed"), then the validated backup is previewed (timetables, counts, what is replaced vs left alone) before Restore commits through the existing recovery journal.
- **NF-08 review queue** — Untagged filter only → a "Review queue (N)" filter over untagged records, local photos without captions and records flagged "Review later", labelled organisational; badges per entry (Untagged, N photos without a caption, N elsewhere, Review later); a "Review later" toggle on journal entries and in the session detail writing only `meta.reviewLater` on the canonical record.
- **NF-08 binder selections with exact preview** — a whole-range binder only → checkboxes select canonical records (no copies); "Binder from selection (N)" opens the binder preview restricted to those keys; the preview states exactly what prints: evidence records, photos embedded from this device, photos recorded elsewhere (labelled missing in the output — distinct from "no photos"), with caption and observer-name inclusion controls; wallet documents and home/location details are never part of the binder; clearing a selection deletes nothing and generating changes no record. `BinderPreviewSheet` moved out of `AdminSheet` and is shared by the PGCE overview and the journal.

Stored schema/contract changes: `SessionMeta.reviewLater?: boolean` (validated as boolean by `shared/contracts.js`; merges like any meta field); backup JSON unchanged; new envelope document format (above). `exportBackup(scope)` gains an optional profile filter; `backupSummary` returns structured counts.

Automated checks + results: `npm run validate` AND `VERCEL=1 npm run validate` green — **152 unit and 99 browser tests**. New unit (`backup-envelope.test.mjs`): document shape and constants, round trip, wrong passphrase / flipped byte / AAD-bound parameter edits / version / malformed / not-an-envelope all fail closed, passphrase policy, the fixed test vector and NFKC normalisation. New browser (`audit-r5a`, 4): review queue + flags + selection preview + no-mutation, data-health states with an unavailable storage estimate and a recoverable draft, scoped export (owner ids kept) → sealed envelope → wrong passphrase changes nothing → preview → restore leaving the other profile untouched, and the detail-page flag. Legacy binder test updated to the new exact-contents wording.

R5a gate (plan §2): export matches preview ✓ (the preview is computed from the very JSON that is downloaded); unavailable attachments explicit ✓; no tokens or credentials in archives (the backup allowlist is unchanged; the envelope wraps only that JSON) ✓; recovery never deletes unrelated profiles ✓.

Known limitations / deferred: the encrypted export is downloaded, not shared through the OS share sheet (that is 54b's iCloud path); the unlocked plaintext lives in memory only for the restore sheet's lifetime; NF-07 checklists unscheduled; 54b blocked on the owner-provisioned Google OAuth client id.

Rollback: revert the sheets independently; keep the envelope module, `reviewLater` validation and the scoped export.

Worker versions + hosted commit statuses: recorded after deployment below.

R5a release verification (8 September 2026): workers deployed FIRST (`shared/backupEnvelope.js` added; `shared/contracts.js` validates `reviewLater`) — push worker `fc0f1cd0-7ce2-4267-8186-7fbd2c942983`, feed worker `30951a2e-b1d1-4387-819c-d3e1a6aee039`. The deploy script's first regression pass failed without a captured test name and the immediate rerun was fully green (both validate gates had just passed 99/99 twice); recorded as a flake to watch. Released commit `04804e2` (merge `9c5d3a5`); GitHub Actions run 34224009607 concluded **success**. `./scripts/deploy.sh verify` all six PASS. The R5a surfaces are code-split: Vercel main `assets/main-CKH6n9dn.js` → envelope format in `SettingsSheet-YF0kl7ML.js`, binder selection in `JournalSheet-eAvDTxqz.js`; Pages main `assets/main-Bd9Qum69.js` → `SettingsSheet-BDsUKldQ.js` and `JournalSheet-BR-fbIuc.js`, each ×1.

### Fix — attendance percentage restored — 8 September 2026 (owner report)

Owner: "The attendance percentage is missing in the app now." Cause: P3-06 (7 September) replaced the old "N of M past sessions marked attended (P%)" line and the Term stats "P%" tile with honest-denominator counts, computing `attendedPct` in `shared/eligibility.js` but never rendering it. Fix on branch `fix-attendance-pct`: the shared summary sentence now reads "P% attended — A of E eligible completed sessions; X absent, Y unrecorded." (so Settings, the binder attendance page and any print surface carry the percentage with its denominator), Settings → Data & devices → Attendance shows the percentage as a headline with per-subject "a/t · p%", and the Term stats tile is "P%" over "attended — A of E eligible completed sessions". Unrecorded sessions are still never counted as absences. Tests: the eligibility unit test asserts `attendedPct` and the new sentence; a browser test asserts the percentage and its denominator on both surfaces. Gates green — 152 unit, 100 browser (one Vercel-layout run failed a single test that passed on the immediate rerun; second such flake today, to watch). Workers redeployed first (`shared/eligibility.js` is shared).

Attendance-percentage fix release verification (8 September 2026): the "flake" was identified — the R5a restore round-trip test read storage while the post-restore reload was in flight (execution context destroyed); it now awaits the reload and passed 8/8 repeats. Workers deployed FIRST (`shared/eligibility.js` sentence) — push worker `79c0cd48-55e8-4def-9903-2d87cfb5dd55`, feed worker `5a8fe233-72c0-4b26-bce1-b2cb6aa6bea2`; the deploy script's regression pass was green. Released commit `b127538` (merge) + test fix; GitHub Actions run 34233152680 concluded **success**. `./scripts/deploy.sh verify` all six PASS. Both main chunks (Vercel `assets/main-D79qb27y.js`, Pages `assets/main-BDPBgVYs.js`) carry the "% attended — " sentence ×1.

### Notifications — leave alerts, attendance prompts and quick answers — 9 September 2026 (owner request)

Owner: (1) no "leave now" when already at that location; (2) never ask "did you attend?" once a session is marked attended or absent; (3) quick Attended / Absent actions on that prompt.

Behaviour before → after:
- **Already there (in-app + background)** — a leave alert fired whenever start − travel ≤ head start, even standing in the building → `shared/travel-state.js` `alreadyAtDestination(loc, dest)`: a fix within 150 m of the session's building/placement (and, for a stamped fix, no older than 3 h) suppresses the alert without marking it notified, so it still fires if the person leaves again. The in-app loop uses the live device fix; the push worker applies the same rule to its cached last-app-open location. Settings copy says so.
- **Already answered (in-app + background)** — the in-app prompt already skipped marked sessions; the push worker could not know → the app reports the keys of today's answered sessions (keys only, no values or notes) to a new worker endpoint `/attendance` whenever that set changes (debounced, only when push and prompts are on); the worker stores them on the subscription record (pruned after two days, kept across re-subscribes) and stays silent for them. A tap on a prompt for a session answered meanwhile opens the session instead of asking again.
- **Quick answers everywhere** — notification action buttons existed (✓ Attended / ✗ Absent on `att-` prompts) but iOS web push shows none → the prompt now carries `kind: 'attendance'` end to end (in-app `showReminder`, worker payload, service-worker `actionsFor`, queued opens); tapping the notification body opens an `AttendancePromptSheet` ("Did you attend X?" with ✓ Attended, ✗ Absent, Open session, Not now) that writes exactly what the buttons write, and Today shows an `AttendancePromptCard` with the same two answers for the most recently ended unanswered real session within 30 minutes (only while prompts are on). The notification body now says "✓ Attended or ✗ Absent — or tap to answer in the app".

Contracts: `NotificationData.kind` gains `'attendance'` (`shared/actions.d.ts`); new push-worker route `POST /attendance { endpoint, keys[] }` (rate-limited like `/location`, 400 on bad input, 404 unknown subscription); subscription records gain `marks`.

Checks: `npm run validate` AND `VERCEL=1 npm run validate` green — 153 unit, 104 browser. New unit: `alreadyAtDestination` (near/far/stale/missing). New browser (`notifications-quick`, 4): leave alert fires 5 km away and stays silent at Bedford Way (real geolocation emulation, 30-second tick after the fix arrives); prompt fires for an unanswered session; an absent-marked session is not asked and Today's card writes ✓ Attended; a queued "open" with kind attendance opens the sheet, ✗ Absent writes the record, and the same tap for an answered session opens the detail instead. Workers deploy first (`workers/push/worker.js`, `shared/`).

Known limits: the worker's "already there" rule depends on the last reported app-open location (background alerts already say when that fix is old); the worker learns answers only while the app is open with push on — an answer given entirely from a notification button with the app closed is applied on next launch and reported then.

Notifications change release verification (9 September 2026): workers deployed FIRST (`workers/push/worker.js` already-there rule, `/attendance` route, prompt kind; `shared/travel-state.js`, `shared/actions.d.ts`) — push worker `78ed5cb2-f9a0-47ab-81f7-30c75e47b7eb`, feed worker `c624c522-ed5c-46d2-a82c-048f81af86e0`; the deploy script's regression pass was green. First CI run 34370284478 on merge `7fcf135` **failed**: the new attendance-prompt test reads the device clock and the runner is UTC (16:40 London = 15:40 there, before the prompt window) — the spec now pins `timezoneId: 'Europe/London'`, verified locally under `TZ=UTC`; run 34370671960 concluded **success**. `./scripts/deploy.sh verify` all six PASS. Both hosts serve the new main chunk (Vercel `assets/main-B0HWAWPX.js`, Pages `assets/main-CSaKe5Br.js`: prompt copy ×4) and the updated `sw-push.js` (attendance kind ×1). Live probe: `POST /attendance` with an unknown endpoint → 404 `unknown subscription`, as designed.


### Further project audit — 10 September 2026 (documentation only)

Added `project-audit-2026-09-10/PROJECT_AUDIT_2026-09-10.md` and synthetic UI/test evidence for Claude. Baseline d933f44; isolated snapshot build passed, 153 unit tests and 104 existing browser tests passed, plus three audit browser checks. Seven further reliability/contract findings, six UI refinements and three feature proposals include implementation contracts and acceptance tests. Existing key-date reminder relocation is confirmed present; cloud backup remains in its existing backlog. The handoff requires completed enhancement specs and supporting assets to move into `archive/enhancements/` after implementation, with active work and runtime files preserved. No application code, production services or existing handoff files changed; no archive move performed.


### Major capability proposals — 10 September 2026 (documentation only)

Expanded the fresh project audit with MF-01–07: adaptive workload planning, assignment/project workspace, placement and mentor review portal, connected calendar/import inbox, course knowledge/revision workspace, collaborative coursework and change-impact replanning. Retained all earlier minor features, defects and UI guidance. Each proposal states current-feature reuse, first release, UX, data boundaries and acceptance gates; recommended order starts assignments → adaptive planning → placement/mentor review. These are proposals, not implementation approval. Existing completion/archive instructions remain the final section. No app code changed.

### Sync — two open devices converge on their own — 10 September 2026 (owner report)

Owner: "The device to device sync is not working correctly. The two devices are not in sync." Investigation (no data from the devices themselves): the merge and revision protocol are sound (existing two-device API test still green), but four behaviours made two open devices drift and look out of sync — (1) no periodic pull: a device only fetched the other's state on start, on a hidden→visible transition (≥ 60 s apart), after its own edit, or when a sheet closed, so two devices left open never converged; (2) no request timeout: sync runs through a queue and a Web Lock, so one stalled mobile request blocked every later sync on that device until a restart; (3) every sheet/detail close ran a full exchange (GET + POST) against a 20/min per-IP server limit, and a 429 surfaced only as "rejected" with no retry; (4) sync state was nearly invisible.

Fix (branch `sync-robustness`, client only — no worker or shared change): `src/lib/sync.ts` gains `syncFetch` (15 s AbortController timeout on every sync request; a 429 becomes `SyncBusyError` with a 60 s default back-off — `Retry-After` is not readable cross-origin — and its own honest status "Sync server busy — the app will retry shortly."); `App.tsx` routes every pull through one retry-aware `pullSyncNow(reason)`: on start, every 3 minutes while the tab is visible, when the tab is shown, focused or back online, after an edit (2 s debounce) and when a sheet/detail closes — throttled (20 s for event-driven pulls, 60 s for the timer, none for start/edit/retry), skipped while offline, and a busy server schedules exactly one retry. Settings → Data & devices → Data health gains a "Sync status" row (current status, its time, and the cadence). `deviceSettings` (theme, reminders, prompts, quiet hours, push/location, usage ping, active view, group credentials) stay device-local by design.

Tests: `tests/sync-devices.spec.ts` (2) — two real app instances on a mocked revision-checked sync object: device A marks Maths 1 attended via the detail page, device B (idle, open on Schedule) receives it within one 3-minute background pull and its detail shows ✓ Attended pressed; a 429 on B's next edit is backed off and retried without user action; A picks up B's change on its own timer; the health row names the status and cadence. Gates green — 153 unit, 106 browser.

Environment note: iCloud Drive had dropped " 2." conflict copies of source and test files into the repo, several of them dataless placeholders whose reads block indefinitely — the cause of two 10-minute `tsc` hangs today (and it would have hung Playwright's spec discovery next). Byte-identical copies were removed; the rest were MOVED, not deleted, to `../timetable-pwa-icloud-conflicts/` for the owner to inspect. None had been committed.

Known limits: the worker's per-IP limit is unchanged (20/min); with the new throttles two devices on one network stay far below it. Photos and documents still never sync (by design).

Sync fix release verification (10 September 2026): client-only change, no worker deploy. Released commit `c97a6db` (merge `5bd3375`); GitHub Actions run 34514240555 concluded **success**. `./scripts/deploy.sh verify` all six PASS. Both main chunks carry the new busy-server status string. Both validate gates green before release (153 unit, 106 browser); the two-device test passed 8/8 repeats. A foreign untracked folder `project-audit-2026-09-10/` that appeared during the session was kept out of the commit.

### Pass 54b — Learner audit R5b: Google Drive and iCloud Drive backups — 10 September 2026

Work items: NF-09 from `timetable-app-audit/TIMETABLE_APP_AUDIT_AND_ENHANCEMENTS.md` (§6) per `timetable-app-audit/DEVELOPMENT_PLAN.md` R5 (54b), started on the owner's instruction after they authenticated `gcloud` as abdulwsaqib@gmail.com.

Google Cloud (done via `gcloud`, recorded in `GOOGLE_DRIVE_SETUP.md`): project `my-timetable-backups-260910` (number 36473996504) created; `drive.googleapis.com` enabled. The OAuth consent screen and the Web client id cannot be created from the CLI for a personal account (the `iap oauth-brands` route is organisation-only and was refused); the runbook gives the exact Console steps, scope (`drive.appdata` only), the four authorised JavaScript origins and where the resulting **client id** goes (`VITE_GOOGLE_OAUTH_CLIENT_ID` on Vercel and as a GitHub Actions variable; `.env.example` added). **Until that id is set, the Google card is deployed in its explanatory disabled state** — exactly as the plan required; no credentials were invented.

Commit and branch: `r5b-cloud-backups`, merged to `main`. `shared/merge.js` adds `cloudBackups` to the device-local settings list, so the workers redeploy first.

Behaviour before → after:
- **Provider adapter contract** — none → `src/lib/cloudBackup/types.ts`: `authorize / listBackups / upload / download / deleteOwnedBackup / disconnect` plus explicit capabilities (`list, upload, download, deleteOwned, fileExport, fileImport`); every adapter uses the SAME archive bytes: the scoped plain backup, minus device credentials (`groupToken`, `groupMemberId`, `cloudBackups`), sealed in the reviewed envelope (54a). `snapshot.ts` builds it with an opaque id, format version, installation id, scope id, creation time, ciphertext size, SHA-256, counts, device label and build id; size failures name themselves before any transfer.
- **Google Drive** (`google.ts`) — Google Identity Services token model (`initTokenClient`, script loaded only after Connect), `drive.appdata` scope only, snapshots in `appDataFolder` under `mt-<id>-<timestamp>.json` with metadata as `appProperties`. The access token lives in module memory (never storage, logs, backups, SW or URLs); 401 → "Reconnect to back up" (no automatic popup); missing scope, cancellation and missing configuration are distinct usable states. Upload → read back → SHA-256 compare → `verified=1`; a mismatch deletes the new object and leaves the previous backup untouched. Retention (opt-in) deletes only THIS installation's snapshots beyond the newest ten; Disconnect revokes/forgets the token and never deletes snapshots; other devices' snapshots list separately and stay restorable (only this device's offer Delete).
- **iCloud Drive** (`icloud.ts`) — the honest file path: the sealed archive goes to the OS share sheet via `navigator.share({files})` when `canShare` supports files ("File handed to the share sheet — choose Save to Files → iCloud Drive. The app cannot confirm it was saved there."), otherwise a download with instructions; a dismissed share sheet is reported as "nothing was saved"; "Last file export" is labelled "not a confirmed cloud backup"; restore is a file picker into the same preview flow. CloudKit stays a documented future option.
- **UI** — Settings → Data & devices → **Cloud backups** (anchor `cloud-backups`, searchable): session passphrase (unlocked in memory only, `session.ts`), scope chips with a contents preview and "N photos recorded on another device cannot be included", the Google card (state, last verified backup, Connect/Back up now/Refresh/Disconnect, progress Preparing → Encrypting → Uploading → Verifying → Complete with a live region, snapshot list with date/device/format/scope/counts/size/verification/build, Restore… and Delete, restore history, the automatic-mode and retention toggles), and the iCloud card. Restore reuses 54a's `RestoreSheet` (passphrase pre-unlocked when known; wrong passphrase changes nothing).
- **Automatic mode** (`useAutoCloudBackup`) — "Back up when I use the app": after a data change, debounced 30 s, at most once per 24 h, only while online, signed in AND unlocked in this session; single-flight; never a popup; a reload forgets token and key so nothing runs until the person reconnects. Copy says nothing runs with the app closed.
- **Restore keeps this device's own switches** — a restore used to replace a profile's settings wholesale (flipping push/location/theme flags and wiping cloud bookkeeping) → `restoreBackup` now carries the device-local settings over from the local profile of the same id, as sync already did.

Stored schema/contract changes: `Settings.cloudBackups` (device-local, never synced, never in archives); new localStorage key `timetable.installation.v1`; dev/test override `timetable.dev.google-client-id`; build variable `VITE_GOOGLE_OAUTH_CLIENT_ID`. No worker route changes.

Automated checks + results: `npm run validate` AND `VERCEL=1 npm run validate` green — **153 unit and 113 browser tests**. New browser (`cloud-backups`, 7, GIS and the Drive API stubbed with synthetic content): not-configured state; sign-in cancelled and missing scope with no token in storage; back up → verify → list with the archive proven (decrypted in the test) to hold the note and profile but not the device token; verification failure discarding the object and leaving "last backup" unchanged; 401 → Reconnect; disconnect keeping snapshots and revoking once; restore through the preview and recovery journal with restore history, then a wrong passphrase after reload changing nothing; retention pruning 12 → 10 of this installation while two foreign snapshots stay; iCloud download fallback with honest wording and a restore from that file; automatic mode uploading once for two changes and not at all after a reload. NF-09 acceptance 1, 2, 4, 5, 6, 7 covered by tests; 3 (real iOS Files/share) and 8 (both origins with a real Google account) are **owner verification steps after the client id is configured** — recorded as such, not as done.

Known limitations / deferred: no real Google end-to-end run yet (needs the Console-created client id); Testing-mode consent screens expire tokens weekly ("Reconnect to back up"); the browser cannot verify an iCloud save; CloudKit adapter not built; automatic backups are foreground-only and session-bound by design.

Rollback: the section, hook and adapters are additive; the restore device-settings carry-over stands on its own.

Worker versions + hosted commit statuses: recorded after deployment below.

R5b release verification (10 September 2026): workers deployed FIRST (`shared/merge.js` device-settings list) — push worker `319c7f21-2282-4257-b1c6-4b4def1b779d`, feed worker `a4937796-82f7-4636-b777-53dba2129f1d`; the deploy script's regression pass was green. Released commit `74dd34e` (merge `e41a90c`); GitHub Actions run 34516363312 concluded **success** (the workflow now passes `VITE_GOOGLE_OAUTH_CLIENT_ID` from repository variables; unset today, so the Google card is live in its disabled, explanatory state). `./scripts/deploy.sh verify` all six PASS. The Cloud backups section ships inside the code-split Settings chunk on both hosts.


### Visual project audit — 10 September 2026 (documentation/design only)

Added `visual-ui-audit-2026-09-10/VISUAL_UI_AUDIT.md` with 15 matched current/proposed UI comparisons, a browsable gallery, inspectable HTML/CSS concepts, a dark-theme example and implementation/acceptance requirements. Reviewed learner, travel/home, settings and admin overview, with source/regression coverage across the project. Baseline 74de8bf plus existing modified config/untracked cloud-backup work preserved. Snapshot unit tests: 153 pass. Normal build: TS2322 in in-progress cloudBackup/snapshot.ts (documented, not changed). A Vite-only visual bundle rendered successfully; 106 existing plus 2 audit browser tests passed against it; this is explicitly not a passing production build. Readability measurements include 11–12px information, small controls and 3.45:1 sampled overdue text contrast. Existing FA issues and major MF proposals remain linked. Archive instructions retain runtime/active work. No application source or production state changed.

R5b configuration (10 September 2026): the owner created the OAuth client in the Console and supplied client id `36473996504-3b6j3dasv2aj23lloo7j5kh8l6oli770.apps.googleusercontent.com` (project `my-timetable-backups-260910`). It is now the built-in default in `src/lib/config.ts` (public, origin-restricted identifier; `VITE_GOOGLE_OAUTH_CLIENT_ID` still overrides it), the GitHub Actions repository variable of that name is set, and `.env.local` (git-ignored) carries it for local dev. The dev/test override accepts `off` to simulate an unconfigured build (the not-configured test uses it). Gates green — 153 unit, 113 browser.

R5b live smoke (10 September 2026, after the client id landed): on `https://pgce-timetable.vercel.app` Connect is enabled, `accounts.google.com/gsi/client` loads (200) and the Google sign-in popup opens normally (no sign-in performed). On `https://abdulwsaqib1996.github.io` the popup shows Google's "Access blocked: Authorization Error … register the JavaScript origin" — the origin `https://abdulwsaqib1996.github.io` is not yet in the OAuth client's authorised JavaScript origins (owner Console step; nothing else needed). Real round trip (sign-in, back up, restore) on both origins remains the owner's verification.

### Visual UI audit — development plan committed (10 September 2026)

`visual-ui-audit-2026-09-10/DEVELOPMENT_PLAN.md` sequences the audit (V-01…V-10, FA-01…07 carry-forwards) as batches V0–V5 → Passes 56–61, gated on a functional preservation register (owner instruction: nothing lost — map, TfL route/departures/disruptions, origin picker, every control), with two named relocations (backup nudge, attendance analysis) each requiring a navigation test. The audit package and the earlier project audit are committed alongside it.

### Pass 56 — Visual UI audit V0: carry-forward defects FA-01…07 and the typed cloud projection — 10 September 2026

Work items: batch V0 of `visual-ui-audit-2026-09-10/DEVELOPMENT_PLAN.md` — the seven source-rechecked defects FA-01…FA-07 from `project-audit-2026-09-10/PROJECT_AUDIT_2026-09-10.md` plus B-01 done properly. Branch `visual-v0`; `workers/push/worker.js` changed, so the workers deploy first.

Behaviour before → after:
- **B-01** — the cloud snapshot stripped settings through an unchecked `as unknown as` cast → `stripSettingsForCloud(settings: Settings): Settings` destructures away only `groupToken`, `groupMemberId`, `cloudBackups`; every required key survives by type.
- **FA-01 acknowledge only stored reports** — `reportAttendanceMarks` awaited `fetch` without checking `ok`, so a 400/404/429/500 became a local "reported" marker → it returns a typed result (`accepted` only for a 2xx `{ok:true}`, `unavailable` when there is no worker/subscription — bounded 3 s wait for `serviceWorker.ready`, which never settles when no worker will control the page — `retryable` for 429/5xx/network/timeout with `Retry-After` honoured, `configuration` for 400/404, `stale` for 409 with the server revision). Only `accepted` writes an acknowledgement; every other outcome leaves a bounded pending state; the local answer itself is never touched.
- **FA-02 identity-scoped acknowledgement** — one device-wide `{day, keys}` marker → acknowledgements keyed by `server origin | SHA-256 endpoint fingerprint (never the raw endpoint) | profile id | course day | protocol v2`, kept to the 20 most recent; the reporting effect re-runs on profile switch, course-day rollover (course clock), server change, `online` and foreground resume; one in-flight report per identity; a late reply for a superseded identity is ignored; failures retry with exponential backoff (≤3, capped at 5 min). Settings → Reminders shows an unconfirmed report ("…not been confirmed by the push server yet") instead of implying delivery.
- **FA-03 reversible suppression (worker)** — `/attendance` unioned keys into a flat `record.marks` for two days, so a cleared answer stayed suppressed → the request is an authoritative snapshot `{v:2, profileId, day, rev, keys}` (keys must start with the day; ≤300); the worker keeps `record.attendance[profileId][day] = {rev, keys}`, rejects a stale revision with 409 (`{rev}`), skips the KV write when unchanged, prunes days older than two days on every write, and migrates legacy `marks` into the subscription's own profile by each key's date prefix (or expires them when no profile is known). The cron gate reads `attendanceMarksFor(record, config.profileId, today)`. Already-delivered prompts stay suppressed by the existing `sent:` keys, so clearing an answer never causes a second notification for the same session.
- **FA-04 course clock in reminders** — the 30-second loop read the device clock (`new Date().getHours()`, `localTodayISO()`), so a New York device with a London course never fired "Did you attend?" at the right moment → sessions, leave alerts, attendance windows, key-date days AND quiet hours now use course wall time from `utcToZonedParts(now, courseZone())`, exactly as Today does; Settings says quiet hours are in course time and names the zone. The London-pinned tests stay and a `America/New_York` describe block adds the same instant plus a quiet-hours case (22:10 London = 17:10 New York → quiet).
- **FA-05 shared-photo inbox** — `getAndClearSharedPhotos` emptied the IndexedDB queue before any write, and a missing target or a failed compress/attach lost the intake → `listSharedPhotos`/`removeSharedPhoto` make the queue a durable inbox; `SharedPhotosSheet` opens whenever items are pending (share landing or next launch), defaults to the current/most recently started session on the course clock over the last seven days (offered, never assumed), attaches each item and removes it from the inbox only after its write succeeded, keeps failures with Retry, reconciles the session's photo count from the files actually stored, and Discard is an explicit confirmed action. "Later" keeps everything across reloads.
- **FA-06 backup coverage per timetable** — one `lastBackupAt` for the whole device meant backing up A silenced the nudge while B had never been included → `markBackedUp({profiles, all, kind})` records a bounded generation history (file and cloud); `backupCoverage()` derives each profile's last inclusion (a legacy scope-less timestamp reads "scope unknown"); Data & devices lists "Backup coverage by timetable"; the nudge (re-evaluated on every generation) shows while any profile is uncovered for 30 days and a new profile is never covered by an earlier export. BackupSheet, the Today nudge, Google Drive and iCloud exports all record their exact scope.
- **FA-07 restore impact plan** — the preview said "replaced" for everything → `restoreImpact()` computes, per profile, whether each section (session records, PGCE/tasks, cached timetable, change history) is replaced, replaced with an explicitly empty set, or absent from the file and kept; attachments incoming vs already present; whether this device has edits newer than the backup's creation time; unrelated profiles kept; backup time and version. `restoreBackup` re-validates the plan's shape inside the recovery lock and refuses if the device changed meanwhile. Non-destructive semantics unchanged.

Stored/contract changes: push route `/attendance` request v2 (profile/day/rev snapshot; v1 bodies → 400); subscription records gain `attendance` (legacy `marks` migrated then removed); localStorage `timetable.marks-ack.v2` / `timetable.marks-pending.v2` replace `timetable.marks-reported.v1`; `timetable.backup.v1` gains `history`; `Backup.exportedAt` typed.

Automated checks + results: `npm run validate` AND `VERCEL=1 npm run validate` green — **156 unit and 120 browser tests**. New unit (push-worker): snapshot apply/clear/stale/unrelated-profile/expiry, legacy migration, and the endpoint contract (400 for v1 or off-day keys, 404 unknown subscription, 200 → migrated, unchanged → `skipped` without a write, 409 stale with the server rev). New browser (`v0-carry-forwards`, 5): accepted-only acknowledgement with fingerprinted identity and each failure kind pending, then retry and recovery; no-subscription → unavailable with the Settings notice and nothing acknowledged; the shared-photo inbox end to end (defaults, Later survives reload, Discard confirmation, attach → stored files → reconciled count → inbox empty); coverage per timetable with a legacy timestamp, scoped then full export, and the nudge following the uncovered profile; the restore impact preview (replaced / replaced-with-empty / kept-absent, newer local edits, new profile, unrelated kept, creation time) matching the resulting records. `notifications-quick` gains the New York block.

Known limits: the shared inbox cannot make "attach + count" one transaction (count is reconciled from stored files after each write); an answer given from a notification button with the app closed is reported on next launch; FA-03's legacy migration assumes the subscription's single profile.


### PGCE/QTS student-experience audit — 10 September 2026 (proposals only)

Added `archive/enhancements/2026-09-11-pgce-student-experience/PGCE_QTS_STUDENT_EXPERIENCE_AUDIT.md`, a separate student-focused handoff with nine connected capabilities, 15 mobile/desktop mockups and a linked gallery. Source baseline d58a91f with active V0 work preserved. Checked current DfE distinctions between academic PGCE, QTS, training curriculum and provider assessment; proposes route-aware programme planning, lesson/practice cycles, mentor preparation, topic learning, academic projects, curated evidence, experience logs, workload and review/transition packs. Reuses existing records and refines MF proposals; does not replace V0–V5. HTML render/link checks passed and mobile 320/390px plus desktop 1440px overflow checks were clear. No current application build/test result is claimed; no app code or production service changed. Completion instructions archive finished handoff assets only after implementation.

### Pass 57 — Visual UI audit V1: foundations (tokens, type, 44px controls, icons, contrast, nudge relocation) — 10 September 2026

Work items: batch V1 of `visual-ui-audit-2026-09-10/DEVELOPMENT_PLAN.md` — V-01 (dense 11–12px text), V-02 (overdue text contrast 3.45:1), V-03 (controls far under 44px), V-05 (emoji-only controls), V-04 (routine backup prompt above every heading). Branch `visual-v1`, client only.

Behaviour before → after:
- **Tokens (V-02, §15)** — hard-coded pinks/oranges/greens in ~30 rules (`#e64980`, `#f08c00`, `#37b24d`…; `.workload-line.heavy` measured 3.45:1) → semantic pairs on `:root` and both dark blocks: supporting text `#526078`, saved `#116653/#e6f4f0`, attention `#865000/#fff3d6`, error `#9f1d1d/#fdecea`, development `#7041a2/#f2ebff`, information `#1e5f98/#e7f1fc`; dark set `#121b29/#1d2a3d/#edf2fb/#b1bfd2` with matching dark status pairs. Every legacy literal is mapped (overdue/conflict/absent → error; approaching deadline/optional/warnings → attention; attended/live/ok → saved). `tests/unit/tokens.test.mjs` computes WCAG contrast from the actual CSS for 15 pairs in light and both dark blocks (all ≥ 4.5:1), checks the dark blocks agree, and fails if any legacy literal returns.
- **Type (V-01)** — page title 26/32 700; section heading 18/24; supporting text 14/20 (`.filter-hint`, hints, health rows, toggles, field labels, task workload line, session-card meta); 12–13px only for named metadata (`.change-meta` 13, badges 12, chips-small 13, kicker labels 13, week-strip day names 12, room chips 12); nav labels 13; inputs 16.
- **Controls (V-03)** — `--control-min: 44px` applied to icon buttons (36 → 44), segmented tabs (27 → 44), filter/plan-week buttons, chips (incl. Teachers' Standards), secondary/ghost/primary buttons, text-link actions (Copy address ~15 → 44), status selects, inputs, Back links, the day-heading add button and the deadline strip. `tests/v1-foundations.spec.ts` walks Today, Schedule, Tasks, PGCE, Settings, Data & devices, Reminders, Travel and Find at 390px and fails on any visible control under 44px, any ordinary text under 14px (outside the metadata allowlist) or any text under 12px.
- **Icons (V-05)** — `✕`/`✎` glyph buttons across 23 components and the `＋` FAB → `IconClose`/`IconEdit`/`IconPlus` line icons (existing names kept); emoji controls → text or icon + text: placements toggle "🏫" → school icon + visible "Placements", "＋ Add task/subtask/block" → plus icon + "Add …", "📊 Term stats" → chart icon, "🖨" → print icon, "🎓 Course setup", theme "Light/Dark", travel "Walking/Public transport/Driving". Badge emoji (📌 key date, 👤 personal) stay for V2. A test asserts no emoji-only control exists on the primary screens.
- **Backup prompt (V-04)** — a banner above every page heading → an attention item (`src/lib/attention.ts`): a dot on the Settings action (name "Settings — 1 item needs attention") and the sidebar Settings entry, a line on the Settings index linking to Data & devices, and the real prompt in the Backup section with "remind me in a week" (snooze). It clears only when coverage changes (any generated backup) or on snooze — never by opening a screen. Save failures stay global.

Automated checks + results: `npm run validate` AND `VERCEL=1 npm run validate` green — **161 unit and 124 browser tests**. Legacy tests updated only for renamed accessible names ("Add task/subtask/block", "Term stats", "Course setup") and the nudge's new home. Both hosting bases rebuilt; before/after captures reuse the audit's `evidence/before-*.png` against `visual-ui-audit-2026-09-10/evidence/v1-*.png`.

Known limits: V1 changes shared rules only; per-screen hierarchy (Today hero, task cards, session detail sections) is V2; badges keep their emoji until V2's per-screen pass; the measurement test covers 390px (the matrix at 320–1440 is V5).

Released 10 September 2026: merge `debeb5e`, CI run 34521503742 success, `deploy.sh verify` six PASS. No worker change.

### Pass 58 — Cloud backup providers removed (owner instruction) — 10 September 2026

Work items: the owner asked (10 Sep) to "remove the Google Cloud and iCloud backup options and keep just a standard backup save option and restore". Branch `remove-cloud-backups`, client only; no `shared/` or worker change.

Behaviour before → after:
- **Settings → Data & devices** had a "Cloud backups" section (session passphrase, scope preview, Google Drive appdata card with connect/list/restore/retention/auto-backup, iCloud file card) → the section is gone. **Backup** (encrypted file Back up sheet with scope + preview + passphrase, Restore sheet with unlock + impact preview) and **Sync between devices** are unchanged and are the only data-movement paths.
- **Code removed:** `src/lib/cloudBackup/` (types, snapshot, google, icloud, installation, session), `src/components/CloudBackupsSection.tsx`, `src/hooks/useAutoCloudBackup.ts`, `CloudBackupSettings` + `settings.cloudBackups`, the Google client id and `googleClientId()` in `src/lib/config.ts`, the R5b CSS block, `tests/cloud-backups.spec.ts`, `GOOGLE_DRIVE_SETUP.md`, `.env.example` (its only entry was the Google id; the `.gitignore` exception went with it) and the `VITE_GOOGLE_OAUTH_CLIENT_ID` build env in `.github/workflows/deploy.yml`. The GitHub Actions repository variable of the same name was deleted. No Google script or endpoint is referenced anywhere in the app.
- **Compatibility:** a settings row written before this pass may still carry a `cloudBackups` object and backup history entries with `kind: 'cloud'`; both are ignored (the coverage row no longer labels them "(cloud)"). `shared/merge.js` keeps `cloudBackups` in its device-local list so an old device's row never leaks through sync — leaving it avoids a worker redeploy for a no-op.
- **Docs:** the visual audit plan's preservation register marks Cloud backups as removed and the remaining batches renumber to V2–V5 → Passes 59–62; the app-audit plan records NF-09 as withdrawn (do not rebuild without a new explicit go-ahead).

Automated checks + results: `tests/backup-only.spec.ts` (Data & devices shows Backup + Sync, no cloud anchor/heading/copy, old `kind:'cloud'` history tolerated, no `accounts.google.com`/`googleapis.com` request, Settings search no longer matches "google drive"). `npm run validate` AND `VERCEL=1 npm run validate` green — **161 unit and 118 browser tests** (six cloud tests removed, one added). Both hosting bases rebuilt.

Known limits: the Google Cloud project `my-timetable-backups-260910` and its OAuth client still exist on the owner's account (harmless; delete from the Console if unwanted). Any Vercel environment variable `VITE_GOOGLE_OAUTH_CLIENT_ID` is now unused. A local `.env.local` holding the id is git-ignored and inert.

Released 10 September 2026: merge `89c6eaf`, CI run 34522377654 success, `deploy.sh verify` six PASS. No worker change.

### Pass 59 — Visual UI audit V2: daily use (Today, Schedule mobile + desktop, Tasks, Session detail) — 10 September 2026

Work items: batch V2 of `visual-ui-audit-2026-09-10/DEVELOPMENT_PLAN.md` — V-01 (dense text on the daily screens), V-06 (detail structure), V-09 (desktop overlaps), V-10 (one primary action). Branch `visual-v2`, client only; no `shared/` or worker change. Preservation register §1 checked row by row for the four screens: every control listed there still exists (Directions stays a one-tap secondary hero action; search, Placements, Filters, Plan week, add-event, Pin, Review later, photos, standards, Copy address, map and TfL surfaces untouched).

Behaviour before → after:
- **Today** — the hero appended the subject to the time line and showed a raw title → a labelled top row (clock icon + "Up next / Now" label, kind badge from canonical flags via `sessionKindLabel`), the sheet title split into heading + subtitle with the separator kept visually hidden so the accessible name and copied text remain the full title (`splitTitle`), a "When and where" fact list with clock/pin icons, then the same `Session details` + `Directions ↗` actions. Later sessions are cards with start/end column, title, pin-labelled place and book-labelled kind; "Rest of today" gains a "View day →" link; breaks show a clock icon. **State contract:** before first (Up next), current (Now), overlapping ("Also now" cards), after last (an accent card "That's the day done" with the next-day line and `See the week` / `See deadlines` — or the promoted Journey-home row), empty day (empty state with the same two actions). The attendance quick-answer card stays above the finished state. Journey home is an explicit icon row; the FAB clearance is 72px so it never covers the last row.
- **Schedule (mobile)** — strip buttons now carry a full spoken date (+ ", today" / ", nothing scheduled"), `aria-current="date"` for today, a roving tab stop on the selected day and Left/Right/Home/End keys (Right from Sunday crosses into the next week; focus follows the selection). The day heading gains a summary line ("3 sessions · 6h scheduled" — a sum of real durations, not a workload estimate). Cards use the V2 hierarchy (16px start, 13px end, 16px title, pin place line, kind line, state badges without emoji). Secondary functions on narrow screens live in a labelled **More** menu (`Plan week`, `Search everything`, `Add a personal event`); Placements and Filters stay visible; wide screens keep every button.
- **Schedule (desktop)** — hour height 56 → 68px comfortable (56px again under Appearance → Density: compact, read live from `data-density`), event text 11 → 13px with time ranges and a place/kind line when the block is tall enough, day heads 14px. **Overlap groups (V-09):** the grid measures its own width (ResizeObserver); when a connected overlap component's lanes would be narrower than `MIN_LANE_PX` (100px) it renders one `.week-group` button spanning the component's real start–end with an exact count ("3 parallel sessions" when all share a moment, "N overlapping sessions" for a chain) and every title; the button opens a dialog listing every record as a card, each opening its own detail/panel. Lanes above 100px stay exactly as TT-14 left them (positions unchanged). A colour key lists the canonical subjects visible that week plus Self study / Placement — colour follows subject data, never a title guess. Share-week and prev/next use line icons.
- **Tasks** — one card per task: badge row (`2 days overdue` in the error pair / `Due in 3 days · Thursday` in the attention pair / `Completed`) + provenance badge (`Your task` / `Key date`, no internal ids), the full title on its own line (wraps at 320px), the full due date, an "Open" affordance, and the same status `<select>` (44px) that updates the single owner. Summary gains an overdue chip; the upcoming sentence no longer repeats instructions; Completed is a quieter disclosure. Titles lost the 👤/📝 emoji.
- **Session detail** — Overview order is identity rows → links → (placement details) → **Attendance** section (state line "Not recorded / Recorded as attended / Recorded as absent · reason", one `role="group"` choice of two 44px buttons `Attended` / `Absent` with `aria-pressed` and a pressed ring, reason select) → **Notes & evidence** section (a truthful "Last saved on this device HH:MM" from `meta.at`, or "Nothing recorded yet"; textarea, standards, Review later, photos with an icon-labelled Add photo). Tabs stay above the content so Travel & map is one tap away; tab order equals reading order. The desktop side panel uses the same grouped attendance choice and a line-icon Pin.

Automated checks + results: new `tests/v2-daily-use.spec.ts` (6): the Today state contract incl. the FAB-clearance measurement and title round-trip; "Also now" card; the desktop overlap group (exact count from the aria-label, dialog lists that many cards, Enter/Escape, picking one selects only it, colour key present); Tasks at 320px (wrapped title, badge wording, 44px select, single-owner status update); detail section order and pressed states; week-strip keyboard. Legacy updates: `audit-r2` TT-14/TT-15 measure lanes at 1600px (at 1440px a two-lane column is ~95px, below the readable minimum, so V2 groups it — the rule is documented in the group test), `sync-devices` and the detail use the names `Attended` / `Absent`, `audit-r4` opens Plan week through the More menu on mobile, `phase-five` reads the kind line instead of the removed Personal badge, `phase-three`/`smoke`/`phase-four-visual` unaffected except the renamed next-action buttons. `npm run validate` AND `VERCEL=1 npm run validate` green — **161 unit and 124 browser tests**. Both hosting bases rebuilt; captures in `visual-ui-audit-2026-09-10/evidence/v2-*.png` (today, schedule, schedule-desktop, tasks, tasks-320, session).

Known limits: the Today hero's left colour is the accent, not the subject colour (cards keep subject colour); the compact/comfortable density toggle is the existing Appearance setting rather than a calendar-local control; the group threshold is a constant (100px) rather than a measured glyph width; the 320–1440 matrix and dark captures are V5.

Released 10 September 2026: merge `eebc41e`; Vercel deployed and `deploy.sh verify` six PASS. CI run 34524980635 FAILED on a pre-existing test fragility (`phase-four-visual` journey-home address matched twice once the map-tile fallback appeared) plus one flaky `phase-three` retry; fixed the locator in the next commit — CI run 34525364799 success. No worker change.

### Pass 60 — Visual UI audit V3: travel, journey home, Settings, Data & devices, Reminders, Find — 10 September 2026

Work items: batch V3 of `visual-ui-audit-2026-09-10/DEVELOPMENT_PLAN.md` — V-06 (travel mechanics before the next action), V-07 (Data & devices mixes health, attendance, backup and sync), Reminders grouping, Find. Branch `visual-v3`, client only; no `shared/` or worker change. Preservation register §1 checked for Session detail — Travel & map, Journey home, Settings, Data & devices and Find: every listed control survives (origin selector as a labelled native `<select>`, plan status/freshness/Retry, journey steps with departures and disruptions, RouteMap and StaticMap with tile states, Copy address, Open in Google Maps, weather, entrance disclaimer, Set home / Not now, category search + anchors + TT-21 link, Back up / Restore / sync, all six Find scopes and the keyboard contract).

Behaviour before → after:
- **Travel & map** — "Arrive early by" came first, then origin, then a small waiting sentence, address, map → **origin → destination → status → primary action → options**: a summary card names the starting point (or "Choose a starting point — use your current location or a saved place") and the destination (building, room, "Session starts HH:MM", Copy address); the status block carries a tone class (`--missing` / `--estimate` / `--live` / `--attention`) so state is text plus colour; when no origin is chosen a `Choose starting point` primary button focuses the real origin control; arrival buffer follows as a secondary preference with an explanatory line; map (220px on mobile, address outside the image, attribution kept), steps, weather, disclaimer and the Google Maps link are unchanged. The unavailable-map card is compact with Retry. Nothing requests geolocation as a side effect of rendering.
- **Journey home** — repeated origin instructions and a single "Directions home" → one "Your return journey" card (starting point explicitly "not selected" until chosen; Home with its saved address, Copy address and `Edit home location` grouped together), one state sentence ("Travel time needs a starting point — a past session does not confirm where you are now"), distinct `Choose starting point` (focuses the select) and `Open home in Maps ↗`; the no-home state still offers `Set home location` and `Not now`. The TT-17 copy rules are untouched.
- **Settings index** — seven flat cards with 12px hints → full-row buttons with coloured icon tiles, a 16px title, a 14px hint and a chevron (one accessible name per row, no nested control); Done only in the header; search, synonyms and anchors unchanged. A "Saved on this device — your settings apply automatically" line renders only from real persistence state (pending saves and failures show their own message).
- **Data & devices** — health list → attendance block → backup → sync, with long right-aligned values → **Your data** (save state, synced records, sync status, last backup generated, backup coverage; "Offline" badge and sync line from live `navigator.onLine` via `useOnline`) → **Back up & restore** → **Sync between devices** → an expandable **Storage & advanced** detail (photos/documents, drafts, sources, storage estimate, the "only safe once you can see the file" note) that stays inside the "Data health" list so existing truthful-state tests hold; recoverable drafts also surface automatically above the detail whenever any exist. Values sit under their labels below 640px and side by side above. **Attendance analysis relocated** to Term stats (headline %, sentence, per-subject list, `Export attendance CSV`) with a one-tap `Term stats` link left in Data & devices.
- **Reminders** — "Notifications at a glance" + mixed sections → groups by decision with the current state beside each heading: **Notification status** ("Allowed on this device" / "Not asked yet" / "Not supported" / **"Blocked on this device"** with numbered recovery steps, iOS-specific when installed, and a "Check reminders →" link to background push), Session reminders, Leave alerts, **Attendance prompts** (its own anchor and search entry), Key-date reminders, Quiet hours (own section, course-time label), Background push. Chips stay 44px and wrap; viewing never requests permission.
- **Find** — a visible "Search" label with a search icon, six scope chips plus an "All types" reset, results grouped by record type in relevance order (the best-ranked group first, so the first button is still the best match), a "what is indexed" note that also says an off-device document is never treated as empty, and an empty-state card; ↓/↑/Enter, Show more, per-profile query memory and the notes opt-in are unchanged; no recent-search tracking.

Automated checks + results: new `tests/v3-travel-settings.spec.ts` (6): travel order + focus + live tone + compact fallback; journey-home summary/actions/edit link; Settings tiles/names/Done/saved line/14px hint; Data & devices order, drafts surfacing, column/row layout by width, Offline, relocation to Term stats; Reminders grouping, blocked fixture with three recovery steps, states, zero permission prompts, search reaching Attendance prompts; Find label/scopes/groups/keyboard/scope reset. `audit-r5a` updated for the relocation (Data & devices asserts the link and the absence of the headline; the dialog asserts %, sentence, subject list and CSV). `npm run validate` AND `VERCEL=1 npm run validate` green — **161 unit and 130 browser tests**. Both hosting bases rebuilt; captures in `visual-ui-audit-2026-09-10/evidence/v3-*.png` incl. the successful-route fixture (`v3-travel-route.png`) the audit asked for.

Known limits: the travel state comparison is covered by the existing fixtures (`audit-r2`, `phase-six`, `phase-seven`) plus the two new states, not a nine-state screenshot matrix (V5); the Reminders "Configure" wording from the concept is not used because each group already shows its values inline; Storage & advanced is collapsed by default, so its rows are read by tests through the list's text rather than visibility.

Released 10 September 2026: merge `793e4f7`, CI run 34527031776 success, `deploy.sh verify` six PASS. No worker change.

### Pass 61 — Visual UI audit V4: PGCE workspace and admin analytics — 10 September 2026

Work items: batch V4 of `visual-ui-audit-2026-09-10/DEVELOPMENT_PLAN.md` — V-08 (admin source explanations compete with metrics), V-10 (equal-prominence actions on PGCE). Branch `visual-v4`, client only; no `shared/`, worker, metrics contract, retention or telemetry change.

Behaviour before → after:
- **PGCE file** — four equal white cards each with a "View all →" and 12px hints → four section cards with an identity tile each (teal Placement, indigo Evidence & reflections, violet Development, blue Documents & reports — identity colours from the semantic tokens, never a warning), 18px headings, 14px summaries, ONE primary per card, and count badges drawn from real records only (`N days logged`, `N records`, `N files`; none in an empty state and no percentage anywhere on the page). "View all" is gone: links name their destination (`Weekly reflections (N)`, `All development records →`, `Print binder & exports`, `Term stats & attendance`). The Placement primary follows state — `Set up placement` with no blocks, `Open placement` otherwise. Desktop (≥1024px) lays the cards out in two columns. A privacy line states that records stay on the device until exported or shared. Attendance analysis is hosted here via Term stats (relocated in V3). Every record editor stays within two purposeful steps (Development menu → type).
- **Admin analytics** — emoji rail and controls, caveat paragraphs competing with numbers → a navy rail (`--rail-*` tokens in both themes) with line icons (`components/icons.tsx`, the learner app's stroke language, kept local so the admin bundle stays independent), all six sections plus a rail `Lock workspace` control (and the same control in the mobile topbar); the lock screen and theme/show-key buttons use icons with text and keep their accessible names. Overview: a single "Two measurement sources are in use" notice, three headline cards with icon tiles that keep the fixture values and their fixed windows on the card, the chart card with a `Legacy dataset` badge (dark teal returning / light teal new, zero baseline, 12px ticks, readout and data table unchanged), a "Collection needs context" card with the 30-day/new/ever-recorded context and a `Review reliability` link, and a separate `New event dataset` card that states the different contract and warns against direct comparison. The status strip shows `UTC reporting` and `Today is partial` as labelled chips beside Refresh. There is deliberately no date picker: every metric window is fixed by the contract. Mobile stacks the headline cards and keeps the labelled section picker.

Automated checks + results: new `tests/v4-pgce-admin.spec.ts` (4): PGCE empty (tiles, one primary each, no View all, Set up placement, no counts, no %, privacy line) and populated (badges from real records, state-driven placement primary, two columns at 1280px, editor reachable via the menu, reflections dialog); admin desktop (six rail links with SVG icons, Lock workspace, rail colour, notice, dataset badge, v2 card warning, chips, data table, reliability link, no emoji controls, no window picker) and mobile (rail hidden, section picker, topbar lock, stacked cards, no overflow, chart keyboard). `admin-a1` uses the new lock name; `audit-r3` asserts the destination-named links instead of four "View all". `admin-a1…a5` otherwise unchanged and green. `npm run validate` AND `VERCEL=1 npm run validate` green — **161 unit and 134 browser tests**. Captures in `visual-ui-audit-2026-09-10/evidence/v4-*.png`.

Known limits: the admin pages other than Overview keep their existing card content (they inherit the new type, badge and notice styles); the adoption table and reliability charts are untouched beyond palette; PGCE record sheets (AdminSheet, journal, binder) are not restyled here.

Released 10 September 2026: merge `e0232a1`, CI run 34528741406 success, `deploy.sh verify` six PASS. No worker change.

### Pass 62 — Gap closure after the owner's check of V0–V4 — 10 September 2026

Work items: the owner reported that not every change the visual audit asked for was visible ("settings once you drill into them are unchanged, the map detail view is unchanged"). A scripted sweep captured 68 screens (every route, every Settings sub-page, session detail overview/travel, journey home, every reachable sheet, all admin pages at two widths) and ran the V1 measurement walk on each; the findings and their status are in `visual-ui-audit-2026-09-10/GAP_AUDIT_2026-09-10.md`. Branch `visual-gaps`, client only; no `shared/` or worker change.

Behaviour before → after (G1–G7 in the gap audit):
- **Settings sub-pages** (My timetable, Travel & home, Connected calendars, Appearance, Help & privacy) — flat 14px headings, long paragraphs and emoji copy → card sections with 18px headings and 14px copy, the current state beside each heading (timetables on this device, specialisms chosen, location on/off + mode, home set, feed availability, export counts, theme, density), the long "what is sent" copy behind a 44px disclosure, emoji removed, Study group with a line icon.
- **Session detail** — Overview now opens with an identity card (kind badge, calendar/clock/pin rows); the details list keeps Duration, Room name, Tutor, Subject, Groups.
- **Travel & journey home copy** — "pick a saved origin above" → "choose a saved starting point below" (the control follows the status); `audit-r2` TT-17 updated.
- **Emoji removed across the learner app** — PGCE file (overview rows, title, target statuses, Add a document), evidence journal (kind markers become badges, photo counts in words), task editor status chips, Filters, Month view pins, Agenda break/placement rows, Setup feature tiles, backup/journey warnings, attendance quick-answer buttons (`Attended` / `Absent` with icons), the static-map pin (SVG). Weather glyphs stay as forecast data. `notifications-quick`, `phase-five` and `admin-a4` use the new names.
- **Measured follow-ups** — 44px urgent chip and week label, 13px kicker/profile line, 16px note/caption/placement inputs, 13px Term-stats captions; admin table headers 14px, sort controls 36px, Refresh 44px, brand sub-line 13px, picker label 14px, chart legend matching the bar colours, context-card heads left-aligned.
- **Deployment note** for the owner's observation: the live main bundle already carried the V3 travel panel; a device that had the app open keeps the previous service-worker build until the app is closed and reopened.

Automated checks + results: new `tests/gap-closure.spec.ts` (3): sub-page cards/headings/state/disclosure/no emoji, identity card + travel wording, a whole-app emoji scan across nine routes and six sheets. `tests/v1-foundations.spec.ts` now measures 14 screens. `npm run validate` AND `VERCEL=1 npm run validate` green — **161 unit and 138 browser tests**.

Known limits: admin table sort controls stay at 36px by design (compact table headers); the What's new list keeps its wording without emoji; the audit's V5 (matrix captures + archive) follows as Pass 63.

Released 10 September 2026: merge `017c0bf`, CI run 34533058288 success, `deploy.sh verify` six PASS. No worker change.

#### Pass 62 addendum — the evening Today state — 10 September 2026

The owner reported the UI "still looks incomplete" after Pass 62. A capture of the LIVE site (seeded demo profile, 390px and 1280px, real tiles) confirmed every shipped change was being served (`detail-identity`, tiled Settings, `travel-od` all present in the live bundles). What looked incomplete was the state being viewed: in the evening, after the last session, Today collapsed to a "Show N finished sessions" link plus the promoted Journey-home row (the finished-day card was hidden whenever home was promoted, and the tomorrow preview was gated off), and the demo profile has no tasks, so Tasks and PGCE show their empty states.

Fix (branch `today-evening`): after the last session Today always shows the "That's the day done" card with the next-day line and `See the week` / `See deadlines`, then the Journey-home row, then the day's sessions listed by default under "Today's sessions" (still collapsible; during the day they stay collapsed under "Finished today"). `v2-daily-use`, `phase-four-visual`, `audit-r1`, `notifications-quick`, `v1-foundations` and `phase-five` (which asserts no finished card while a personal event is still running) all pass. Live captures for comparison are in `visual-ui-audit-2026-09-10/evidence/live-*.png`.

Released 10 September 2026: merge `4d10f1a`, CI run 34534053257 success, `deploy.sh verify` six PASS.

### Pass 63 — Design fidelity: the mockups' look and feel — 10 September 2026

Work items: the owner reviewed the live app after Pass 62 and asked that the Settings detail pages and the session detail (and everything else) match the look and feel of the visual spec, not only its acceptance bullets. The spec's real visual language is the designers' shared `visual-ui-audit-2026-09-10/designs/design.css` (cards 18px radius with a hairline and a faint shadow, 42px icon tiles in teal/amber/violet/blue, a gradient "hero" identity card, white-on-grey segmented tabs, 13px/650 rectangular tags, a filled selected day, amber callouts, key/value rows, a bordered search box, a grey route box, a single card of link rows for the Settings index). Branch `design-fidelity`, client only; no `shared/` or worker change; every control in the preservation register kept.

Behaviour before → after:
- **Session detail** — page title is now "Session details" / "Travel to session" / "Task details" with a `PS1 - … · Mon 7 Sept` subtitle; Overview opens with the gradient hero (kind tag with icon, title split into heading and subtitle with the separator kept for the accessible name, clock and pin rows), then tiled cards: Details (duration, tutor, subject, groups, Moodle, Google Calendar), Attendance (teal check tile, state line, two 46px buttons), Notes & evidence (violet book tile), Placement details, and a wide "Travel to this session" primary. Travel & map follows the mockup order: route box → map (or the compact unavailable card) → callout → `Choose starting point` → origin control → wide "Open in Google Maps" → arrival buffer → steps → weather → a "Planning the return trip?" card with `View journey home` (new `onOpenHomeJourney` prop wired from App).
- **Settings** — the index is one card of link rows with tiles, a bordered search box with an icon, a subtitle and the teal "Saved on this device" note; every section on every sub-page has an icon tile (28 sections), an 18px heading, its state as a tag, and the pages carry the mockup subtitles ("Keep a recovery copy of your timetable", "Choose which alerts help you", …).
- **Today** — a visible time-of-day greeting (the accessible page name stays "Today"), tag pills for "Up next" and the kind, the hero as the gradient card, `→ Open session` primary with Directions beside it, "Later today", event cards, and the Journey-home row with a chevron action.
- **Schedule** — subtitle "… · London time", white-on-grey tabs, filled selected day, 17px day numbers, "Today" tag, event cards, and a wide "Plan study time this week" button under the mobile day list (Plan week stays in More as well).
- **Tasks** — subtitle, red/indigo/teal summary tags, icon search box, "Needs attention / Due today / Coming up", cards with tag row, 18px title, due line, a wide `Review task` primary on overdue cards and `Open task` on others (status control kept), Completed as a tiled card row.
- **Shared** — canvas `#f4f6fb`, 28px page titles, 46px buttons with 11px radius, 44px icon buttons with 12px radius, event cards 14px radius, callouts, key/value rows, bottom-nav active pill. Dark values mirror the mockups' `.dark` block and are scoped to the dark media query and `data-theme="dark"`.

Automated checks + results: legacy names updated (`Open session`; the light canvas colour). `npm run validate` AND `VERCEL=1 npm run validate` green — **161 unit and 138 browser tests**. Captures in `visual-ui-audit-2026-09-10/evidence/fidelity-*.png`.

Known limits: Back stays a 44px control (the mockup's 30px would fail V-03); Reminders keep their controls inline rather than "Configure" links; the desktop grid keeps its measured lanes and 68px hour height.

Released 10 September 2026: merge `56d72ec`, CI run 34535511182 success, `deploy.sh verify` six PASS. No worker change.

### Pass 64 — Sync: one timetable, one profile across devices (owner bug report) — 10 September 2026

Work items: the owner entered the sync code from one device on another and found that attendance marks and Term stats "did not sync". Root cause: each device creates its profile with a random id (`newProfileId`), and sync merges profile stores by id. Two devices that set up the same timetable independently therefore end up, after connecting, with TWO profiles each — the marks had synced, into the other device's profile, while the active profile (the device's own) stayed empty; Term stats follow the active profile. The two-device test seeded both devices with the same id, which hid this. Branch `sync-unify`, client only; no `shared/` or worker change (the worker stores opaque blobs).

Behaviour before → after (`src/lib/sync.ts`):
- **Adoption on merge** — `unifyProfiles(local, remote)`: before any merge, a local profile that is the same timetable as a remote one (same sheet id and tab; for the built-in demo only in the unambiguous case of exactly one demo profile on each side, since several demo profiles on a device are deliberately distinct — `phase-two`) but has a different id is re-keyed to the remote id; its notes/attendance, PGCE file and identity history fold into that profile (records merged newest-wins, never dropped). The remote id wins because the first device to park state defines it.
- **Dedupe after merge** — `dedupeProfiles(payload)`: any two profiles for the same real sheet and tab in a merged (or local) payload fold into the earliest id with a profile tombstone in `deletedProfiles`, so devices that already parked duplicates converge on their next exchange and every other device drops the copy. Applied in `mergePayload`, on the local payload when the remote is empty, and in `applySyncPayload`.
- **Active profile kept** — when the device's active profile is re-keyed or folded, the active id follows it (by id map, else by timetable identity); device-local settings still come from the device.

Automated checks + results: `tests/sync-devices.spec.ts` gains two tests — (1) device B with its own profile id and its own note adopts device A's profile, sees A's mark in its active timetable without a reload, keeps its note, deletes its old keys, and A ends with one profile carrying B's note; (2) a device already holding the same real sheet twice folds to the earliest id with a tombstone and merged records. `npm run validate` AND `VERCEL=1 npm run validate` green — **161 unit and 140 browser tests**.

Known limits: two different real sheets stay separate profiles by design; a timetable set up from the same sheet but a different tab (gid) is treated as a different timetable.

Released 10 September 2026: merge `023264c`, CI run 34536830361 success, `deploy.sh verify` six PASS. No worker change.


### Separate P1/P2/P3 placements — 11 September 2026 (documentation and concepts only)

Updated `archive/enhancements/2026-09-11-pgce-student-experience/PGCE_QTS_STUDENT_EXPERIENCE_AUDIT.md` with owner clarification PG-07A: three placements at three different schools, separately configured and linked to school-specific lessons, attendance, mentor records, evidence and travel. Preserves existing per-tag details through explicit reviewed migration. Added 12 mockup screens (27 total), separate setup pages and outward/return journeys for each school, with illustrative non-live maps/times and preserved TfL/train requirements. All three school/setup/outward/return prototype navigation checks passed; no broken asset links or captured-width overflows. Existing application changes were preserved; no app source or production service changed.

### Pass 65 — Bottom navigation follows the visual viewport (owner bug report) — 11 September 2026

Work items: on iPhone the bottom navigation floated about 62% down the screen with page content continuing beneath it (owner screenshots, 11 Sep). The nav is `position: fixed; bottom: 0`; on iOS Safari and standalone PWAs a fixed element is left where the bottom of the screen WAS while the on-screen keyboard was open when the focused input is removed from the page — the Schedule search toggle and leaving Find both unmount a focused `autoFocus` input. Branch `nav-viewport`, client only.

Behaviour before → after:
- **Nav and FAB positioned from the live visual viewport** — `useVisualViewportOffset` (mounted in AppShell) keeps `--vv-bottom-offset` = the layout-viewport height hidden below the visual viewport, recomputed on visual-viewport resize/scroll, window resize, orientation change and shortly after any focus change; `.bottom-nav` uses `inset: auto 0 var(--vv-bottom-offset, 0px)` and the FAB adds the same offset. While a keyboard is up the nav sits on its edge; when it closes the offset returns to 0, so it can no longer be stranded.
- **Inputs blur before they unmount** — the Schedule search close blurs the active element first; Find blurs on unmount.
- `--nav-height` is 76px to match the Pass 63 nav so content padding clears it.

Automated checks + results: `tests/nav-viewport.spec.ts` (2): a mocked, resizable visual viewport moves the nav up by 300px and back to 0 (and the FAB follows); closing the Schedule search blurs the input before removal. Both gates green.

Known limits: the mock stands in for iOS; the real device check is the owner's.

Released 11 September 2026: merge `2871d61`, CI run 34579780657 success, `deploy.sh verify` six PASS. No worker change.

### Pass 66 — Self-study rows are self study, never a placement; no attendance for self study (owner bug report) — 11 September 2026

Work items: "SE1a Briefing Self Study" showed as a Placement (kind tag, Placement details form, school-day counting) and offered attendance. Root cause: `shared/timetable.js` only flagged `isSelfStudy` when the whole title was "Self Study", and `isPlacementTitle` matches the "SE1a" block code, so a self-study briefing about a placement fell into the placement rule. Branch `self-study-not-placement`; `shared/` changed, so both workers deploy first.

Behaviour before → after:
- **Parser** (`shared/timetable.js`, mirrored in `src/lib/demo.ts`): any title containing "self study" / "self-study", or a tutor of "Self Study", is self study.
- **Placement rule** (`shared/eligibility.js` + the push worker's mirror): `isSelfStudyTitle(t)` is exported and `isPlacementTitle` excludes it, so self-study rows never count as school days, never expand into placement day spans, never get placement reminders, and never show the Placement details form.
- **Attendance**: self-study sessions were already outside the eligibility denominator and the "did you attend?" prompts (app and worker); the detail view now hides the Attended/Absent choice for them and states that attendance is not recorded.

Automated checks + results: `tests/unit/eligibility.test.mjs` asserts the title rules; unit 162; both gates green. Worker deploy recorded below.

Released 11 September 2026: workers deployed first (push `4a011823-48dd-4d23-95c8-44165a5fb23c`, feed `9d88255d-0824-4e2f-9667-fb45353b4c91`); merge `333c9dd`, CI run 34580402515 success, `deploy.sh verify` six PASS.

### Pass 67 — Visual UI audit V5: validate, capture matrix and archive — 11 September 2026

Work items: the audit's closing batch (`DEVELOPMENT_PLAN.md` V5 / audit §17 Batch E). Branch `visual-v5`, client only (one CSS wrap fix and one focus-return fix), plus the required archive step.

Checks and results:
- **Matrix, as assertions** — new `tests/v5-matrix.spec.ts` (4): no page-wide horizontal overflow on nine primary screens at 320/390/768/1024/1440px in light and dark; reduced motion plus 200% zoom at 390px (a 2px device-pixel tolerance); keyboard-only navigation (Tab to the bottom nav, Enter changes route, focus lands on the new page heading, a closed dialog returns focus to its opener); scroll reachability (the last actionable element on every screen clears the bottom navigation and the FAB).
- **Matrix, as captures** — 96 files under `evidence/matrix/` (nine screens × five widths × light/dark, plus the session detail, travel and a 200% sample). Designer/developer review: dark hero, callouts and tabs hold; 320px wraps cleanly; two defects found and fixed — the page header actions and the Today hero actions clipped instead of wrapping at 200% (now `flex-wrap`), and a dialog whose first field autofocuses remembered its own input as the opener, so closing it returned focus nowhere (`useModalA11y` now restores the last element focused outside any dialog).
- Permission failures, map failure, stale data, unsaved edits and offline are covered by the permanent suites (`v3-travel-settings` blocked permission and Offline, `audit-r2` TT-16 map failure and stale/expired plans, `audit-r5a` drafts, `v0-carry-forwards` restore impact); they ran green in both gates.
- `npm run validate` AND `VERCEL=1 npm run validate` green — **162 unit and 146 browser tests**.

Archive: `visual-ui-audit-2026-09-10/` moved to `archive/enhancements/2026-09-10-visual-ui/` (audit, batch plan, gap audit, designs incl. `design.css` and the gallery, evidence, capture matrix) with a README listing completed IDs (B-01, FA-01–07, V-01–V-10, look-and-feel) and verification results; relative links unchanged; PLAN.md carries an archive note at the top. Production source, permanent tests and runbooks stayed in place. The PGCE student-experience audit package (then `pgce-student-audit-2026-09-10/`) was archived after Pass 72 at `archive/enhancements/2026-09-11-pgce-student-experience/`.

Released 11 September 2026: merge `78a1936`; Vercel deployed and `deploy.sh verify` six PASS. CI run 34581806662 failed its first attempt on two telemetry-timing tests (`admin-a4` task_created/task_completed counts, `admin-a2` usage toggle — both green locally in every gate and on the previous five CI runs) and passed on rerun of the failed job. No worker change. The visual UI audit is complete and archived.

### Pass 68 — PGCE G0: placement entity, reviewable tag → placement migration, typed references, provenance, schema versioning — 11 September 2026

Work items: the first batch of the PGCE student-experience plan ([archive/enhancements/2026-09-11-pgce-student-experience/DEVELOPMENT_PLAN.md](archive/enhancements/2026-09-11-pgce-student-experience/DEVELOPMENT_PLAN.md) §G0). Branch `pgce-g0`. `shared/contracts.js` and `shared/merge.js` changed, so both workers deploy first.

Behaviour before → after:
- **Placement entity** — `AdminFile` gains two optional collections, `placements` (`id`, `code` SE1/SE2/SE3, optional label, `schoolLocationId`, date range, `mappedBlockTags[]`, mentor name/contact, working-hours override, inset policy, arrival buffer, return place, notes, `at`) and `schools` (`id`, name, address, lat/lng with `confirmedAt` only once the learner confirms a pin, entrance note, `at`). Both are validated on the wire (`validatePayload`: code format, tag list bounds, hours, date order, coordinate ranges), merged per record with tombstones, and ride through backup/restore untouched (raw key copy). Placements never merge by school name or dates — only by id.
- **Reviewable migration sheet** — the PGCE Placement card gains `Set up SE1, SE2, SE3 →` (later `Review …`). The sheet lists the timetable's placement block tags (`placementTagOf`; self-study rows excluded since Pass 66) with their session counts and any imported `Settings.placements[tag]` school, and a **proposed** placement by block prefix (SE1a → SE1). Nothing is written until `Confirm placements`; Cancel and Escape write nothing even after edits. Confirming creates or updates one record per used code, carries the imported school/address/coordinates/mentor into a `schools` record and the placement only where the record has none yet, and never deletes anything from Settings. A block may stay Unassigned; on review an Unassigned block stays Unassigned (the proposal is shown as text, never re-applied). Re-confirming the same choices is a no-op — identical records, no `at` bump. The placement overview labels a mapped block `SE1 · <school>`.
- **Typed references** — `src/lib/refs.ts`: `{ profileId, entityType, entityId, revision? }` with `makeRef`/`sameRef`/`refKey`/`parseRefKey` and `resolveRef`, which returns `unassigned` for a missing or tombstoned target (callers render that word, never a guess) and `other-profile` for a cross-profile ref. `placementId?` is additive on Lesson, Observation, Meeting and MeetingAction; the contract rejects a malformed id but accepts a dangling one (shown as Unassigned).
- **Provenance** — Observation gains `sourceType: 'personal-reflection' | 'learner-entered'`; `reviewer-authenticated` exists in the type but the contract rejects it from any client (no portal). Pre-G0 observations are read as learner-entered without a rewrite, so their `at` and sync identity are untouched.
- **Schema versioning** — `AdminFile.schemaVersion` (2) is stamped on save; `loadAdminFile`, `mergeAdmin` and backup keep any field this build does not know (remote-first on merge), so an older device can never strip a newer one's data.

Automated checks + results: `tests/unit/pgce-g0.test.mjs` (6: optional collections and legacy payloads, placement/school validation, typed links and provenance limits, a three-placement round trip, schemaVersion bounds, merge with tombstones and unknown-field preservation) and `tests/pgce-g0.spec.ts` (2: proposals → cancel/Escape → confirm with Unassigned → card and overview labels → idempotent re-confirm → later assignment keeps ids; an older admin file with an unknown field loads and survives a save). Existing PGCE suites unchanged. `npm run validate` AND `VERCEL=1 npm run validate` green — **168 unit and 148 browser tests**.

Scope notes: the demo world has no placement blocks, so the G0 fixture is a cached real-profile timetable (SE1a/SE1b/SE2); demo placement rows arrive with the chooser in Pass 69. No new telemetry, no outcomes inferred (attendance and standards untouched).

Released 11 September 2026: workers deployed first (push `4c8cbcd4-1e7a-4f91-8829-2ef4cf79f0d4`, feed `6450b4d0-ee10-43d3-94e9-8a769cb5f2c7`); merge `9571060`, CI run 34584061351 success, `deploy.sh verify` six PASS.

### Pass 69 — PGCE G1a: SE1/SE2/SE3 placements (PG-07A) and the programme roadmap (PG-01) — 11 September 2026

Work items: the plan's Pass 69 ([archive/enhancements/2026-09-11-pgce-student-experience/DEVELOPMENT_PLAN.md](archive/enhancements/2026-09-11-pgce-student-experience/DEVELOPMENT_PLAN.md) §G1). Branch `pgce-g1a`. `shared/contracts.js` gained four collections and `shared/programme.js` is new, so both workers deploy first.

Behaviour before → after:
- **Placement chooser** on the PGCE file (`PlacementChooser`): one card per placement — SE1, SE2, SE3 — with code, school, dates, mentor, hours (the profile default shown as such), mapped timetable blocks, a setup state (Not set up / School details incomplete / Ready to plan) and timing by dates (Dates not set / Upcoming / Current / Finished). One primary action per state: `Set up SE2`, `Complete setup`, or `Open placement` with `To school` / `Back home`. Journeys are offered only once the school pin is confirmed. "All placements" opens the existing grouped day-by-day view; "Review block mapping" opens the G0 sheet. Accents per card are identity only.
- **School setup flow** (`PlacementSetupFlow`): Placement → School (name, address, `Locate on map` via the existing geocoder, then a visible **`Confirm this pin`** step that stamps `confirmedAt`; an address alone is never treated as verified) → People & dates → Pattern & travel (hours override with the default shown, arrival buffer with the Settings default shown, inset policy, return destination = home or `Set return destination`) → Timetable (preview of the block mapping: this placement's tags plus prefix proposals; blocks mapped to another placement are shown disabled as "mapped to SE1") → Save. `savePlacementSetup` is pure and touches only this placement and its school record: saving SE2 leaves SE1 and SE3 byte-for-byte identical (tested). A confirmed pin, school, address and mentor are mirrored into `settings.placements[tag]` for each mapped block so the push worker and the session travel tab resolve the school from the session's mapping, not from whichever placement is open.
- **Placement workspace** `#/placement/:id` (`PlacementWorkspacePage`): hero with dates, hours, mentor and address; `To school` / `Back home` / `Edit setup`; Experience & attendance for its mapped blocks (planned vs logged days, next school days, link to the day-by-day log); Lessons, Observations & meetings (open actions) and Evidence & resources, each linking to the record editors and journal. `#/placement/<unknown>` is an honest "Placement not found"; an unknown journey leg falls back to the workspace with a notice (router + `navigationState` parse the new `id` and `leg`).
- **Typed links in the editors**: the Lessons, Observations and Meetings quick-add forms and `RecordEditSheet` gain a Placement field (Unassigned by default; an unassigned link is absent on the wire, never an empty string); list rows show `SE1 · <school>`; the session detail hero shows the same label from the block mapping.
- **To school / Back home** `#/placement/:id/journey/out|back` (`PlacementJourneyPage`, reusing `useJourney`): outward plans **arrive-by** the next mapped school day's start minus this placement's buffer (leave-now once the day has started); the request identity — profile, placement, direction, origin, destination, mode, time — lives in the journey key, so a late SE1 response never renders under another placement (tested against SE3, which has no confirmed pin). Return is planned separately: leave-now from the school (default origin) or the device to home; no reversed polyline. From → To with entrance note, arrival time and buffer are shown before results; origin choice, live TfL steps, route or static map, Copy address and Open in Maps are all kept. No home → "Set return destination" (→ Settings → Travel & home); no confirmed pin → "School location not confirmed yet" (→ Complete setup).
- **PG-01 programme roadmap** (`shared/programme.js`, `ProgrammeSheet`, Settings → My timetable → Programme): course profile (route, phase, mode, provider, year, jurisdiction, subject, age range, dates) in the learner's words; requirement packs imported as JSON (validated whitelist rebuild, **diff preview before anything is written** — added/changed/unchanged/removed, milestones added/moved/kept); requirements listed by section with verification **Unconfirmed** until the learner confirms ("Confirmed by you from <source>"); a changed wording clears the confirmation, an unchanged one keeps it; milestones (academic / training / review) marked done only by the learner, and a review already held keeps its date and pins its pack version when a newer pack is applied. The PGCE file gains a **Programme roadmap** card: two pathways (next academic, next training with done/total), one next review, and the unconfirmed count — "Requirements not yet confirmed" when nothing has been imported; **no built-in 120-day target and no completion ring**.
- Contract: `programmes`, `packs`, `requirements`, `milestones` optional collections with enum/date/link validation; `ADMIN_SCHEMA_VERSION` 3.

Automated checks + results: `tests/unit/pgce-g1a.test.mjs` (5: pack validation, v1→v2 apply semantics incl. pinned review version, other packs untouched by reference, roadmap summary, wire contract) and `tests/pgce-g1a.spec.ts` (5: chooser states + SE2 setup with byte-for-byte SE1/SE3 isolation and the settings mirror; workspace records, next-day labels, session label and the editor's Placement field; outward arrive-by with buffer, return from school, late-response isolation, bad routes; no-home return state; programme profile → pack import → diff → confirm → v2 → milestones → roadmap card). `tests/pgce-g0.spec.ts` updated for the chooser (mapping sheet via "Review block mapping", grouped view via "All placements"). `npm run validate` AND `VERCEL=1 npm run validate` green — **173 unit and 153 browser tests** (the default gate's first run hit the known admin-a4 telemetry-timing flake noted in Pass 67 and passed on rerun; the Vercel-base gate was green first time).

Scope notes: the demo world still carries no placement blocks (G0 note stands; the chooser shows three "Not set up" cards on the demo). Reminders keep reading `settings.placements[tag]`, now fed by the setup mirror — no worker logic change beyond the shared contract. Deferred to G1b: nothing from this pass; G1b is the lesson workbench, practice cycle and mentor preparation.

Released 11 September 2026: workers deployed first (push `7613dd67-334f-4e23-a298-ca740df023b1`, feed `72c653ea-4558-4569-b8dd-c68d8ee09980`); merge `d98c7bd`, CI run 34587584291 success, `deploy.sh verify` six PASS.

### Pass 70 — PGCE G1b: lesson workbench (PG-02), practice cycles and mentor preparation (PG-03) — 11 September 2026

Work items: the plan's Pass 70 ([archive/enhancements/2026-09-11-pgce-student-experience/DEVELOPMENT_PLAN.md](archive/enhancements/2026-09-11-pgce-student-experience/DEVELOPMENT_PLAN.md) §G1). Branch `pgce-g1b`. `shared/contracts.js` gained two collections and `shared/practice.js` is new, so both workers deploy first.

Behaviour before → after:
- **Lesson workbench** (`LessonWorkbench`, reached from a lesson row in the placement workspace, the Lessons list's `Workbench` link, a practice focus's attempts, or a Today next step): **Plan → Rehearse → Teach → Review** as four stage views (a real `tablist`, arrow keys move) over ONE lesson record. Plan adds the additive fields — linked timetable occurrence (typed session ref from the lesson's date), unit reference, intention, prior knowledge, misconceptions, sequence, checks, planned responses, resources — with two optional templates, a placement and a practice focus (a new focus can be started inline). Saving the plan bumps `planRevision` only when a plan field changed; the evaluation and standards are the other side of the record. Rehearse shows the plan, keeps the focus's rehearsal note, adds a **rehearsal block to the Schedule through the canonical plan model** (a task plus a timed block — no new block type) and marks the lesson rehearsed. Teach is the essential plan in large type with a 56px control; `Start teaching` pins `taughtPlanRevision` and **records nothing else** — attendance and standards are untouched (tested against the meta store). Review carries the evaluation and standards, names the taught revision when the plan changed afterwards, lists feedback with its provenance label, and lets the learner add feedback they received as **Entered by you** (`learner-entered`; the reviewer-authenticated state is still unsettable). `Next attempt` duplicates the plan under a **new identity** with no outcomes copied (evaluation, standards, taught/rehearsed/review stamps, feedback links). The quick retrospective lesson form is unchanged and still works. Everything is local, so the full cycle runs offline after load — the workbench, practice and preparation sheets therefore ship in the main bundle rather than as lazy chunks.
- **Practice cycles** (`PracticeSheet`, PGCE → Development menu → Practice focus): focus, curriculum reference, rehearsal note, attempts (lessons carrying the focus, each opening the workbench), feedback records, a review decision (keep going / adapt / close) with a note; **one active focus** — starting or resuming another pauses the current one with the reason shown; pause and archive carry no failure label.
- **Mentor preparation** (`MentorPrepSheet`, PGCE → Development menu → Mentor preparation): an agenda per planned meeting — what changed, where help is needed, examples selected by reference (lessons and feedback records, with provenance), proposed next steps, and the open actions already owed from meeting records. `Meeting held — save` records what happened, an optional duration and the next review date, and **creates a Meeting record that owns the agreed actions** (the same owner as before; the preparation links to it). Editing an observation in the record editor now increments its `revision`, so a reviewed record never changes silently.
- **Today next steps** (`NextStepsCard`): at most three, derived only from dated work and the active focus — plan a lesson due within a week, rehearse a planned one, review a taught one without an evaluation, plan an attempt for the active focus, prepare for a meeting within a fortnight, an agreed next review. Nothing undated can be overdue and nothing is labelled late; a past unplanned lesson is not nagged. Dismiss hides a step for that day; pin keeps it first (both per profile on this device). Opening a step lands on the right stage.
- Contract: `cycles` and `preps` optional collections; additive lesson, observation (`lessonId`, `cycleId`, `revision`) and meeting (`prepId`) fields validated; `ADMIN_SCHEMA_VERSION` 4.

Automated checks + results: `tests/unit/pgce-g1b.test.mjs` (6: plan revisions vs evaluation, next attempt without outcomes, one active focus, next-step rules incl. dismissed-per-day/pinned/never-overdue, provenance labels, wire contract) and `tests/pgce-g1b.spec.ts` (4: the full cycle offline from plan to next attempt with no attendance change and the quick form intact; Today next steps with dismiss, pin, reload and stage landing; mentor preparation through to the meeting record and Today; practice focus pause/resume/decision/archive with no failure wording). `npm run validate` AND `VERCEL=1 npm run validate` green — **179 unit and 157 browser tests**.

Owner report handled in the same pass (11 Sep, second screenshot): the bottom navigation still floated mid-screen — about one keyboard height up — with no keyboard open. Root cause: iOS can keep reporting a shrunken visual viewport after the keyboard has closed, and Pass 65's hook trusted it. Now `useVisualViewportOffset` applies an offset ONLY while an editable element has focus (the only time a keyboard can be open) and at no pinch zoom; with nothing focused the offset is 0 whatever the viewport reports, and a one-frame compositing nudge after focus leaves lets WebKit re-anchor the fixed nav. `tests/nav-viewport.spec.ts` now covers the stale report (shrunken viewport, nothing focused → nav at the bottom), the real keyboard (focused input → nav on its edge), the stale-after-blur case, and pinch zoom.

Scope notes: templates are two built-in outlines, not a template editor; the audit's later items (subject-knowledge goals, academic workspace, workload, experience ledger, review packs) stay in G2/G3. Demo world unchanged.

Released 11 September 2026: workers deployed first (push `ac80053f-ba61-408d-884e-528877195bb9`, feed `36b6c3a2-0be9-41c0-a798-76e7b21637e8`); merge `70dcd13`, CI run 34590930280 success, `deploy.sh verify` six PASS.

### Pass 71 — PGCE G2: subject knowledge (PG-04), academic workspace (PG-05), workload & support (PG-08) — 11 September 2026

Work items: the plan's Pass 71 ([archive/enhancements/2026-09-11-pgce-student-experience/DEVELOPMENT_PLAN.md](archive/enhancements/2026-09-11-pgce-student-experience/DEVELOPMENT_PLAN.md) §G2). Branch `pgce-g2`. `shared/contracts.js` gained eight collections and `shared/workload.js` is new, so both workers deploy first.

Behaviour before → after:
- **Subject knowledge** (`KnowledgeSheet`, PGCE → Development menu → Subject knowledge): goals in the learner's words — topic (offered from the programme pack's curriculum/subject sections when a pack carries them; free text otherwise), breadth or depth, the learner's question, **dated self-ratings of confidence (1–5 with a note)**, resources shared across goals (one resource ↔ many goals, linked or created inline), an application opportunity (a lesson), a next review, park/done/reopen. A subject-audit entry marked `secure` can be brought in as a goal whose question reads **"Previously marked secure by you (date)"** — nothing anywhere is assessed mastery. No question cards, no AI.
- **Academic work** (`AcademicSheet`, same menu): each assignment or enquiry as a project — brief, provider criteria, a **deadline that references an existing key date** (chosen from the timetable's key dates; never a typed copy; a missing reference is shown as such), optional words/credits with no award arithmetic, **milestones as ordinary tasks** (carrying `projectId`; they appear on Tasks and in the workload planner), reading notes that keep **quotation / paraphrase / interpretation** apart with source and page, and a classroom-enquiry approval-planning section (context, participants, consent, risks, approval as told to you — recorded, not a workflow). Status draft → ready → submitted → feedback → result moves only on the learner's action; **submitted needs the "I submitted this myself" confirmation**; feedback and result are text plus their source, and the result line says it awards nothing. Exports were not changed and never touch a status.
- **Workload & support** (`WorkloadSheet`, same menu; `shared/workload.js`): a deterministic needed-vs-available reckoning over the next fortnight using the **existing plan model** — needed = remaining estimated effort on open tasks (subtask/milestone estimates minus blocks already on the calendar), available = plan-week gaps after timetabled sessions with their travel buffers, personal commitments, existing blocks, quiet hours and the learner's **protected weekly windows** (also honoured by the week planner). It reports the gap plainly ("2 h 30 m unallocated — not enough free time before the due dates"), lists tasks with no estimate as unknown (never guessed) and due dates already passed, and proposes earliest-deadline-first blocks (30–90 min, never after a task's due date, never in protected time) **as a diff** the learner accepts or leaves. Accepting adds ordinary plan blocks under each task; re-running proposes nothing already planned (no duplicate scheduling); a deadline change recomputes without accepting anything; **Undo last accept** removes exactly that batch (ids kept per profile on this device). Support: user-entered contacts, a private support agenda, and course questions marked answered by the learner — **no wellbeing score, no streaks, nothing sent to staff**.
- Contract: `goals`, `resources`, `projects`, `readings`, `contacts`, `questions`, `protected`, `supportNotes` optional collections with enum/date/link validation; `tasks.projectId` additive; `ADMIN_SCHEMA_VERSION` 5.

Automated checks + results: `tests/unit/pgce-g2.test.mjs` (5: remaining effort and unknown estimates, protected time/travel buffers/due dates/EDF, no duplicate scheduling and undo, deadline change recompute with an honest gap, wire contract + merge round trip of every new collection) and `tests/pgce-g2.spec.ts` (3: knowledge goal end to end incl. the audit import and a shared resource; academic project from key-date deadline through confirmed submission, feedback, result, milestone task and reading note; workload protected time → proposals → accept → no duplicates → blocks on the Schedule → undo → deadline change recompute → support). `npm run validate` AND `VERCEL=1 npm run validate` green — **184 unit and 160 browser tests** (first full run: the known admin-a4 timing flake and the TT-20 menu list, which now includes the three new items).

Scope notes: the audit's later items (evidence narratives, experience ledger, review packs) stay in G3; question cards, AI suggestions and provider connectors are out as planned. Demo world unchanged.

Released 11 September 2026: workers deployed first (push `b4023dce-e171-4325-975b-cb1001f74bb0`, feed `dd70ae73-6ca2-4e63-8657-1a2ea2012113`); merge `636b736`, CI run 34592671643 success, `deploy.sh verify` six PASS.

### Pass 72 — PGCE G3: evidence narratives (PG-06), experience ledger (PG-07), reviews & transition (PG-09); What's new restored — 11 September 2026

Work items: the plan's Pass 72 ([archive/enhancements/2026-09-11-pgce-student-experience/DEVELOPMENT_PLAN.md](archive/enhancements/2026-09-11-pgce-student-experience/DEVELOPMENT_PLAN.md) §G3) plus the owner's instruction that What's new is updated with every release. Branch `pgce-g3`. `shared/contracts.js` gained four collections and `shared/evidence.js` is new, so both workers deploy first. With G3 complete the audit package is archived (plan §4.7).

Behaviour before → after:
- **Evidence examples** (`EvidenceExamplesSheet`, PGCE → Evidence & reflections card): a narrative — situation, decision, what I noticed, what I changed next — over **references** to existing lessons, feedback, meetings and reflections (one lesson can sit in many examples with no copied blob; removing an example never touches the lesson; a removed source shows as "no longer present"). Contexts: course curriculum / practice cycle / provider assessment. **ITTECF areas and Teachers' Standards are two separate reference lists with no combined score; Part Two is a list of conduct contexts only.**
- **Review packs** (`ReviewPackSheet`, same card): a pack **pins the canonical JSON of each selected record at selection time** (with its revision), so an old pack stays readable after the source is edited and says "source changed since pinned — the pack keeps the pinned version"; captions per item; feedback carries its provenance. Attachments from the wallet are listed **by name with a live state — on this device only, or missing** — and never travel as bytes in synced data. Selected → Draft → Discussed (with a date) is the learner's own marking. **The on-screen preview and the exported text file come from the same function and object, so export equals preview** (tested byte for byte, including a missing attachment line). Every pack ends "Nothing in this pack is an assessment or an award."
- **Experience ledger** (`ExperienceSheet`, Placements card): per placement, four layers kept apart and **never summed** — planned, logged by you, discussed/reviewed, provider outcome (referenced, with its source required) — by activity type (mentor meeting, observation, teaching, provider-designated ITAP, other), date and an optional duration that is **only ever what was entered** (untimed entries are counted, not inferred). The same date, type, layer and source is refused as a double count. The timetable's planned school days, whole-day ticks, minute corrections, excluded and inset days are shown from the existing placement code with its inset policy, unchanged. **Comparisons run only against a requirement the learner has confirmed** ("1 of 120 (Assessed school days, confirmed by you from …)"); otherwise the total stands with no deficit. The provider-conversation export names a source for every value.
- **Reviews & handover** (`ReviewSheet`, Development menu): a review record — date, participants, focus, questions, **private notes (never exported)**, a provider judgement that cannot be saved without its source, next steps, the pack discussed. The optional **first-post handover pack** is built from evidence examples, experience by layer and review next steps with private notes, contacts, addresses and identifiers left out; it states it was prepared by the learner and that no induction body has received it; export equals preview. No field anywhere can set QTS.
- **What's new updated with every release** (owner, 11 Sep): `src/lib/changelog.ts` is now a dated list of entries (`WHATSNEW_ENTRIES`, newest first; the version follows from it); a once-per-version banner on Today (`WhatsNewBanner`) shows the latest entry with "Got it" and a link to the history in Settings → Help & privacy, which lists every entry; the banner had been missing since the V-series redesign. Entry 7 covers Passes 68–72 and the iOS nav fix. AGENTS.md decision rule 5 makes the changelog entry part of every release pass.
- Accessibility fix found by the tests: chip and checkbox groups were wrapped in a `<label>`, which gives the first control the whole group's text as its name; a `FieldGroup` (a grouped `div`) now wraps every chip grid and checkbox list in the PGCE sheets.
- Contract: `examples`, `reviewPacks`, `experience`, `reviews` optional collections with enum/link/snapshot validation; `ADMIN_SCHEMA_VERSION` 6.

Automated checks + results: `tests/unit/pgce-g3.test.mjs` (6: pinned snapshots survive source edits and export == preview; one record in many examples without a copied blob; ledger double-count rule, layers apart, untimed not inferred; comparisons only against confirmed requirements with no deficit wording; handover excludes private notes/contacts/identifiers; wire contract + merge round trip) and `tests/pgce-g3.spec.ts` (5: examples end to end incl. separate framework lists and removal leaving the lesson; review pack pin → source edit → stale note → missing attachment → download equals preview → Draft → Discussed; ledger with duplicate refusal, provider source, timetable ticks line, confirmed-requirement comparison and sourced export; reviews with a source-required judgement and the handover excluding private data; What's new banner once and history in Settings). `npm run validate` AND `VERCEL=1 npm run validate` green — **190 unit and 165 browser tests** (first full run: the TT-20 menu list, which now includes Reviews & handover).

Archive: on completion of this pass the package (then `pgce-student-audit-2026-09-10/`) moved to `archive/enhancements/2026-09-11-pgce-student-experience/` with a README (audit, plan, designs, evidence). Production code, permanent tests and runbooks stay in place; G4 (mentor portal) remains out unless separately requested.

Released 11 September 2026: workers deployed first (push `5c7f4c96-a7f3-46f8-9c1f-0d6086a99484`, feed `92857cd7-50f9-44f0-a4ae-a59836eb1232`); merge `1e1a47d`, CI run 34597979050 success, `deploy.sh verify` six PASS.

### Next workstream — PGCE / QTS student-experience audit (planned 11 September 2026)

The owner asked for the deployment of `archive/enhancements/2026-09-11-pgce-student-experience/PGCE_QTS_STUDENT_EXPERIENCE_AUDIT.md` to be planned in batches. The plan is [archive/enhancements/2026-09-11-pgce-student-experience/DEVELOPMENT_PLAN.md](archive/enhancements/2026-09-11-pgce-student-experience/DEVELOPMENT_PLAN.md): G0 (Pass 68) placement/provenance foundations and a reviewable tag migration; G1a (Pass 69) P1/P2/P3 school workspaces with outward/return journeys plus the course-aware roadmap; G1b (Pass 70) lesson workbench, practice cycle and mentor preparation; G2 (Pass 71) knowledge goals, academic workspace, workload; G3 (Pass 72) evidence narratives, experience ledger, review packs; G4 (portal) only on a separate go-ahead. Owner questions are in its §5. G0–G3 shipped as Passes 68–72; the package is archived at [archive/enhancements/2026-09-11-pgce-student-experience/](archive/enhancements/2026-09-11-pgce-student-experience/README.md). G4 (mentor portal) is out unless separately requested.
