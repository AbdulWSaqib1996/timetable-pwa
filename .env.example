# Build-time public configuration (no secrets belong here — never a client secret).
# Google Drive backups (R5b / NF-09): the OAuth *client id* of the dedicated Google Cloud
# project `my-timetable-backups-260910` (see GOOGLE_DRIVE_SETUP.md). Empty = the Google
# Drive card stays in its explanatory disabled state.
VITE_GOOGLE_OAUTH_CLIENT_ID=
